# Work Helper / Super Troubleshooter Handoff

**STATUS / TASK:** WR-148 nonproduction deployment/restore evidence feasibility, diagnosis only; Workflow V3.5, STANDARD_CHAT_HIGH / FAST_REFRESH. Assigned branch `wr-148-nonproduction-recovery-evidence-feasibility` from exact canonical `ad81c79d39c8b7b7ca904c47bf5d5e8fbc865e84`, initially 0 ahead/behind.

**RESULT:** `NONPRODUCTION_EXPERIMENT_PLAN_READY` as a future, unexecuted local experiment. Select one loopback-only static fixture/Playwright browser architecture: freeze accepted current A runtime assets at an exact immutable Git SHA/tree and manifest; create a benign B copy with visible nonproduction badge and fixture-only worker cache-name change; atomically switch A → B → original A on the same local origin; verify four checkpoints, browser controller/cache, ordinary and diagnostic requests, no-provider smoke, exact restored bytes and cleanup. The current scratch has Playwright package but no Chromium executable; existing full CI installs Chromium. A separate Manager-authorized task must provision the browser and execute in an outbound-blocked, isolated profile/container. No site, repository, account or Pages resource is requested.

**LIMIT:** This tests local byte transport, service-worker lifecycle, browser state and frozen restoration only. GitHub Pages main/root Jekyll build, artifact upload, deployment/status, CDN propagation, guarded main merges and production visitor convergence remain `NOT_COVERED` or `UNKNOWN / NOT PROVEN`. It is never a passed production rollback. WR-147's rejected historical production versions are not fixture B. The full fidelity matrix, source/provenance contract, exact A/B manifests, required resources/approvals, four checkpoints, abort and fresh independent Auditor gate are in `.ai/work_helper/WR148_NONPRODUCTION_RECOVERY_EVIDENCE_FEASIBILITY.md`.

**ONE NEXT MANAGER RECOMMENDATION:** `AUTHORIZE_SEPARATE_NONPRODUCTION_EXPERIMENT_TASK`, scoped to ephemeral local fixture/harness and browser execution with network isolation, immutable receipts, cleanup and later distinct Independent Auditor/QA. This recommendation is not execution authority in WR-148 and does not decide formal A6 or release.

**PUBLICATION:** Exactly this handoff plus `.ai/work_helper/WR148_NONPRODUCTION_RECOVERY_EVIDENCE_FEASIBILITY.md`; final immutable PR SHA and applicable exact-head Governance receipt are in the Manager-facing return/PR metadata, product should SKIP. Work Helper does not merge.
