# WR-151 — Dormant GitHub-Hosted Inert Loopback-Egress Preflight (Builder Implementation)

**STATUS:** IMPLEMENTED AS SOURCE / DORMANT. First publication, exact-final-head FULL CI, Governance and dedicated-job SKIPPED receipts must be independently read from the live draft PR after this documentation/handoff publication. This is **NOT an executed preflight, security/isolation PASS, independent audit, runner qualification, WR-149 recovery, WR-148 A→B→A, production recovery or release evidence**.

## Authority, custody and exact write scope

- Workflow V3.5; WR-D077 / WR-151; `STANDARD_CHAT_HIGH` / `FAST_REFRESH`; assignment `STRICT IMPLEMENTATION — DORMANT HOSTED-RUNNER INERT PREFLIGHT ONLY`.
- Verified before any write: canonical `main` and assigned `builder/wr-151-inert-hosted-runner-loopback-preflight` both `55e3de8068c974870cd245811302741d322ba4ae`, 0 commits ahead/behind; `ACTIVE_TASKS.json` assigns WR-151 alone. Accepted WR-150 PR #420 was documentary diagnosis only and returned `RUNNER_PREFLIGHT_IMPLEMENTATION_SCOPED`, **not an isolation pass**.
- One NEW DRAFT Builder PR on that branch, base `main`. Its final head must be fetched from the unchanged PR after publishing the separate handoff; do not mistake the pre-documentation implementation head for final. Exactly FIVE cumulative paths: `.github/workflows/ci.yml`, `scripts/wr150-loopback-netns.sh`, `scripts/wr150-loopback-preflight.mjs`, this WR-151-specific evidence, and `.ai/builder/HANDOFF.md`.
- No product/Pages assets, real War Room app, rankings, datasets, Companion, source import, live ESPN/provider address, production URL, user WSL/self-hosted runner, deployment, credential, package or lockfile, other test, other workflow, Manager/Auditor/shared file or label is written or contacted. No pilot has been dispatched in this Builder lane.

## Technical first-trigger control

The existing War Room CI `push` and default `pull_request` opened/reopened/synchronize event coverage remains; the sole event change is explicit `pull_request.types: [opened, reopened, synchronize, labeled]`. Existing classify, Governance, bootstrap-reuse and product job bodies/decisions are preserved. One new job `wr151-inert-hosted-loopback-preflight` has a **job-level** condition requiring ALL of:

`github.event_name == 'pull_request'`; actual `github.event.action == 'labeled'`; **event** `github.event.label.name == 'wr151-inert-preflight-reviewed'` (NOT label membership); PR `state == open` and `draft == true`; target branch `main`; exact same-repository `head.repo.full_name == github.repository`; exact `head.ref == builder/wr-151-inert-hosted-runner-loopback-preflight`.

Therefore ordinary push, initial DRAFT PR opening, re-opening, synchronization or stale preexisting label, unrelated PR, fork, other branch or target MUST give this dedicated job **SKIPPED**. The label is intentionally absent from first publication and is never created/applied in this lane. A label alone is NOT a cryptographic audit proof or branch protection: Manager must independently accept a fresh exact-head security audit and approve ONE purpose-specific action on unchanged head. Any head movement requires new freeze/audit and no automatic relabel/retry. No `workflow_dispatch`, `pull_request_target`, deployment environment, secret, private runner or self-hosted label exists in this job.

The dedicated job uses fixed `runs-on: ubuntu-24.04` and `permissions: contents: read`, `timeout-minutes: 20`, `actions/checkout@v7` with explicit `github.event.pull_request.head.sha` and `persist-credentials: false`, and Node 22. Before installing or running inert code it logs explicit PR-head, base and synthetic GitHub merge SHA, requires checked-out `git rev-parse HEAD` to equal event head, requires event head repository/branch/label to match, and rejects any `package.json` or `package-lock.json` change from the event base. Package-lock SHA-256 is logged without credentials. Provisioning (`npm ci --ignore-scripts`, then `npx --no-install playwright install --with-deps chromium`) occurs on the **disposable hosted VM only, before** isolation; dependency lifecycle scripts are disabled. A second exact-head check precedes the shell wrapper. Static `node --check` / `bash -n` are in this future job, **not run by this Builder**. Browser installation/namespace capability on that future runner remain unverified.

## Namespace, privilege and IPC controls (design, UNEXECUTED)

`scripts/wr150-loopback-netns.sh` refuses non-`github-hosted`, non-PR, missing/moved expected head, root-start or missing `sudo`/`ip`/`unshare`/`setpriv`/browser-cache prerequisites. It logs only head, package-lock hash and sanitized isolation receipts. On a **later separately labeled job only**, it creates one randomized run-scoped `sudo ip netns add` namespace, enables `lo` only, requires exactly one loopback interface and empty IPv4/IPv6 main/default routing tables, then launches its entire Node→Playwright→Chromium process tree through `ip netns exec`. An additional mount namespace masks host `/run`, `/tmp`, `/var/tmp` and `/dev/shm` filesystem socket paths; accessible runner-workspace/cache Unix socket paths are rejected. The child closes inherited non-stdio FDs, drops to the unprivileged runner UID/GID via `setpriv` with empty groups, `--bounding-set=-all`, `--inh-caps=-all`, `--ambient-caps=-all` and `--no-new-privs`, and uses `env -i` carrying only minimal PATH, ephemeral HOME, TMPDIR, namespace/head/inode, locked browser-cache location and NODE_ENV=test. No host proxy, broker, GitHub token or credential environment is passed. The private temporary HOME/profile and fixture are not production assets.

Inside that namespace, the Node preflight independently compares its namespace inode against the recorded host inode; requires nonroot UID, zero inheritable/permitted/effective/bounding/ambient caps, `NoNewPrivs: 1`, only loopback and no default/nonlocal main routes; rejects ANY inherited proxy/broker variable **names** or extra inherited socket FDs before fixture creation. It serves exclusively a small constant inert HTML document, constant inert service-worker JS and constant local `/ok` response on an ephemeral `127.0.0.1` port. No War Room HTML/JS, ESPN, ranking input or page origin is loaded.

Each of two Chromium scenarios (scrubbed direct and documentation-only explicit proxy path) must positively confirm actual local page load, active `navigator.serviceWorker.controller`, local `/ok` fetch and a live unique Chromium DevTools `service_worker` target. It independently observes browser OS PID from `/proc` parentage (not an unsupported Playwright `browser.process()` API), recursively validates every observed Chromium descendant's namespace inode, UID and effective capabilities, and requires a **unique** kernel-named ServiceWorker thread in that descendant tree for OS-level worker PID attribution. The CDP live worker target alone is explicitly insufficient: if this runner/Chromium does not expose a credible unique worker PID, classification is `UNVERIFIED` and the pilot FAILS closed rather than inventing a worker identity.

Both the page and the controlling service worker must independently attempt bounded `no-cors` fetches to **documentation-reserved literal IPs only**: `198.51.100.1:443` and `[2001:db8::1]:443`. Actual network success always FAILS. Complementary direct TCP probes must return kernel `ENETUNREACH`/`EHOSTUNREACH`/unavailable-family, not timeout or a CORS/TLS/Playwright interception excuse; actual browser-context `requestfailed` receipts must distinguish page versus service-worker-owned requests with network-layer `ERR_*` denial. Missing worker request attribution, missing Chromium process attribution, failed local control, route drift, browser/worker failure, unexpected external success, or ambiguous error provenance fail the gate. The separately scoped proxy test deliberately injects only a reserved-address proxy variable then removes it BEFORE browser launch; its Chromium explicit proxy path goes only to that same reserved literal IPv4 address with loopback bypass and must also be denied by the namespace. It neither imports nor routes through any host proxy/broker.

The shell EXIT/signal trap terminates only PIDs in its unique disposable namespace (TERM then KILL if necessary), verifies no namespace process remains, deletes the ephemeral namespace, erases the private fixture/profile directory, and emits `WR151_CLEANUP_PASS` only on verified success. Any cleanup failure makes the job nonzero/UNVERIFIED. Forced SIGKILL, unavailable namespace capability or a future runner/browser difference may prevent a positive cleanup receipt; this is a FAIL, not authorization to use the user's persistent Ubuntu WSL runner or to relax scope. GitHub-hosted VM teardown is not itself a successful isolation receipt.

## Explicit boundaries / unverified items / next gate

**Unverified until a separate FIRST pilot after independent audit:** future hosted VM image and privilege/namespace support; Playwright/Chromium installation; actual service-worker network interception and unique OS worker PID attribution; Chromium process-tree closure, proxy-path behavior and kernel/browser error codes; route, mount, FD/IPC and teardown receipts. This Builder lane does NOT run `scripts/wr150-loopback-netns.sh` or `scripts/wr150-loopback-preflight.mjs`. Source syntax and event-gate safety need independent static review, and observable FIRST PR / synchronize dedicated-job SKIP and exact-final-head applicable FULL CI/Governance/product result must be recorded from real GitHub CI before Manager freeze.

**Mandatory sequence:** Builder publishes one DRAFT five-file PR and stops writes on one exact final head after actual ordinary-PR FULL CI SUCCESS with pilot SKIPPED. Manager independently freezes that exact unchanged implementation and activates a FRESH independent security/workflow Auditor on a separate task/branch/PR. Only a separately accepted audit PASS-family and distinct Manager approval can authorize a single labeled first inert pilot on the same unchanged head. A real pilot, if later executed, requires fresh independent execution-evidence audit and Manager disposition BEFORE any possible integration or future WR-148 local A→B→A task. A failed pilot means `NO_QUALIFIED_RUNNER`/defer, not automatic retry. Any later guarded workflow integration separately requires actual same-landed-SHA canonical-main FULL CI.

**No inference:** This inert preflight is not production War Room app/service-worker/Pages traffic qualification, A→B→A recoverability evidence, actual Pages build/deploy/CDN rollback/visitor convergence, public-site mutation, A6 closure, 2027/A4, Track B, provider contact or draft-ready release.
