# WR-126 — Legacy Overall Board Pressure rendered truthfulness reproducer

STATUS: **REPRODUCED** in the existing real-page mounted production widget under a bounded source-free synthetic *display-input* fixture. Task/test-only diagnostic, NOT a product fix or live ESPN validation.
TASK/OWNER: WR-126 / Builder / Workflow V3.5 / STANDARD_CHAT_HIGH / FAST_REFRESH.
REPOSITORY: `Ryan42062001/The-War-Room`; original verified canonical main **and** branch creation baseline `eb0421bb67435a6e4b728e44b1791497ef5a7189`; assigned existing branch `wr-126-overall-board-pressure-rendered-truthfulness-reproducer`. WR-D040 is sole active authorization. Historical WR-125 inventory accepted/closed.

## What was actually executed

The test-only addition lives inside the existing `scripts/test-browser.mjs` and uses its normal real page/browser, real original 717-player source rows and production `updateDraftDayDashboard` to replace the content of the ACTUAL mounted `#draft-day-dashboard` inside the original `#board-pressure-widget`. This is NOT stand-alone injected static HTML, not a stubbed renderer, not an output fabricated by matching source code, not a mock of `calculateNextPickSurvival` or `getMarketTimingDetails`. It switches the actual board to Overall (where the widget is computed-visible), selects a real available RB row-backed player with genuinely missing ESPN board rank/ADP, FantasyPros ADP, real-time ADP and ADP rank from both the player object and original source-row attributes, and constructs a deterministic one-player RB **synthetic positional input pool** so this player is the ACTUAL rendered position card best. That restricted pool is intentionally synthetic; Isaac Guerendo is ECR #293 and the rendered card says “RB 0 relevant” under this early 160-pick draft. It does **NOT** establish that he would be the normally highest-ranked available RB during a live draft or that the entire natural production pool is unknown-market. It DOES establish what this production widget renders if a legitimate row-backed no-market player becomes its `bestAvailable`, a branch the widget explicitly supports. No row/data/engine/source attributes were mutated; original dashboard HTML and Position view were restored after each viewport.

The real configured draft at the fixture checkpoint had teams=10, rounds=16, user slot=10, current overall pick=1, actual next OWN pick=10, eight picks in between. For the synthetic display context the test uses this actual upcoming own pick (not the debug engine's *later* next-turn calculation, which points to #20); validates integer team/round/pick bounds, the next pick’s true snake ownership, consistent 10−1−1=8 intervening opponents, then calls the **unchanged** `calculateNextPickSurvival(player,context)`. It requires `getMarketTimingDetails(...).marketRank===null`, `source==='Unknown market'`, numeric engine neutral survival 50, absence of all five market fields on both row and player, and actual rendered identity matching the existing available row. The test executes the same controls at viewport **390×844** and **1280×900**.

### Actual mounted rendered evidence, both viewports

| Case | 390×844 | 1280×900 |
| --- | --- | --- |
| Unknown-market row-backed available RB card | Isaac Guerendo, ECR #293, market source `Unknown market`, resolved market rank `null`, ESPN rank/ADP and FP fallback ADP all `null`; actual card text normalized: `RB0 relevantIsaac Guerendo50% next-pick survival · No immediate cliff` | Same rendered identity, market fields, text and conditions |
| Internal numeric engine value versus DISPLAY | `calculateNextPickSurvival` = 50 sentinel; rendered text **`50% next-pick survival`**, meter inline width **`50%`**, meter `aria-label` **absent** | Same |
| Actual existing Overall widget/card rendered | `widgetVisible:true`, `cardRendered:true` from computed style, client rects and card dimensions | Same |
| Known-market SAME-POSITION control | Jahmyr Gibbs, ECR #3; ESPN board rank #1, market source `ESPN board`, known market rank 1; actual card text `RB1 relevantJahmyr Gibbs0% next-pick survival · No immediate cliff`, meter width `0%` | Same |
| Default Position-view control | Legacy `#board-pressure-widget` computed-hidden after switching back | Same |

The known-market control establishes that the source resolver is functioning, not that its displayed 0% value is a measured statistical probability or appropriate UI wording. The missing-market 50 is a deliberate internal neutral fallback, NOT evidence of a 50% chance. The fixture **records** this incorrect old copy as an observation and does not assert it as the desired behavior of a future repaired UI. Existing WR-122 repaired recommendation-card behavior is untouched.

### Invariance / exact test basis

Before/after in the actual same browser: `window.latestDraftRecommendation.player === 'Jahmyr Gibbs'`, `recommendation === 'DRAFT'`, primary market source `ESPN board`, same complete scored candidate order and every final score, same original market/ECR row attributes and classes for tested rows; all assertions passed. The fixture does not change current ranked candidate selection, action, weighting, numeric survival or position tiers. The test returns the literal `WR126_RENDERED_BOARD_PRESSURE` entries (compact logged identities, market preconditions, actual text/meter, view visibility and unchanged invariants), followed by `WR126_RENDERED_OUTCOME` with `classification:'REPRODUCED'` for both viewports. Actual production source: `js/war-room-ui.js:updateDraftDayDashboard` uses numeric survival for percent text, meter and urgency tone; `js/war-room-scoring.js:calculateNextPickSurvival` returns neutral 50 when market rank is unavailable and opponents intervene. `draft-polish.css` deliberately hides the legacy widget only in Position view.

**Historical test chronology, do not relabel failures:** The first test checkpoint `ebbc2bedeae7d3b6b15424d2d41f4b057f77422f`, War Room CI [#35554893703](https://github.com/Ryan42062001/The-War-Room/actions/runs/35554893703), had a failed fixture-precondition assertion: the debug context calculated later `nextPick=20` for an initial slot-10 draft, whereas actual next own pick is #10. This was a TEST construction error and does not demonstrate a UI failure. Repaired code checkpoint `9843d1b3b2b067698ea4e551a4882428514ef194`, FULL push War Room CI [#35554984927](https://github.com/Ryan42062001/The-War-Room/actions/runs/35554984927) **COMPLETED SUCCESS**: classify, Governance and actual full-test job SUCCESS; browser-determinism repeatedly logged identical `REPRODUCED` outcomes at both viewports; the full `npm test` also passed. Subsequent test-only commit `54b102d177aaf7ddfbdee37389517ffe6e68b141` makes CI logging bounded (does not change evidence/assertions). **The final PR head and exact-final-head FULL CI must be independently verified after writing documentary files.** Never substitute the earlier passing checkpoint for final-head evidence. `npm run test:browser`, phone, responsive and other suites are exercised through the full CI; no local terminal execution or separate standalone custom browser run is claimed.

## Classification, limits and ONE next Manager decision

**REPRODUCED — narrow legacy Overall Board Pressure presentation truthfulness gap**, under a clearly restricted synthetic input with actual unmodified row-backed no-market data. This proves the UI *can* present the engine's non-probabilistic neutral 50 as a calibrated-looking “50% next-pick survival”; it does NOT prove incidence among normally highest-ranked players in natural full-board state, a physical phone screenshot, real ESPN/Companion E2E, live Direct, market freshness or empirical calibration. Existing recommendation card truthfulness fix WR-D038 and other Command Center features remain intact.

**Proposed smallest separate future Manager-authorized display-only fix** (NOT authorized or implemented by WR-126): existing `js/war-room-ui.js:updateDraftDayDashboard` plus focused assertions in existing `scripts/test-browser.mjs` only. When `getMarketTimingDetails(best,context).marketRank==null` with a legitimate nonadjacent next pick, show **“Market timing UNKNOWN — no survival estimate”** rather than `50% next-pick survival`; do not paint a 50%-length probability-like meter or assign a scarcity/urgency label based on the neutral 50. Retain true tier/cliff/count data. For known-market scenarios, label the current percentage-like output as a **heuristic timing signal rather than an empirical survival probability**, without changing engine scoring/timing. Any replacement visual tone/meter/urgency when market is UNKNOWN needs explicit Manager-defined display policy, not Builder-invented thresholds. Preserve valid adjacent-own-turn factual semantics only when actually established and the second pick remains eligible. No scoring/ranking/market resolver/Companion/dataset/weights/player-action edits; no new Command Center, roster, queue or sync badge. Require new separately assigned Builder task and fresh independent Auditor, actual known/unknown/Position browser controls at both widths, phone/responsive/scoring/WR-118/full exact-head CI, and mandatory canonical-main FULL CI after any later integration.

**Manager immediate gate:** review exact WR-126 final three-file test-only PR, its bounded observed result and genuine exact-final-head FULL CI. If accepted, integrate this diagnostic via Manager authority with full canonical-main canary, then separately decide whether to commission the above minimal future display correction. No WR-126 production patch, self-merge, employee activation, provider contact or release.
