# WR-127 — Overall Board Pressure truthful market display: implementation and evidence

TASK: WR-127 | OWNER: Implementation Engineer / Builder | WORKFLOW: V3.5 | MANAGER AUTHORITY: WR-D041.
REPOSITORY: `Ryan42062001/The-War-Room` | ASSIGNED BRANCH: `wr-127-overall-board-pressure-truthful-market-display`.
IMMUTABLE VERIFIED INITIAL MAIN AND ASSIGNED BRANCH: `907ed83b301fae0340c07f124a431952990918b9`, after merged Manager activation PR #359. WR-127 was independently read from the active-only registry as the sole assigned task before any Builder write. Earlier accepted WR-126 PR #358 demonstrated a **bounded actual-mounted-render** missing-market display problem; do not overstate this as natural full-board frequency.

## Exact production change — `js/war-room-ui.js` only

Only `updateDraftDayDashboard(liveState,scarcityState)` changes, specifically the legacy Overall Board Pressure **display**, not the engine or shared source metadata:

* Resolve the current best player with the existing `getMarketTimingDetails(best,context).marketRank`. Only a finite positive resolved rank is known market. Preserve the same numeric `calculateNextPickSurvival(best,context)` invocation/result for known-market cards. In missing-market state, skip exposing the engine's internal neutral value of 50 in the display; the engine itself remains untouched.
* Missing market: render **`Market timing UNKNOWN — no survival estimate`** with the original cliff note and ECR-relevant/count/position/best-player text. Omit the `.board-pressure-meter` element altogether, not merely a visually hidden 50%-width child; no accessible numeric survival label is created. Where independently derived scarcity status is not CRITICAL CLIFF, TIER CLOSING, or HIGH SCARCITY, assign neutral `pressure-unknown` instead of a class inferred from the internal 50. Preserve these existing independently supplied tier warnings if actually present. No new thresholds or urgency policy.
* Known market: the unchanged 0–100 engine value remains the displayed meter width, but its user-facing text becomes **`Market timing index (heuristic): N / 100`** (not `N% next-pick survival` or an empirical chance), retaining original cliff/context and existing input-resolution hierarchy. Meter is marked `aria-hidden="true"` so assistive tech reads the single explicitly qualified visible textual index rather than inferring a probability from a decorative bar. No CSS, index, rank/score, data, source resolver, draft-state, Recommendation Card WR-122, Companion, package, or workflow changes.
* No player winner, selected action, candidate order, ECR value, final score, numeric engine output, positional tier calculation, next-pick calculation, source rows or initial draft configuration is written/changed.

## Actual mounted regression — `scripts/test-browser.mjs`

Extended the existing WR-126 diagnostic **without a new runner, simulated HTML replacement, new test script or market-data mutation**. For each Chromium viewport **390×844** and **1280×900**, use the actual loaded application; the *actual mounted* legacy Overall `#board-pressure-widget > #draft-day-dashboard`, production `updateDraftDayDashboard`, original available row-backed no-market RB Isaac Guerendo (ECR #293), and known same-position ESPN-board player Jahmyr Gibbs. The synthetic positional *display input pool* restricts bestAvailable to this one true player in each control, and the original dashboard is restored after checking. This does **not** prove Isaac is a natural full-board top RB; the test deliberately reproduces the accepted WR-126 display branch and does not re-rank any player.

Preconditions asserted: both candidate and row identity match and remain available; unknown player's ESPN board rank/ADP and FantasyPros ADP, real-time ADP, and ADP rank are naturally missing on the player AND its original row; `getMarketTimingDetails(...).marketRank === null` with `Unknown market`; known-market same-position rank is finite and positive. The actual configured ten-team, 16-round snake, own slot 10, current #1, upcoming own #10 and eight intervening selections pass integer/ownership and next-pick coherence checks. The original engine still returns numeric 50 for unknown timing; tests intentionally assert that **internal** fact while requiring the **display** to withhold that sentinel. Known-market numeric engine output is verified to equal the visible heuristic index and the same decorative meter width.

The two actual-render fixture cases assert visible/copy/accessibility outcomes: unknown says UNKNOWN/no estimate, contains no `50%`, `50 / 100`, `N% next-pick survival`, no meter node, no accessible numeric survival label, and neutral `pressure-unknown` under the synthetic `NORMAL` tier status. Additional independent-tier control uses `TIER CLOSING` and one player before cliff to require the retained `pressure-limited` class and `1 before tier drop` text despite unknown market, with no meter. Known ESPN control displays `Market timing index (heuristic): N / 100`, NOT an unqualified percent survival, and its existing numeric meter is accessible-hidden. Returning to Position hides the legacy widget. The test also compares before and after complete scorer candidate order, every final score, existing recommendation player/action/market source and original player-row ECR/market attributes/classes. Earlier WR-122 card rendering/accessibility/negative-turn browser assertions remain in the same original suite and were not modified.

Actual diagnostic log markers: `WR127_RENDERED_BOARD_PRESSURE` (bounded per-viewport preconditions, user-visible text/meter, source, preserved invariants) and `WR127_RENDERED_OUTCOME` (requires truthful display in BOTH viewports; never enshrines misleading `50%` copy as desired behavior).

## Required actual commands and execution provenance

Existing package/CI invokes `node --check js/war-room-ui.js` and `node --check scripts/test-browser.mjs` as part of `npm run test:syntax`; separately, the existing browser runner performs its literal `node --check scripts/test-browser.mjs` and `node --check js/war-room-rankings.js` inside the hosted `test:browser` entry point. Verify both exact literal edited-file checks and their output on the FINAL head. Applicable actual hosted commands are `npm run test:browser`, `npm run test:phone-decision-view`, `npm run test:responsive-overflow`, `npm run test:scoring-corrections`, `npm run test:wr118-espn-replay-reconnect`, and `npm test` under the repository's existing FULL War Room CI pipeline; inspect exact-head actual test-job logs for the completed verdict before declaring PASS. The former WR-126 source baseline has already passed genuine canonical-main FULL CI; that historical pass is NOT sufficient for this new code.

**Execution limit:** A local Work-mode computer/terminal handoff was offered but not accepted by the client in this turn. This Builder used the connected GitHub repository operations to commit in scope and GitHub-hosted real Chromium/Node CI for actual browser execution, rather than claiming local terminal/physical-device testing. No live ESPN, private-room access, downloaded provider data, real Companion transport, custom model/protected scoring, or provider outreach.

**Final-head validation record:** The immutable final Builder SHA, source/test commit checkpoints, EXACT final PR-triggered FULL CI run ID/test+Governance job IDs, results, and concise actual rendered values must be attached to the same PR **description/comment** after CI completes; do not append a new documentary commit after pinning the head and pretend earlier CI validated it. Do not classify historical or intermediate code-head runs as final-head evidence. If full CI fails, repair only within the four authorized files, make a new immutable head, rerun and report the original failed historical run honestly. Manager independently verifies PR branch/head, full four-file diff, test logs and freezes the exact target; Builder never self-freezes in Manager registry.

## Limits and Manager gate

The synthetic input is a controlled display path, not a measured population rate for normal live drafts, physical-device accessibility certification, statistical survival calibration, independent ESPN Direct completion, per-player market freshness or release readiness. Known-market output remains an **uncalibrated heuristic index**, not measured odds. Passing Builder CI is NOT fresh independent QA. This task requires Manager frozen-head review, fresh independent Auditor on a distinct branch/task/PR, PASS-family on EXACT unchanged target, later separate Manager merge and canonical-main FULL CI canary. No implementation merge, Auditor activation, deployment, or downstream employee work is authorized from this Builder lane.
