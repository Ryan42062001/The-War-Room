# Implementation Engineer / Builder — WR-151 Dormant Hosted Runner Inert Preflight Handoff

STATUS | TASK | ROLE | BRANCH | BASE | HEAD | PR | DONE | CHANGED | TESTS | CI | BLOCKERS | DECISIONS CONSUMED | NEXT ACTION | FILES / ARTIFACTS THAT MATTER | DO NOT REPEAT

**STATUS:** WR-151 source implementation prepared as a DORMANT, pre-audit SKIPPED hosted-only inert preflight. No namespace/browser/service-worker/egress pilot was executed in this Builder lane. Final head and actual first DRAFT-PR full CI/pilot-SKIPPED receipts must be read from GitHub after this handoff publication; no security PASS or runtime qualification is claimed.

**TASK / ROLE:** WR-151 / WR-D077; Implementation Engineer / Builder, Workflow V3.5, STANDARD_CHAT_HIGH / FAST_REFRESH, STRICT IMPLEMENTATION ONLY.

**BRANCH / BASE:** `builder/wr-151-inert-hosted-runner-loopback-preflight` from verified identical canonical main `55e3de8068c974870cd245811302741d322ba4ae` (initial 0 ahead/behind).

**HEAD / ONE PR:** Freeze immutable exact final Builder SHA from live NEW DRAFT PR on this assigned branch, verify exact FIVE-file diff, then observe actual exact-head applicable FULL War Room CI/Governance/product and dedicated pilot job SKIPPED on ordinary PR opened/synchronize. Leave DRAFT / OPEN / UNMERGED; never apply activation label here.

**DONE:** Existing normal `push` and PR opened/reopened/synchronize semantics retained; explicit PR labeled event added solely to permit future independent Manager-accepted single-purpose activation. New job `wr151-inert-hosted-loopback-preflight` is job-level gated on actual labeled action, exact event.label.name `wr151-inert-preflight-reviewed`, open DRAFT same-repository PR, exact assigned head branch and main base. Ordinary first PR/open/synchronize/push must SKIP it, even if prior label membership persists. Fixed `ubuntu-24.04`, contents:read, checkout exact event head SHA with `persist-credentials:false`, record merge/head/base, reject package/lockfile drift, provision locked dependencies/Chromium before isolation, no workflow_dispatch/pull_request_target/self-hosted/secret/deployment. Future inert-only wrapper creates ephemeral loopback-only netns, drops privilege and inherited env/broker socket paths, runs inert HTML + controlling SW and reserved-IP page/SW/proxy negative probes, requires browser/worker OS PID/netns evidence, and fails closed on any ambiguity or failed teardown. Static design and full future gate details are in task evidence; NO runtime result is asserted.

**CHANGED:** EXACT FIVE cumulative paths ONLY: `.github/workflows/ci.yml`, `scripts/wr150-loopback-netns.sh`, `scripts/wr150-loopback-preflight.mjs`, `.ai/builder/WR151_INERT_HOSTED_RUNNER_PREFLIGHT_IMPLEMENTATION.md`, this `.ai/builder/HANDOFF.md`. No other repository/runner change, no active Ubuntu WSL self-hosted runner target, no public War Room site/Pages or ESPN/provider interaction.

**TESTS / CI:** Source has NOT been executed as a pilot; neither `sudo ip netns add`, browser launch, SW activation nor network probes were triggered by Builder. Normal PR FULL CI and Governance and the dedicated pilot SKIPPED on actual opened/synchronize are required from real exact-final-head GitHub Actions logs. `node --check`/`bash -n` occur inside the future inert job but cannot be counted as executed before it is separately approved; existing product test job must complete independently. Any missing/failed/accidentally executed pilot gate is BLOCKED/STOP, not a success. No future runtime or audit has been promised/claimed.

**BLOCKERS / UNVERIFIED:** Future disposable hosted sudo/namespace/browser capability, browser/SW PID attribution from kernel thread names, service-worker requestfailed network provenance, reserved IPv4/IPv6 negative result, proxy/IPC bypass absence and cleanup all remain UNVERIFIED pending fresh independent security audit and at most ONE separately authorized inert pilot. If direct worker PID/request or route proof cannot be established, fail `UNVERIFIED` rather than relax assertions. User's persistent Ubuntu WSL runner and WR-149 recovery remain outside this experiment.

**DECISIONS CONSUMED:** WR-D077 single dormant-job assignment; accepted WR-150 `RUNNER_PREFLIGHT_IMPLEMENTATION_SCOPED` and Manager WR-150 acceptance comment 5805671131; WR-149 `BLOCKED_BROWSER_OR_RUNNER` is NOT a production failure or recovery pass; 2027/conditional release plan and all formal A6/custody/rights/provider prohibitions unchanged.

**NEXT MANAGER ACTION:** Independently refresh canonical main, DRAFT PR/head/5-file diff, examine actual PR event/if expression and checkout safety, nonproduction inert source, browser/SW/proxy/PID/network fail-closed claims, static syntax and first-publication FULL product/Governance logs plus pilot SKIPPED. Freeze the unchanged exact Builder head only if those receipts qualify; activate a FRESH separate Independent Auditor/QA security/workflow task/branch/PR. No label/pilot before separate Manager acceptance of exact-head fresh independent PASS-family and a subsequent explicit ONE pilot activation decision. Any pilot run needs its own separate execution-evidence audit and Manager disposition before any possible integration.

**FILES / ARTIFACTS THAT MATTER:** `.ai/manager/WR-151.md`, `.ai/work_helper/WR150_HOSTED_RUNNER_ISOLATION_QUALIFICATION.md`, five authorized PR files, exact head and genuine first PR FULL CI / pilot SKIPPED job IDs.

**DO NOT REPEAT:** No Builder self-audit/merge/label, no pilot run, A→B→A, public site, Pages, production, ESPN/provider, source/ranking/dataset, WSL/self-hosted runner, credential, deploy/rollback, Track B/A4/2027/A6 or draft-ready declaration. A successful ordinary PR CI is NOT an isolation PASS.
