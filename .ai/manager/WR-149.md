# WR-149 — Isolated Local Recovery A → B → A Execution Evidence

TASK ID: WR-149
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
ASSIGNMENT MODE: CROSS-ROLE RECOVERY — BOUNDED LOCAL NONPRODUCTION EXPERIMENT ONLY
STATUS: ASSIGNED — execute only after WR-D075 Manager activation exact-head Governance SUCCESS, guarded merge, genuine canonical-main PUSH Governance SUCCESS and new fresh exact-current-main worker branch 0 ahead/0 behind
DATE: 2026-09-23
WORKFLOW: V3.5 CANONICAL
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: wr-149-isolated-local-recovery-evidence-execution
ASSIGNED BRANCH: wr-149-isolated-local-recovery-evidence-execution
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: f6fe0c002219c9729d92003a520398135c7a4e13
DEPENDENCY: INDEPENDENT
AUDIT REQUIRED: NO — this is execution/evidence publication, NOT Manager acceptance of experiment correctness; a separate fresh independent Auditor is REQUIRED after Worker evidence
POST-MERGE CANARY REQUIRED: NO — evidence-only documents; no production files modified

## Prior acceptance and hard distinction

WR-148 PR #416 exact Worker head ad4fb06d080398d019aa068b2d0ee4d81b1c04b0, exactly two Worker documentation paths, exact-head War Room CI #35904139101 SUCCESS / Governance #107327478967 SUCCESS / product #107327553260 SKIPPED. Manager acceptance comment 5800802273 accepts NONPRODUCTION_EXPERIMENT_PLAN_READY; evidence integrated canonical f6fe0c002219c9729d92003a520398135c7a4e13; genuine post-integration main War Room CI #35904602828 SUCCESS / Governance #107329064304 SUCCESS / product skipped.

WR-147 REHEARSAL_NOT_SAFE remains binding: NEVER expose rejected old canonical product to public Pages. WR-148 is a plan, not evidence of executed restoration. Manager separately authorizes WR-149 to execute ONLY the chosen local ephemeral loopback fixture architecture described in .ai/work_helper/WR148_NONPRODUCTION_RECOVERY_EVIDENCE_FEASIBILITY.md. GitHub Pages dynamic build/upload/deployment/status/CDN, actual production rollback, guarded main restoration and recovery of real users remain NOT COVERED. A local PASS is only a claim for later independent verification, never formal A6/release readiness.

## Exact allowed source and product mutation limits

Read-only inspect main, active registry, workflow, current Manager spec, Work Helper handoff and WR-148 design, accepted WR-143 product-origin commit 45cab0c189c284b4a3011b78ce953b99dd857194, accepted WR-146/147/148 evidence, actual service-worker.js and existing scripts/test-browser.mjs for safe reuse. At execution freeze one exact THEN-CURRENT accepted product tree and record its SHA; never silently switch to HEAD when product bytes change.

All A/B changes are exclusively in a NEW disposable local temporary fixture OUTSIDE checkout and user profile. A is immutable exact allowlisted runtime bytes from frozen Git tree; B changes only COPIED index.html for clearly visible NONPRODUCTION RECOVERY FIXTURE B marker and COPIED service-worker.js cache name (unique B); restored A uses the EXACT same original immutable A fixture directory, never an inferred recreated approximation.

Repository writes are limited to exactly:
1. .ai/work_helper/WR149_ISOLATED_LOCAL_RECOVERY_EXECUTION_EVIDENCE.md
2. .ai/work_helper/HANDOFF.md

An optional separately created evidence archive may be supplied as an attached, checksum-identified local artifact but may NOT be committed to repository without new authorization, and may NOT contain user data, credentials, live browser profiles or unrelated data. Keep essential immutable receipts, test failures, manifest/hashes and concise sanitized logs in the report for independent reviewer reproducibility; do not claim inaccessible archive bytes were independently checked.

No PR/test/product/workflow/dependency/lockfile/Companion/dataset/Manager/shared writes, git main mutation, GitHub deployment/Pages settings, public host, repo/site/account/environment creation, public tunnel, production rollback, ESPN or source/provider access. The WR-149 worker branch contains DOCS-ONLY execution receipts, not fixture or harness code. Do not invoke a repo workflow or push unreviewed executable fixtures.

## Prerequisite network/browser gate — FAIL CLOSED BEFORE APP LOAD

Use an available isolated ephemeral Linux/CI/sandbox runner or equivalent local runtime if already accessible. Prior to loading any War Room static asset in Chromium, demonstrate actual LOOPBACK-ONLY OUTBOUND RESTRICTION for BOTH page and service-worker/fetch processes at OS/container/network boundary, with specific commands/configuration and a controlled negative probe that proves non-loopback TCP/HTTP cannot reach internet or private LAN. Browser route interception alone is insufficient; service-worker traffic must be covered. DNS-only denial or simply "tool internet disabled" is not sufficient unless supported by actual equivalent enforced routing/egress proof and negative probes. Avoid invoking network resources in the negative probe other than an approved inert/documented probe destination; do not contact ESPN/FantasyPros/other data providers or local private LAN. Record browser launch flags and exact network namespace/container/host constraint, local origin and bound address. If unavailable, STOP with BLOCKED_NETWORK_ISOLATION, no app load or experiment.

Use an already present Chromium executable or provision via existing licensed/available Playwright mechanism into the disposable sandbox with explicit controlled package-download resource authority within this task; no new account, cloud site, public endpoint, paid resource, system-wide package-management side effects or credentials. Perform any necessary browser provisioning BEFORE the egress-closed runtime phase, with a recorded executable version/path and budget. If provisioning or a constrained runner is unavailable, STOP with BLOCKED_BROWSER_OR_RUNNER. No unsafe browser/profile reuse. A user must not be asked to weaken host firewall or run privileged system commands merely to pass this task.

If fixture allowlist, verified current Git tree, stable port/address isolation, atomic non-mixed document-root swap, worker lifecycle observation or safe cleanup cannot be established, report BLOCKED_OR_ABORTED with evidence, rather than treating visual badge alone as success.

## Execution acceptance contract

Implement the WR-148 architecture as ephemeral scratch harness/fixture only. Do not commit runnable harness. For each checkpoint record wall-clock timestamps, process/runner/browser versions and scrubbed commands, frozen commit/tree, per-path Git blob + SHA-256 and sorted path+hash manifest, HTML/worker hashes and worker cache-name identity, origin/port/binding, full A/B patch and changed-path diff, atomic document-root switch log, ordinary and diagnostic HTTP request/response headers and body hashes, both fresh and existing tab loaded-resource + service-worker controller/cache hashes/state, online/offline observations, isolated no-provider manual app/saved-draft smoke, unexpected outbound attempts, fatal errors and actual outcomes.

1. BEFORE/A: freeze accepted A source/manifest, verified complete runtime allowlist (including referenced images/worker CORE_ASSETS), HTTP/browser fresh+existing A, worker controlled/online+offline, no B badge, no-provider smoke.
2. B CHANGED: benign fixture-only marked HTML plus distinct worker cache name with only TWO changed runtime paths, atomic same-origin A→B, prove served manifest B (not mixture), ordinary and cache-busted fetch and fresh+existing tabs, controller/cache transition and offline fallback. Report stale clients honestly, do not silently force reload/update then claim natural convergence.
3. RESTORED/A: atomically repoint to ORIGINAL immutable A, re-prove entire frozen A manifest and no badge, fresh/existing browser controllers/cache online+offline and repeat app smoke. If existing browser remains stale, preserve FAIL/UNVERIFIED outcome.
4. POST-RESTORE STABILITY: repeated regular and diagnostic fetch/browser/cache/worker checks for documented bounded period and convergence, zero external network/console errors, explicit teardown and retained sanitized evidence. Do not equate fixed sleep or local headers with GitHub CDN max-age=600 or public visitor recovery.

A cleanup-failure or security boundary violation is never PASS. If a mandatory step is impossible, record the earliest unverified checkpoint and STOP. On failure, restore local fixture pointer to A ONLY if safe, close local browser/server, remove disposable profiles/directories and log any unresolved cleanup. NEVER touch production to recover local failure.

## Publication and adjudication

Overall result exactly ONE:
- LOCAL_RECOVERY_EVIDENCE_PASS_PENDING_INDEPENDENT_AUDIT
- LOCAL_RECOVERY_EVIDENCE_FAIL
- BLOCKED_NETWORK_ISOLATION
- BLOCKED_BROWSER_OR_RUNNER
- BLOCKED_OR_ABORTED

One next Manager recommendation exactly ONE:
- ACTIVATE_FRESH_INDEPENDENT_WR149_EVIDENCE_AUDIT
- REMEDIATE_SPECIFIC_LOCAL_TEST_DEFICIENCY
- DO_NOT_COUNT_LOCAL_RECOVERY_EVIDENCE

A PASS must have real A→B→A execution, complete required receipts, no unsupported production claim and verified cleanup. A failure or blocker still publishes honest evidence with exact incomplete gates, never a fake PASS. Fresh independent Auditor after frozen target separately recomputes actual receipts, checks potential false positives, network isolation and service-worker/old-tab convergence; Manager later adjudicates if evidence is admissible for conditional release planning, WITHOUT claiming production rollback, GitHub Pages deployment/CDN or actual visitor recovery is proven.

Publish one DRAFT Work Helper PR with exactly the two allowed markdown paths, provide immutable final head, actual exact-head War Room CI/Governance SUCCESS and product correctly SKIPPED, plus result and one next recommendation. Stop writes after frozen final head. Do not merge own PR or change Manager-owned state.
