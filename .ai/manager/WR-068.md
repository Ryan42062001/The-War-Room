# WR-068 — Independent Audit of Deterministic CSV Schema-Inference Contract Clarification

TASK ID: WR-068
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-15
DEPENDENCY: HARD — WR-067 immutable contract target not yet frozen
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-068-csv-schema-inference-contract-audit`
PRODUCTION AUTHORIZATION: NONE

## Objective

Freshly and independently determine whether WR-067 provides a sufficiently explicit, deterministic, source-independent, and reproducible versioned clarification of the Returning-Player v2 raw CSV `(column,type,nullable)` schema contract.

This is a contract audit only. Do not inspect retained provider bytes, implement the parser bridge, resume WR-059, or authorize model/scoring work.

## Activation condition

Remain BLOCKED until Manager freezes:

- exact WR-067 PR;
- exact WR-067 head;
- exact contract clarification/version ID;
- exact machine-lock SHA-256;
- exact synthetic conformance corpus identity/hash.

Manager will populate those values before activation.

## Required independent audit scope

Verify at minimum:

1. WR-067 is explicitly versioned and does not silently rewrite or reinterpret accepted WR-039 v1.0.0;
2. the semantic delta is limited to executable deterministic raw CSV schema inference needed by the evidence contract;
3. CSV decoding/grammar, headers, malformed input, row width, quoting/escaping, whitespace, and line-ending behavior are unambiguous;
4. null-token and empty-field semantics are exact, including quoted versus unquoted empties where relevant;
5. the canonical raw type vocabulary is closed and unambiguous;
6. integer/decimal/boolean/date/datetime/string lexical rules are exact, including exponent, overflow, leading-zero, identifier, signed-zero, and non-finite behavior where applicable;
7. mixed-type promotion/coercion is deterministic and source-independent;
8. nullable semantics are explicitly defined and reproducible over a full file;
9. physical row-count and complete-file scan semantics are exact and sampling is forbidden;
10. ordered schema preservation and all fatal/fail-closed conditions are specified;
11. canonical typed-schema serialization and SHA-256 procedure are exact, version-bound, and independently reproducible;
12. any parser/library/runtime dependency is either fully pinned with all behavior-affecting options or non-normative relative to explicit lexical semantics;
13. synthetic conformance vectors cover every material edge case and expected outputs/hashes reproduce independently;
14. no rules are tuned to the retained 15 source files or derived from retained raw-byte inspection;
15. no source classes, rights, custody semantics, draft-picks disposition, target/outcome rules, or model semantics are changed;
16. no 2026 regular-season outcomes, target joins, model fitting/scoring/tuning/comparison/evaluation, ranking/production change, or Phase-6 work occurs;
17. changed paths are limited to `.ai/research/**`;
18. WR-021/WR-023 frozen artifacts remain untouched.

If two independent implementations could reasonably produce different `(column,type,nullable)` outputs from the same synthetic CSV under the written contract, treat that as a blocking ambiguity.

## Independent reproduction

Do not rely only on WR-067's claimed outputs. Independently implement/recompute enough of the frozen semantics against the synthetic corpus to verify every expected typed schema and digest.

Retained provider bytes are out of scope and must not be accessed.

## Write scope

Write only `.ai/auditor/**`.

Do not modify WR-067 research evidence, Manager/shared state, Work Helper files, workflows/scripts, production/model/ranking files, custody surfaces, or historical checkpoints.

## Publication contract

After Manager activation, publish:

- fresh WR-068 audit report;
- updated `.ai/auditor/HANDOFF.md`;
- immutable audit branch/head;
- Auditor-only PR.

Return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

PASS-family authorizes only Manager acceptance of the exact audited WR-067 contract clarification and creation of a fresh successor safe-consumer parser implementation task. It does not itself authorize retained-byte parsing, WR-059 resumption, model protocol, scoring, or production work.
