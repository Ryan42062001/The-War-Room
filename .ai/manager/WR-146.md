# WR-146 — GitHub Pages Deployment Identity and Rollback Preconditions

TASK ID: WR-146
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
ASSIGNMENT MODE: DIAGNOSIS ONLY — DEPLOYMENT IDENTITY / ROLLBACK PRECONDITIONS
STATUS: CLOSED — WR-D072 independently accepted DEPLOYMENT_IDENTITY_ESTABLISHED and integrated WR-146 evidence; rollback execution remains unauthorized
DATE: 2026-09-23
WORKFLOW: V3.5 CANONICAL
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: wr-146-pages-deployment-identity-evidence
ASSIGNED BRANCH: wr-146-pages-deployment-identity-evidence
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: 82d4add25aab57efe1fa05d1f79e652d7d48a1cd
DEPENDENCY: INDEPENDENT
AUDIT REQUIRED: NO — evidence-only diagnosis; any later deployment/rollback mutation gets separate prospective authority and validation
POST-MERGE CANARY REQUIRED: NO — publication is documentation-only

## Accepted upstream decision

WR-145 is accepted. Its single next recommendation is DEPLOYMENT_IDENTITY_ROLLBACK_EVIDENCE_NEXT.

Manager stages that recommendation deliberately:
1. WR-146 first establishes current GitHub Pages deployment identity / served-version evidence and the exact rollback preconditions.
2. WR-146 performs NO deployment mutation and NO rollback.
3. Only a later separate Manager decision may authorize a controlled rollback/restore rehearsal if the necessary identities and safe targets are established.

This ordering is required before any later exact-current live ESPN fallback mock is attributed to the current release candidate.

## Problem

Current accepted evidence identifies the public application host as:
https://ryan42062001.github.io/The-War-Room/

Repository metadata identifies main as default branch and Pages existence/homepage, but current connected GitHub capability could not directly inspect Pages configuration/deployment-history endpoints. WR-145 therefore classifies Pages settings/deployment history as UNVERIFIED_DUE_TO_CAPABILITY, not absent.

There is no accepted immutable served-SHA/deployment receipt and no deliberate deployment rollback/restore receipt.

The latest accepted product integration is:
45cab0c189c284b4a3011b78ce953b99dd857194

Subsequent canonical commits through WR-145 are documentation/control-plane/evidence only. Do not assume the public host serves any specific SHA merely from that lineage.

## Broad READ-ONLY authority

Read/inspect as needed:
- canonical main, ACTIVE_TASKS, Workflow V3.5, Work Helper charter, WR-145 accepted report/handoff;
- current repository metadata/default branch/homepage/has_pages;
- current repository root, README, index/service worker/manifest/app shell/assets, .nojekyll/CNAME if present, workflows and any Pages/deploy configuration;
- commit/tree/blob history needed to bind candidate served files to immutable repository bytes;
- public War Room Pages site and its same-origin static assets read-only, including response headers/body, cache/version query strings, service worker registration/cache names and source-visible immutable markers if obtainable;
- public GitHub Pages/deployment metadata that is accessible without account mutation;
- current exact product integration 45cab0c... and later docs-only canonical lineage;
- prior known-good product commits only to identify a potential rollback candidate, never to execute rollback.

No login/account/provider action is needed or authorized.

## EXACT authorized writes — TWO files only

1. .ai/work_helper/WR146_PAGES_DEPLOYMENT_IDENTITY_EVIDENCE.md
2. .ai/work_helper/HANDOFF.md

NO OTHER WRITES.

## Required evidence questions

### 1. Deployment mechanism/source
Determine, with confidence classification:
- whether Pages is branch/folder based or Actions-based;
- source branch and folder if observable;
- whether checked-in workflow/config controls deployment;
- whether current connector capability can directly observe Pages settings/deployment history;
- if not observable, state exactly what one user/admin screenshot/read-only setting view would establish.

Never infer a branch/folder setting from default_branch alone.

### 2. Served application identity
Attempt to bind the public served application to immutable repository content without mutating anything.

Use the strongest available evidence, in order:
- explicit immutable commit/build marker served by the site;
- GitHub Pages deployment record binding environment/deployment to commit;
- immutable artifact/build metadata;
- exact content/blob/hash comparison of served app shell and critical static assets against repository versions.

At minimum inspect enough critical served bytes to discriminate the accepted current product tree from materially different historical product trees. Prefer:
- index.html
- primary script/app entry
- war-room-config.js
- js/war-room-command-bar.js
- js/war-room-ui.js
- relevant Companion-facing sync assets if served
- service worker/cache manifest if applicable.

If multiple canonical commits have byte-identical served product files, say so. In that case bind to an immutable PRODUCT TREE / served-byte manifest and identify the set/range of compatible SHAs rather than falsely claiming one unique commit.

Do not use URL reachability or visual similarity as sufficient proof.

### 3. Candidate release identity
State the strongest defensible candidate identity for later live fallback testing:
- exact served SHA, OR
- exact immutable served-byte manifest/tree digest mapped to one or more repository SHAs, OR
- UNRESOLVED with exact missing observation.

Separate:
- canonical repository HEAD;
- latest accepted product integration;
- served deployment identity;
- app/Companion version.

### 4. Rollback preconditions — NO ROLLBACK
Identify what must be known before Manager can authorize a bounded rollback/restore rehearsal:
- current served identity;
- one prior known-good immutable target;
- exact deployment mechanism;
- expected propagation/cache behavior;
- validation method after rollback;
- restore target;
- evidence capture before/during/after;
- stop/abort criteria.

You may identify candidate prior known-good commits from accepted history, but do not choose or enact a rollback target as production authority unless evidence supports it. Never mutate Pages, branches, releases, environments, workflows or repository source.

### 5. User/admin-only observation
If Pages settings/deployment history remain inaccessible through current tools, specify the smallest read-only user action needed. Prefer one concise observation such as:
- Settings -> Pages source branch/folder or Actions source;
- latest Pages deployment environment record with commit SHA/status;
- Pages build/deployment link showing commit.

Do not ask for secrets, tokens, cookies or credentials.

## Classification

Return one overall result:
- DEPLOYMENT_IDENTITY_ESTABLISHED — immutable served identity is sufficiently bound for later exact-current live fallback attribution; rollback rehearsal still separately gated.
- DEPLOYMENT_IDENTITY_PARTIAL — served bytes/tree are substantially bound but one admin observation is still required.
- DEPLOYMENT_IDENTITY_UNRESOLVED — current tools/evidence cannot bind served application sufficiently.

Then return exactly ONE next Manager recommendation:
- ROLLBACK_RESTORE_REHEARSAL_READY_FOR_SEPARATE_AUTHORIZATION
- USER_ADMIN_READ_ONLY_PAGES_OBSERVATION_NEXT
- DEPLOYMENT_IDENTITY_REMEDIATION_NEXT

Do not recommend live ESPN fallback yet unless deployment identity is established and rollback evidence requirement has been separately disposed by Manager.

## Hard stops

NO:
- Pages settings mutation;
- deploy/redeploy;
- rollback or restore;
- branch reset/revert/force push;
- release/tag/environment mutation;
- workflow/product/test/source change;
- live ESPN/account interaction;
- provider contact;
- formal A6 go/no-go;
- release/draft-ready declaration;
- A4/2027 or Track B work.

## Publication

Publish ONE Work Helper PR from wr-146-pages-deployment-identity-evidence changing exactly the two authorized Work Helper files.

Verify exact two-file cumulative scope, one immutable final head, exact-final-head applicable War Room Governance SUCCESS, and product correctly skipped for docs-only scope.

Stop writes after freeze. Return final SHA, PR, CI run/job IDs, overall classification and exactly one next Manager recommendation. Do not merge your own PR.


## WR-D072 — WR-146 ACCEPTED/CLOSED; WR-147 rollback/restore preflight assigned (2026-09-23)

Manager independently accepted WR-146 PR #411 exact head `afce7bcd50d181908cc0e248e141c294ca9d9813`, exact two Work Helper files, exact-head War Room CI #35898079375 SUCCESS / Governance #107306983909, and independently inspected the actual Pages dynamic build/deploy run #35896736306 at `bcb2e89d0da0e1768b8f8688c95f45c53cc49a85`: build #107302424025 checked out `ref: main`, Jekyll `source: .`, `build_revision: bcb2e89d...`; deploy #107302562693 created the Pages deployment from that artifact and reported success to the production URL. Manager acceptance comment: `5800036567`.

WR-146 evidence integrated canonical as `72c888e426a44155f928c71dfcfe58bdfcddd515`; post-integration canonical-main War Room CI #35898669523 SUCCESS / Governance #107308989302. WR-146 CLOSED with `DEPLOYMENT_IDENTITY_ESTABLISHED`.

Manager accepts WR-146's recommendation only as permission to perform a new **preflight**, not a production rollback. WR-147 must qualify a materially distinct target and exact rollback/restore execution contract. Historical pre-WR-143 candidates carry known WR137-F01 early completion behavior and are not automatically known-good; docs-only candidates are byte-identical and do not prove a material rollback. WR-147 classifies candidates as release-safe, transport-only-with-known-limitations, or rejected; defines one reversible no-force-push mechanism, cache/service-worker verification, smoke checks, restoration identity and abort conditions; then returns whether a real rehearsal should be separately authorized.

No rollback, deploy/redeploy, Pages mutation, live ESPN, provider contact, formal A6, release, A4/2027 or Track B is authorized by WR-D072.
