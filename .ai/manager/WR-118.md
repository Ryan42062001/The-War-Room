# WR-118 — Synthetic ESPN Replay + Reconnect Regression

TASK ID: WR-118
ROLE: Implementation Engineer / Builder
STATUS: CLOSED — ACCEPTED REPAIRED TARGET / FULL CANARY PASS
DATE: 2026-09-19
DEPENDENCY: INDEPENDENT — accepted WR-117 inventory; production/ranking scope remains unchanged
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-118-synthetic-espn-replay-reconnect-regression`
PRE-ACTIVATION MAIN CHECKPOINT: `44ac8b88c4949465fcbcd7124e4b7119124e36e4`
START: verify post-activation canonical-main SHA and exact assigned branch starting there.
AUDIT REQUIRED: YES — separate fresh independent Auditor must verify exact implementation head and behavior before Manager merge.
POST-MERGE CANARY REQUIRED: YES — test-harness/CI script registration demands exact canonical-main full suite after integration.
PROVIDER CONTACT / LIVE ESPN / ACCOUNT ACCESS / NEW DATA OR SOURCE / PRODUCTION BEHAVIOR OR RANKING MODIFICATION: NONE.

## Accepted evidence / narrow problem
WR-117 PR #336 (paper-only) audited existing feature and test inventory. It found many separate synthetic tests: app draft invariants, stale/duplicate/corrected ESPN snapshots, session isolation/rollback, reload/recovery, companion parser/background, terminal state, and phone views. **The missing proof is an integrated app-side mechanical scenario** connecting some of these; this is NOT evidence of a production defect and NOT authorization to build a second simulator application. WR-D001 FantasyPros expert PPR ECR remains VALUE authority and ESPN market/ADP TIMING only; WR-D018 Board/Pick History fallback-first and independent structured Direct `LIVE_DIRECT_UNVERIFIED` persist. WR-D027 prohibits all provider contact; custom-model rights `OPTION_C_UNPROVEN / NO_SOURCE_ADMISSION` remain.

## Work product: ONE test-only app-side scenario
Read canonical `.ai/shared/WORKFLOW.md`, registry, `.ai/roles/BUILDER.md`, this task, `.ai/builder/WR117_DRAFT_DAY_RELIABILITY_SIMULATOR_INVENTORY.md`, Builder handoff, WR-D001/018/027/028, README/package scripts, and the actual `scripts/test-draft-invariants.mjs`, `scripts/test-persistence-recovery.mjs`, `scripts/test-espn-offboard.mjs`, `scripts/test-recovery-failures.mjs`, `js/war-room-espn-sync.js` existing fixture interfaces. **Reuse established test fixtures and injection interfaces, not live ESPN and not production code.** The permitted test surface is a single `scripts/test-wr-118-espn-replay-reconnect.mjs` deterministic synthetic replay/reconnect integration test plus `package.json` to register it with `npm test`, a task-owned Builder evidence report and Builder handoff.

Implement one deterministic app-side/browser fixture (fixed seed/input order/explicit numbered pick IDs) that proves the existing app can accept a valid numbered ESPN-like slice, repeat it and receive reordered/duplicate/stale/partial source-state delivery; apply a correction **only under existing accepted authority rules**, persist two isolated saved draft sessions, undergo controlled app reload and synthetic reconnect/replay, and continue to a terminal full count/then reload. Start with a *bounded early-pick segment* and explicit fixture order, then use existing fixture generators to complete a 10-team x 16-round draft if feasible within the permitted test surface. Only select an app interface actually exposed by existing tests; an untested extension-to-app end-to-end bridge, provider source, live room or arbitrary new runner infrastructure is OUT OF SCOPE. If direct app-only replay cannot substantiate a separate Companion-layer behavior, label the limitation rather than claiming complete end-to-end Companion certification.

Required assertions when supported by existing accepted behavior: no duplicate numbered pick or canonical player; deterministic convergence of equivalent permutations, duplicate replay idempotence; stale lower-progress packet must not cause an erroneous authoritative state regression; partial/unresolved pick does not create a fabricated owner/player or overwrite unrelated slots; permitted correction converges to exactly one correct numbered pick/player/team/ownership; distinct saved session is unmodified; persisted pick and source/ownership metadata survive reload; terminal/provisional labels reflect whether the ledger is complete; no invalid next turn or unavailable-player recommendation caused by stale state; existing FantasyPros ECR and other committed source ranking order remain untouched. Do NOT assert a contested precedence rule (e.g., manual override always wins) or a new recommendation winner/threshold; if an existing rule is ambiguous, isolate it and report for Manager/Draft Strategy instead of inventing behavior.

Failure diagnostics: log deterministic seed/order, session/key and pick number, expected and actual authoritative ledger digest, source confidence/ownership fields where already available, failure stage; never log credentials, cookies, player-source rows or live ESPN URLs. Failure of a *new synthetic invariant* requires a reproducible bounded failing fixture and Manager disposition: do NOT patch `js/`, `extensions/`, `data/` or production files on this task. If the intended integrated scope cannot be proven with permitted files/helpers, return `BLOCKED — NARROW MANAGER SCOPE DECISION` with smallest required extension; do not silently widen.

## Exact allowed writes
1. `scripts/test-wr-118-espn-replay-reconnect.mjs` — one deterministic fixture/test file.
2. `package.json` — only test command registration in `test` and a named `test:wr118-espn-replay-reconnect` script; no dependency/toolchain changes.
3. `.ai/builder/WR118_SYNTHETIC_REPLAY_RECONNECT_EVIDENCE.md` — scenario matrix, exact pre/post snapshots, assertions actually executed, pass/fail and limitations.
4. `.ai/builder/HANDOFF.md` — compact actual evidence and Manager next action.

No changes in `js/`, `extensions/`, `index.html`, CSS, `data/`, dataset/IDs/ECR/ADP, `.ai/shared/`, `.ai/manager/`, `.ai/auditor/`, `scripts/workflow-*`, `.github/`, deployment, ranking weights, recommendation policy or source/provider interactions. No fresh Strategy contract is required for **mechanical-only existing behavior assertions**; any desired QB/WR/RB run, high-ECR faller, scarcity-vs-need outcome or modified policy must receive separate Draft Strategy Manager task and cannot be included here.

## Tests and finish criteria
Run actual new named synthetic test repeatedly with identical deterministic final digests, `node --check` on new script, all relevant existing `test:draft-invariants`, `test:persistence-recovery`, `test:recovery-failures`, `test:espn-offboard`, `test:extension` as applicable, `npm test`, and observe applicable exact-head full War Room CI; report each actual result, skips and environment limitations separately. A fixture PASS is not live ESPN/Direct proof. Inspect exact FOUR-file PR diff and final immutable head; leave Builder PR UNMERGED; no self-review/audit. Manager freezes exact audited target and separately assigns a FRESH Independent Auditor/QA task with distinct branch, report and PR. After auditor PASS-family and Manager merge, exact canonical-main full CI is required before WR-118 closure. If test fails, preserve failure evidence and request separate Manager-authorized bounded remediation; do not change application behavior here.


## Manager exact-target freeze and independent audit routing — 2026-09-20

Manager reviewed live Builder PR #338 `OPEN / UNMERGED`, exact final target `39491e672b6177834aa029b7a716c612c7cc892d` (branch `wr-118-synthetic-espn-replay-reconnect-regression`, historical PR base `5dc8906d5285d1c51b51ef0068bd0a98753610ba`) with exactly the four authorized files. Exact-head PR War Room CI `35488672532` SUCCESS: classify `106019608130`, Governance `106019627767`, full test `106019654656`; bootstrap reuse skipped. Full-test logs confirm `npm test`, required existing extension/off-board/draft-invariant/recovery suites and named syntax-checked WR-118 test with two deterministic identical source/A/B digests and zero page errors. This is **AUDIT_READY**, not independent PASS, real ESPN/Companion end-to-end verification or merge approval.

Freeze the Builder PR/head exactly, prohibit new Builder writes while WR-119 audits, and assign separate **WR-119 — Fresh Independent Audit of WR-118 Synthetic Replay + Reconnect Regression** at `.ai/manager/WR-119.md` on a distinct Auditor branch. Any Builder head movement or proposed remediation invalidates the present target's final acceptance and requires a new Manager freeze / fresh independent audit. Preserve no production/source/provider/ranking changes. After independently published PASS-family audit, Manager separately decides evidence integration and Builder target merge and must observe exact canonical-main FULL CI canary before closing WR-118.


## WR-D030 — Bounded in-place test-only remediation after failed WR-119 audit (2026-09-20)

The exact historical WR-118 Builder target `39491e672b6177834aa029b7a716c612c7cc892d` remains **FAILED** under independent Auditor report WR-119 PR #340 final head `4e1432e306ade195816d685c5b3065e8a4be99c8` (exact audit-head Governance CI `35489592441` SUCCESS). Manager accepts both: **WR-119-F01 MEDIUM/BLOCKING**, integrated fixture permits vacuously empty candidate list and does not independently validate nonterminal user next turn/on-clock; **WR-119-F02 LOW/non-blocking alone**, apply/reconciliation failures log null session and identical empty ledger digests. This decision makes no claim of a demonstrated production defect. The audit PR #340 remains open/unmerged as historical evidence; WR-119 is CLOSED and removed from active-only registry.

Authorize *only* the original WR-118 Builder role to amend the **existing** `wr-118-synthetic-espn-replay-reconnect-regression` branch and PR #338, from the known frozen failed SHA above, using `STANDARD_CHAT_HIGH / FAST_REFRESH`. No replacement task/PR or new code path. The narrow remediation write set is ONLY:
- `scripts/test-wr-118-espn-replay-reconnect.mjs`: modify existing fixture/oracle and its safe diagnostics, not production app code.
- `.ai/builder/WR118_SYNTHETIC_REPLAY_RECONNECT_EVIDENCE.md`: record exact accepted audit findings, assertion gap closure, negative controls actually executed, test outputs and limitations.
- `.ai/builder/HANDOFF.md`: compact changed-head/CI and re-audit handoff.

`package.json` was already correctly registered and **must remain unchanged** in this remediation; this still yields the same four authorized cumulative PR paths relative to main. No other path may change.

### F01 required oracle repairs
- Derive the next lawful user snake pick/on-clock state independently from fixed 10-team/16-round/slot-7 configuration and the **actual accepted numbered pick count**, using only existing accepted semantics (not by computing expected from `state.myNextPick` or from the value being tested). Assert correct `myNextPick` at representative A active initial/stale/partial/correction/reconnect #20, B distinct active session, and representative 159/160 checkpoint when all user's slots are filled. Precisely distinguish the next user pick, currently-on-clock and legitimate null/finished-roster cases; do not assert there is another user pick after user has finished its roster. Do not require a new, disputed manual-versus-ESPN precedence, market/survival winner or QB/WR/RB positional-run policy.
- In selected actively drafting, roster-eligible states, assert a **nonempty actual eligible candidate set** and meaningful available-player/recommendation/decision-path agreement from the existing app interface, rather than only `invalidCandidates === 0` on `[]`. Verify no selected top candidate is already unavailable, and distinguish genuinely complete/legitimately exhausted rosters and provisional terminal states in which the eligible set may lawfully be empty. Preserve original FantasyPros ECR / ESPN timing split and do not invent a preferred player/winner, threshold, new source or score.
- Add bounded **ephemeral negative controls** that demonstrate the active candidate assertion would reject an empty candidate list and that a wrong next-pick/on-clock value would fail the new oracle, with actual test-run evidence. Keep negative controls fixture/test-only; do not mutate production files or persist deliberately corrupted user/player data.

### F02 required diagnostic repairs
- On failed app-result/reconciliation-counter path, read the actual in-page app snapshot if possible and obtain the explicit expected fixture ledger for that stage. Populate real, separately computed expected and actual authoritative ledger hashes, safe synthetic session identifier and stage/pick/input-order metadata, and first meaningful mismatch/source/ownership fields available. A diagnostic helper must not replace the real observed state with `null` or both ledgers with `[]` when an inspection is available; if inspection genuinely fails, disclose this in an explicit fallback error instead of claiming two equal empty ledger hashes.
- Preserve no credentials, cookies, account identifiers, URLs, full source player rows or protected provider data in logs. Demonstrate an applicable ephemeral counter-failure negative control if feasible.

### Freeze/re-audit gates
Run syntax-check and new named test at least twice deterministically, plus relevant existing draft invariants, persistence/recovery, recovery-failures, off-board and extension tests and full `npm test`. Require new exact-final-head **FULL** War Room CI success and a truthful output/evidence record. Leave existing Builder PR #338 open/unmerged; do not merge Auditor PR #340. A legitimate new application failure is **STOP** and Manager decides separate remediation; absolutely no `js/`, `extensions/`, data, `.github/`, workflow/runner, ranking policy or product patch is authorized.

Report repaired Builder head, cumulative exact four-file diff and new full CI to Manager. Manager will freeze a **NEW** immutable repaired SHA and assign a **fresh independent re-audit WR-120** on its own task/branch/PR; neither the failed WR-119 verdict nor its historical Auditor branch can serve as a repaired-head audit. No Auditor activation yet, and no deployment, provider contact or downstream work is authorized by this task.


## WR-D031 Manager freeze — WR-D030 repaired exact-head audit readiness (2026-09-20)

Repaired **existing Builder PR #338** remains OPEN/UNMERGED at **NEW immutable head `c80aaa8807ed9ef94619b1117988e64d9b773234`**, branch `wr-118-synthetic-espn-replay-reconnect-regression`; historical creation/PR base `5dc8906d5285d1c51b51ef0068bd0a98753610ba`; historical failed WR-119 audit target `39491e672b6177834aa029b7a716c612c7cc892d` remains a distinct failed parent. Manager independently verified the cumulative PR diff still contains exactly the four originally authorized files. The failed parent → repaired SHA comparison shows **only** `scripts/test-wr-118-espn-replay-reconnect.mjs`, `.ai/builder/WR118_SYNTHETIC_REPLAY_RECONNECT_EVIDENCE.md`, and `.ai/builder/HANDOFF.md`; `package.json` registration is unchanged during remediation. No production JS/Companion, source/data, strategy, ranking, workflow/runner or deployment changes.

Repaired exact-final-head FULL War Room CI **`35491052010` SUCCESS**: classify `106025927564`, Governance `106025949438`, full test `106025970045`, bootstrap-reuse skipped. Actual full-job log shows `npm test` (including extension, off-board, draft-invariant, persistence/recovery/recovery-failures) plus syntax-checked WR-118 synthetic test. Two independent fixed-seed browser iterations agree on final A/B/source digests and report zero page errors; all eight ephemeral F01/F02 negative controls executed and rejected, including a counter-failure log with a true synthetic session and distinct actual/expected 12-vs-11 ledger hashes and pick #12 mismatch. This is self-validation and Manager **AUDIT_READY**, NOT independently audited closure or a live ESPN/Companion/reconnect claim.

Builder may not advance or merge PR #338 while **WR-120 Fresh Independent Re-Audit of Repaired WR-118** is active. New audit task `.ai/manager/WR-120.md`, distinct `wr-120-repaired-synthetic-replay-reconnect-reaudit` branch from verified post-activation main, audits ONLY repaired `c80aaa8807ed9ef94619b1117988e64d9b773234` and independently challenges F01/F02, the negative controls, original replay/ownership/session/terminal proof and evidence limits. Historical WR-119 FAIL remains bound to `39491e672b6177834aa029b7a716c612c7cc892d`; its PR #340 stays OPEN/UNMERGED as historical negative evidence. Any Builder head movement or fresh defect requires Manager refreeze/new independent review, not automatic following. No Manager merge or production changes are authorized at this freeze. After a separately published PASS-family audit and Manager disposition, any later Builder integration requires exact canonical-main FULL CI canary before closure.


## WR-D032 FINAL CLOSURE (2026-09-20)

WR-120 independent PASS on exact repaired Builder SHA `c80aaa8807ed9ef94619b1117988e64d9b773234`, Auditor PR #343 integrated to main `34d852e5fea021548c76d0ff803113097eabeff4`; exact Builder PR #338 merged to `396462a0649a6bb6f1e0b212f7bfda700759ba6c`. Mandatory canonical-main FULL CI `35511010222` SUCCESS including test job `106078842642`, named WR-118 synthetic fixture and eight negative controls. WR-118 CLOSED / active registry removal authorized by WR-D032. Historical WR-119 FAIL old SHA and unmerged audit PR #340 preserved. Test-only synthetic app-side scope; no live ESPN/Companion E2E, production/ranking, provider or deployment change.
