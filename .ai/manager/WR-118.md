# WR-118 — Synthetic ESPN Replay + Reconnect Regression

TASK ID: WR-118
ROLE: Implementation Engineer / Builder
STATUS: AUDIT_READY
DATE: 2026-09-19
DEPENDENCY: INDEPENDENT — accepted WR-117 inventory; production/ranking scope remains unchanged
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-118-synthetic-espn-replay-reconnect-regression`
PRE-ACTIVATION MAIN CHECKPOINT: `44ac8b88c4949465fcbcd7124e4b7119124e36e4`
START: verify post-activation canonical-main SHA and exact assigned branch starting there.
AUDIT REQUIRED: YES — separate fresh independent Auditor must verify exact implementation head and behavior before Manager merge.
POST-MERGE CANARY REQUIRED: YES — test-harness/CI script registration demands exact canonical-main full suite after integration.
PROVIDER CONTACT / LIVE ESPN / ACCOUNT ACCESS / NEW DATA OR SOURCE / PRODUCTION BEHAVIOR OR RANKING MODIFICATION: NONE.

## Accepted evidence / narrow problem
WR-117 PR #336 (paper-only) audited existing feature and test inventory. It found many separate synthetic tests: app draft invariants, stale/duplicate/corrected ESPN snapshots, session isolation/rollback, reload/recovery, companion parser/background, terminal state, and phone views. **The missing proof is an integrated app-side mechanical scenario** connecting some of these; this is NOT evidence of a production defect and NOT authorization to build a second simulator application. WR-D001 FantasyPros expert PPR ECR remains VALUE authority and ESPN market/ADP TIMING only; WR-D018 Board/Pick History fallback-first and independent structured Direct `LIVE_DIRECT_UNVERIFIED` persist. WR-D027 prohibits all provider contact; custom-model rights `OPTION_C_UNPROVEN / NO_SOURCE_ADMISSION` remain.

## Work product: ONE test-only app-side scenario
Read canonical `.ai/shared/WORKFLOW.md`, registry, `.ai/roles/BUILDER.md`, this task, `.ai/builder/WR117_DRAFT_DAY_RELIABILITY_SIMULATOR_INVENTORY.md`, Builder handoff, WR-D001/018/027/028, README/package scripts, and the actual `scripts/test-draft-invariants.mjs`, `scripts/test-persistence-recovery.mjs`, `scripts/test-espn-offboard.mjs`, `scripts/test-recovery-failures.mjs`, `js/war-room-espn-sync.js` existing fixture interfaces. **Reuse established test fixtures and injection interfaces, not live ESPN and not production code.** The permitted test surface is a single `scripts/test-wr-118-espn-replay-reconnect.mjs` deterministic synthetic replay/reconnect integration test plus `package.json` to register it with `npm test`, a task-owned Builder evidence report and Builder handoff.

Implement one deterministic app-side/browser fixture (fixed seed/input order/explicit numbered pick IDs) that proves the existing app can accept a valid numbered ESPN-like slice, repeat it and receive reordered/duplicate/stale/partial source-state delivery; apply a correction **only under existing accepted authority rules**, persist two isolated saved draft sessions, undergo controlled app reload and synthetic reconnect/replay, and continue to a terminal full count/then reload. Start with a *bounded early-pick segment* and explicit fixture order, then use existing fixture generators to complete a 10-team x 16-round draft if feasible within the permitted test surface. Only select an app interface actually exposed by existing tests; an untested extension-to-app end-to-end bridge, provider source, live room or arbitrary new runner infrastructure is OUT OF SCOPE. If direct app-only replay cannot substantiate a separate Companion-layer behavior, label the limitation rather than claiming complete end-to-end Companion certification.

Required assertions when supported by existing accepted behavior: no duplicate numbered pick or canonical player; deterministic convergence of equivalent permutations, duplicate replay idempotence; stale lower-progress packet must not cause an erroneous authoritative state regression; partial/unresolved pick does not create a fabricated owner/player or overwrite unrelated slots; permitted correction converges to exactly one correct numbered pick/player/team/ownership; distinct saved session is unmodified; persisted pick and source/ownership metadata survive reload; terminal/provisional labels reflect whether the ledger is complete; no invalid next turn or unavailable-player recommendation caused by stale state; existing FantasyPros ECR and other committed source ranking order remain untouched. Do NOT assert a contested precedence rule (e.g., manual override always wins) or a new recommendation winner/threshold; if an existing rule is ambiguous, isolate it and report for Manager/Draft Strategy instead of inventing behavior.

Failure diagnostics: log deterministic seed/order, session/key and pick number, expected and actual authoritative ledger digest, source confidence/ownership fields where already available, failure stage; never log credentials, cookies, player-source rows or live ESPN URLs. Failure of a *new synthetic invariant* requires a reproducible bounded failing fixture and Manager disposition: do NOT patch `js/`, `extensions/`, `data/` or production files on this task. If the intended integrated scope cannot be proven with permitted files/helpers, return `BLOCKED — NARROW MANAGER SCOPE DECISION` with smallest required extension; do not silently widen.

## Exact allowed writes
1. `scripts/test-wr-118-espn-replay-reconnect.mjs` — one deterministic fixture/test file.
2. `package.json` — only test command registration in `test` and a named `test:wr118-espn-replay-reconnect` script; no dependency/toolchain changes.
3. `.ai/builder/WR118_SYNTHETIC_REPLAY_RECONNECT_EVIDENCE.md` — scenario matrix, exact pre/post snapshots, assertions actually executed, pass/fail and limitations.
4. `.ai/builder/HANDOFF.md` — compact actual evidence and Manager next action.

No changes in `js/`, `extensions/`, `index.html`, CSS, `data/`, dataset/IDs/ECR/ADP, `.ai/shared/`, `.ai/manager/`, `.ai/auditor/`, `scripts/workflow-*`, `.github/`, deployment, ranking weights, recommendation policy or source/provider interactions. No fresh Strategy contract is required for **mechanical-only existing behavior assertions**; any desired QB/WR/RB run, high-ECR faller, scarcity-vs-need outcome or modified policy must receive separate Draft Strategy Manager task and cannot be included here.

## Tests and finish criteria
Run actual new named synthetic test repeatedly with identical deterministic final digests, `node --check` on new script, all relevant existing `test:draft-invariants`, `test:persistence-recovery`, `test:recovery-failures`, `test:espn-offboard`, `test:extension` as applicable, `npm test`, and observe applicable exact-head full War Room CI; report each actual result, skips and environment limitations separately. A fixture PASS is not live ESPN/Direct proof. Inspect exact FOUR-file PR diff and final immutable head; leave Builder PR UNMERGED; no self-review/audit. Manager freezes exact audited target and separately assigns a FRESH Independent Auditor/QA task with distinct branch, report and PR. After auditor PASS-family and Manager merge, exact canonical-main full CI is required before WR-118 closure. If test fails, preserve failure evidence and request separate Manager-authorized bounded remediation; do not change application behavior here.


## Manager exact-target freeze and independent audit routing — 2026-09-20

Manager reviewed live Builder PR #338 `OPEN / UNMERGED`, exact final target `39491e672b6177834aa029b7a716c612c7cc892d` (branch `wr-118-synthetic-espn-replay-reconnect-regression`, historical PR base `5dc8906d5285d1c51b51ef0068bd0a98753610ba`) with exactly the four authorized files. Exact-head PR War Room CI `35488672532` SUCCESS: classify `106019608130`, Governance `106019627767`, full test `106019654656`; bootstrap reuse skipped. Full-test logs confirm `npm test`, required existing extension/off-board/draft-invariant/recovery suites and named syntax-checked WR-118 test with two deterministic identical source/A/B digests and zero page errors. This is **AUDIT_READY**, not independent PASS, real ESPN/Companion end-to-end verification or merge approval.

Freeze the Builder PR/head exactly, prohibit new Builder writes while WR-119 audits, and assign separate **WR-119 — Fresh Independent Audit of WR-118 Synthetic Replay + Reconnect Regression** at `.ai/manager/WR-119.md` on a distinct Auditor branch. Any Builder head movement or proposed remediation invalidates the present target's final acceptance and requires a new Manager freeze / fresh independent audit. Preserve no production/source/provider/ranking changes. After independently published PASS-family audit, Manager separately decides evidence integration and Builder target merge and must observe exact canonical-main FULL CI canary before closing WR-118.
