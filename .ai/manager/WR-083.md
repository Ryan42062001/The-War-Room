# WR-083 — Protected Historical Scoring Execution Bridge

TASK ID: WR-083
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: CLOSED — ACCEPTED
DATE: 2026-09-17
DEPENDENCY: HARD — WR-081 exact fail-closed blocker evidence
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
ASSIGNMENT MODE: CUSTODY / WORKFLOW / MODEL-EXECUTION BRIDGE — DIAGNOSIS + REMEDIATION
TARGET BRANCH: `wr-083-protected-historical-scoring-bridge`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-089
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES
POST-MERGE CANARY REQUIRED: YES

## Objective

Build the smallest separately reviewed protected execution bridge that can later execute WR-081 against the exact accepted retained historical rows without weakening WR-042/059/063/065/069 custody, credential-isolation, source-identity, or outcome-chronology guarantees.

This task implements and proves the bridge. It does **not** authorize real Returning-Player v2 scoring, target joins, model fitting, prediction inspection, or gate evaluation. Actual WR-081 scoring remains blocked until WR-084 independently passes the exact bridge target, Manager integrates only that audited target, and the required canonical-main protected canary passes.

## Frozen authority

Preserve exactly:
- WR-072 protocol `returning-player-v2-model-protocol/1.2.0-wr072`;
- WR-072 machine lock `aed044e6b7df9684153181a7a97a47a86c50ce9049db063ecdd8a973b1a832b6`;
- source snapshot `wr-returning-player-v2-source-snapshot/1.2.0-wr059` / `6af88adaea478351a2b9c4884ca248dfed9527cb97f05e3648bd82c5fe0b3cea`;
- cohort `returning-player-v2-cohort/1.2.0-wr059` / `f62075ec3c13784dea4568fa69aae8a84d39ca70f074ca769132f1f143f2c3d4`;
- exactly 14 admitted annual Player Summary Stats identities;
- zero Players-metadata / draft-capital predictors;
- `draft_picks.csv` exclusion;
- no upstream reacquisition, refresh, replacement, or substitution.

## Required bridge properties

The implementation must fail closed unless all are true:

1. retrieve only the exact 14 admitted retained annual stats objects by immutable accepted identity;
2. preserve the accepted read-only provider boundary and exact-key/version semantics;
3. independently verify B2 and R2 bytes agree in SHA-256 and size before any consumer reads them;
4. confine raw bytes and manifests to `RUNNER_TEMP`;
5. provider credentials exist only in the provider/retrieval step;
6. the consumer starts with provider credential variables absent, preferably under an explicit clean environment;
7. raw retained bytes are never committed, uploaded as Actions artifacts, printed, or left after success/failure;
8. reviewed consumer code is hash-bound to the exact frozen implementation;
9. synthetic/unit/conformance tests prove prediction-lock-before-target-exposure chronology, fail-closed target access, stage gating, serialization, and cleanup without exposing real historical scoring results before audit;
10. workflow/ref gates prevent arbitrary fork/PR code, `pull_request_target` abuse, branch substitution, and target-branch race;
11. after future Manager authorization only, the bridge may commit complete authorized `.ai/research/**` WR-081 evidence to an explicitly supplied WR-081 execution branch iff its expected head SHA still matches;
12. the bridge may not write model results to `main` directly;
13. release validation recognizes only the exact Manager-approved protected workflow while remaining fail closed.

## Pre-audit live proof boundary

A protected readiness proof before WR-084 may retrieve and rehash the exact 14 retained stats objects and exercise a **NO-SCORING / CONFORMANCE** consumer mode only. It must not expose historical targets, construct real model features, fit Ridge, emit predictions, compare baselines, calculate result gates, or inspect development/validation/confirmation outcomes.

The readiness proof must publish only privacy-safe proof: exact implementation SHA, run/job IDs, accepted identity/hash/size verification, credential-isolation result, cleanup result, no-artifact result, branch/ref gate result, and synthetic/conformance digests.

## Authorized write scope

Work Helper may modify only:
- `.ai/work_helper/WR083_*`;
- `.ai/work_helper/HANDOFF.md`;
- exact `.github/workflows/wr083-protected-historical-scoring-bridge.yml`;
- exact `scripts/custody/wr083_protected_historical_scoring.py`;
- exact `scripts/custody/test_wr083_protected_historical_scoring.py`;
- exact `scripts/validate-release-candidate.mjs` only as necessary to recognize the approved WR-083 workflow without weakening the release guard.

Do not modify `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, `.ai/research/**`, `src/**`, `public/**`, unrelated workflows/scripts, WR-069 accepted artifacts, or production ranking/model code.

## Required publication

Publish one immutable WR-083 implementation target and PR with:
- exact branch/head/PR;
- exact changed files;
- implementation and test hashes;
- static/unit/conformance results;
- protected no-scoring proof run/job IDs;
- exact provider-boundary evidence and actual read-only capability evidence;
- proof that only 14 admitted stats objects were handled;
- credential absence in consumer;
- raw-byte cleanup on success and deliberate failure;
- zero raw Actions artifacts;
- branch/ref/race fail-closed tests;
- release-validator result;
- exact-head War Room CI;
- explicit attestation that no real WR-081 model scoring/result inspection occurred.

Do not merge and do not activate WR-084. Return to Manager for exact freeze.

## V3.4 routing rationale

`WORK_MODE` remains justified because WR-083 is execution-heavy: protected workflow implementation, custody consumer code, provider-boundary proof, repeated fail-closed testing, runtime/CI validation, and likely edit-test-diagnose-fix loops. Work remains an accelerator, not a dependency. If Work capacity is unavailable, continue this exact task/branch in `STANDARD_CHAT_HIGH` without restarting or re-solving accepted custody decisions.

## Final V3.4 audit target — 2026-09-17

Manager reconciled canonical Workflow V3.4 main `8855e00e19d37c0cffca9d2c392262f34febe9cd` into the completed bridge without changing any of the seven WR-083 file blobs. Final immutable target: `4ac5fa2c6148960094fde81b217bd3af080e4213`, PR #234, clean/mergeable, current canonical main as exact merge base, behind 0.

Exact-head Full War Room CI `35305591247` completed `SUCCESS`: classify `105477013314`, governance `105477036724`, full test `105477068265`.

Reconciled-head protected workflow `35305591290` completed `SUCCESS` for implementation/frozen-authority/synthetic-chronology preflight. Credentialed proof jobs remained gated by design; the successful live no-scoring proof `35300775802` remains byte-bound because the reviewed bridge script/test/workflow blobs are unchanged. Additional exact-head custody regressions WR-046 `35305591251`, WR-063 `35305591242`, and WR-069 `35305591273` all completed `SUCCESS`.

No real WR-081 scoring or target exposure occurred. WR-083 is AUDIT_READY for WR-084 only.


## Bounded remediation assignment after WR-084 — 2026-09-18

WR-084 independently audited exact target `4ac5fa2c6148960094fde81b217bd3af080e4213` and returned `FAIL — REMEDIATION REQUIRED` with exactly two blocking HIGH findings:

- `WR-084-AUD-01` — future WR-081 expected head / consumer identity is not Manager-bound before retained target exposure.
- `WR-084-AUD-02` — the future publication guard can commit retained raw bytes under an allowed `.ai/research/**` path.

Audit evidence:
- PR #243 — merged as immutable evidence;
- Auditor head `69b491dfff87c08413ae335448c2b9ec2a2515f0`;
- audit-head War Room CI `35307131616` — SUCCESS;
- evidence merge `9f6eba965b11e3ee8c30be71cd9b7aac387a79e2`;
- post-merge War Room CI `35307517063` — SUCCESS.

### Remediation scope — WR-084-AUD-01

Before any retained row or historical target becomes visible to a future scoring consumer, fail closed unless canonical Manager-controlled authority binds all of:

1. exact authorized WR-081 execution branch;
2. exact current authorized branch-head SHA;
3. exact authorized consumer path;
4. exact Manager-reviewed consumer SHA-256.

Required behavior:
- verify live remote authorized branch head equals the Manager-authorized SHA before provider data is supplied to the scoring consumer;
- verify the checked-out commit is exactly that authorized branch head before any retained row/target becomes consumer-visible;
- verify consumer path/digest against Manager-controlled canonical authority, not caller-supplied values alone;
- retain the second remote-head race check immediately before non-force push;
- fail closed for unrelated same-repository SHA, stale branch SHA, unreviewed consumer path/digest, and branch advancement between authorization and execution.

### Remediation scope — WR-084-AUD-02

Before staging or committing future WR-081 publication, mechanically exclude retained raw-source passthrough.

At minimum:
1. bind publication validation to the verified retained-input manifest;
2. reject any publication entry whose digest/size or exact bytes equal any retained raw source object;
3. preserve a narrow explicit contract/allowlist for authorized WR-081 evidence paths/types sufficient to prevent source-file passthrough;
4. add negative tests proving that copying an exact retained input under an allowed `.ai/research/**` path is rejected;
5. preserve existing no-artifact, no-log and cleanup protections.

### Preserve without reopening

Do not re-litigate or redesign independently PASSed behavior unless a remediation dependency makes it strictly necessary:
- exact 14-source custody identities;
- WR-059 source/cohort authority;
- WR-072 protocol/machine lock;
- B2/R2 digest/size equality;
- consumer provider-credential isolation;
- runner-temporary raw storage and cleanup;
- NO-SCORING pre-audit boundary;
- prediction-lock-before-target-exposure chronology;
- source/predictor exclusions;
- release guard exact allowlist;
- no production/ranking/composition/Phase-6 changes.

### Authorized files

Remain within the existing WR-083 authorized write scope. The expected remediation files are primarily:
- `.github/workflows/wr083-protected-historical-scoring-bridge.yml`;
- `scripts/custody/wr083_protected_historical_scoring.py`;
- `scripts/custody/test_wr083_protected_historical_scoring.py`;
- `.ai/work_helper/WR083_*`;
- `.ai/work_helper/HANDOFF.md`.

Touch `scripts/validate-release-candidate.mjs` only if the remediation genuinely requires it; do not broaden the release allowlist.

### Validation / completion

Use the existing branch `wr-083-protected-historical-scoring-bridge` and PR #234. Preserve failed audited target `4ac5fa2c6148960094fde81b217bd3af080e4213` as immutable history.

Before returning to Manager:
- add direct negative tests for every WR-084-AUD-01 and WR-084-AUD-02 failure mode above;
- run local/static/unit/conformance validation;
- run exact-head Full War Room CI;
- run protected bridge/custody regressions;
- preserve `real_scoring=false` and `historical_targets_exposed=false`;
- perform no real WR-081 scoring;
- publish one new immutable WR-083 target and update PR #234 with exact head, changed files, hashes, tests/run IDs and explicit finding-closure mapping.

Do not merge PR #234 and do not activate WR-089. Return to Manager for exact freeze.

### V3.4 routing

This remediation starts in `STANDARD_CHAT_HIGH` using bounded/FAST refresh because the audit reduced the problem to two precise fail-closed defects. If hands-on execution expands into substantial repeated environment/CI debugging, return a `WORK_MODE_ESCALATION_RECOMMENDED` packet instead of restarting the task.


## Final bounded-remediation audit target — 2026-09-18

Manager verified completed WR-084 bounded remediation on PR #234 and froze exact head:

`c9b13959f598b3633a78e2ff78d0862881982dd2`

Canonical V3.4 remediation base / exact merge base:
`ca7fda518386fc23f44344e78fc3b4169602c254`

Compare is ahead 35 / behind 0 with exactly the seven authorized WR-083 paths.

### Finding closure evidence

`WR-084-AUD-01`:
- Manager-owned WR-081 `future_execution_authority` binds exact branch, head SHA, consumer path and reviewed consumer SHA-256;
- live authorized branch head is verified before retained retrieval;
- checked-out HEAD and Manager-reviewed consumer identity are verified before retained retrieval;
- live branch head is checked again immediately before consumer exposure;
- existing pre-push race checks remain;
- negative tests cover unrelated SHA, stale branch SHA, unreviewed path/digest, missing/blocked authority and branch advancement.

`WR-084-AUD-02`:
- publication validation is bound to the verified retained-input manifest;
- output digest+size or exact-byte equality with retained raw source objects is rejected;
- evidence publication paths/types are narrowly constrained to WR081 JSON/Markdown evidence;
- raw passthrough is rejected both during publication validation and final staging;
- previously frozen evidence-path mutation protection remains.

### Exact-head validation

Full PR-event War Room CI:
- run `35309111018`: SUCCESS;
- classify `105487272881`: SUCCESS;
- governance `105487309809`: SUCCESS;
- full test `105487359262`: SUCCESS.

Final-head protected/custody regressions:
- WR-083 `35309111079`: SUCCESS;
- WR-046 `35309111050`: SUCCESS;
- WR-063 `35309111093`: SUCCESS;
- WR-069 `35309111021`: SUCCESS.

Credentialed remediated NO-SCORING proof:
- implementation/proof SHA `648ae9372bf2eb49e0fcebcf921d7bafd7d26d1b`;
- protected run `35308823649`: SUCCESS;
- trust gate `105486504930`: SUCCESS;
- protected no-scoring proof `105486527602`: SUCCESS;
- future scoring job `105486528622`: SKIPPED;
- Actions artifacts: 0.

The final target preserves the reviewed remediated script/test/workflow blobs byte-for-byte from proof SHA `648ae9372bf2eb49e0fcebcf921d7bafd7d26d1b`. Only Work Helper evidence files changed after the proof.

Boundary remains:
- `real_scoring=false`;
- `historical_targets_exposed=false`.

WR-083 is AUDIT_READY for WR-089 only. Do not merge PR #234 and do not reactivate WR-081 before PASS-family re-audit, exact audited integration and protected canonical-main canary.


## WR-089 PASS / integration disposition — 2026-09-18

WR-089 independently returned `PASS` with no findings on exact target `c9b13959f598b3633a78e2ff78d0862881982dd2`.

Audit evidence:
- PR #247;
- exact Auditor head `a4cc4983982e91d820772d18b1019708a78f5b78`;
- audit-head War Room CI `35348224226` — SUCCESS;
- audit evidence merge `3ce2ad4e135d66a0b705dda0d726abd36a569d77`.

Manager integrated only the exact audited target through PR #234 as canonical-main merge:
`ee0071717364441db1130336a318e4288a993a41`.

Post-integration Full War Room CI `35348990387` completed SUCCESS across classify, Governance and full test.

WR-083 remains `MERGED`, not CLOSED, until WR-090 verifies the required credentialed NO-SCORING protected canary on canonical main.


## Final closure — WR-090 canonical-main canary — 2026-09-18

WR-083 is CLOSED after:
- WR-089 fresh independent `PASS`;
- exact audited integration;
- Full canonical-main CI;
- WR-090 protected canonical-main NO-SCORING canary run `35366265783` SUCCESS.

Canary evidence:
- preflight SUCCESS;
- trust-gate SUCCESS;
- protected-no-scoring-proof SUCCESS;
- future scoring SKIPPED;
- 14 retained inputs verified;
- provider mutations 0;
- consumer provider credentials absent;
- consumer re-hash/re-size 14/14;
- cleanup PASS;
- Actions artifacts 0;
- `real_scoring=false`;
- `historical_targets_exposed=false`.

The protected bridge is accepted for future Manager-authorized WR-081 execution.
