# WR-003 — ESPN Completion-State Consistency

TASK ID: WR-003

OBJECTIVE:
Ensure the Companion cannot report a draft as incomplete after the authoritative numbered-pick ledger is complete, including post-Rescan false UI-heartbeat scenarios.

WHY IT IS NEEDED:
A complete ledger is terminal draft-state evidence. Allowing a later false UI-derived `draftComplete` heartbeat to demote completion creates contradictory Companion diagnostics/state and can undermine trust in live synchronization.

VERIFIED STARTING STATE:
- Canonical main at task normalization: `a6506d5815e6ec9027f71da759fbe607a40b5020`
- PR #108 is open, mergeable, not merged
- PR #108 head: `d9b537ddac665207ab61aed7527d7da986cc4815`
- PR #108 base: current `main`
- PR description reports a deterministic pre-fix regression, production fix, and automated validation
- no independent Auditor verdict exists under the new operating contract

REQUIRED BEHAVIOR:
- a complete unique configured numbered-pick ledger is terminal completion authority
- after ledger completion, false UI/heartbeat completion signals must not demote `draftComplete`
- current/expected completion counters must not regress below configured total after terminal ledger completion
- positive ESPN terminal signals may still mark completion before reconciliation finishes if existing architecture allows it
- incomplete/UI-only completion may still clear when authoritative completion has not been reached
- explicit reset/session change must still clear completion and ledger normally

LIKELY FILES / COMPONENTS:
- Companion background/service-worker completion state
- outbound War Room snapshot state
- deterministic completion-state regression tests
- manifest only if required to load the corrected required background entry

NON-GOALS:
- no click-provenance behavior changes
- no ESPN view navigation changes
- no ranking/scoring/recommendation changes
- no UI/presentation redesign
- no new browser permissions

EDGE CASES:
- complete ledger followed by false top-frame heartbeat
- incomplete ledger with stale positive/negative UI completion signal
- rescan after completion
- explicit draft reset or new session
- outbound snapshot and War Room ACK after terminal completion

TEST REQUIREMENTS:
- Level 1 independent diff review
- Level 2 automated regression and full relevant Companion/root suites
- Level 3 deterministic Rescan → false heartbeat → complete ledger → snapshot/ACK scenario
- Level 4 only if Auditor determines browser/live uncertainty remains material after lower-level evidence

ACCEPTANCE CRITERIA:
- task scope matches the objective above
- branch freshness/integration against current main is verified
- deterministic regression proves the pre-fix bug and post-fix invariant
- no permission expansion
- no unrelated production changes
- Independent Auditor returns PASS or PASS WITH NON-BLOCKING FINDINGS

EXPECTED HANDOFF:
Auditor handoff with task ID, audited base/head, diff scope, tests independently verified, validation levels achieved, findings by severity, PASS/FAIL verdict, and explicit merge recommendation.
