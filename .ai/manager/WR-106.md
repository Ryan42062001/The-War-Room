# WR-106 — v2.1 Stage-Gate Bridge Status Contract Remediation

TASK ID: WR-106
ROLE: Work Helper / Super Troubleshooter
STATUS: ASSIGNED
DATE: 2026-09-19
DEPENDENCY: HARD — failed WR-101 R2 protected execution run 35444278227
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-106-v21-stage-gate-status-remediation`
AUDIT REQUIRED: YES
SCORING AUTHORITY: NONE

## Failure evidence

The fresh R2 authorized WR-101 execution failed closed:

- workflow run `35444278227`;
- canonical dispatch head `6dc3d5ff523f556302cc1b7fab5f3fe6ff4d3121`;
- preflight `105900417742` SUCCESS;
- trust gate `105900534166` SUCCESS;
- protected scoring job `105900552923` FAILURE;
- fresh execution branch `wr-101-v21-validation-scoring-execution-r2`;
- exact authorized pre-execution head `c47209cbd21ff3d42ee2867108cb9f2707212969`;
- consumer SHA-256 `74ae7a44bf60399957fdca57bad0c879486df07c4ff0524093a69c84d82e2296`;
- authority SHA-256 `2ef299a0de94fabda98095676208f9c50a34076d52ed14e53a322b963b411c0f`;
- live head / exact checkout / reviewed consumer / retained retrieval / pre-exposure live-head checks all PASS;
- protected failure: `WR-097 FAIL CLOSED: stage gate decision status missing`;
- publication staging, push and receipt verification all SKIPPED;
- cleanup PASS;
- Actions artifacts zero;
- R2 execution branch remained exactly at `c47209cbd21ff3d42ee2867108cb9f2707212969`.

The R2 authority has been revoked. Do not dispatch scoring.

## Deterministic contract evidence

Current consumer `_stage_gate()` computes `status_label` and includes it in the published stage-gate artifact.

However, its bridge result passed to `_finish()` includes only:
- `stage`;
- `gate_pass`;
- `prediction_lock_set_sha256`.

The accepted protected wrapper requires a non-empty bridge `status_label` immediately after the consumer returns.

Therefore the live failure is explained by a bridge-result contract omission even though the internal stage-gate artifact itself contains the label.

Do not treat this Manager analysis as sufficient proof. Reproduce it independently with synthetic/local state before changing behavior.

## Objective

Apply the smallest correction required so the consumer bridge result exports the already-computed `status_label` expected by the protected wrapper, without changing gate calculation, decision labeling, chronology, source/model semantics, publication families or authority behavior.

## Authorized write scope

Only:
- `.ai/work_helper/**`;
- `.ai/research/WR097_V21_PROTECTED_SCORING_CONSUMER.py`;
- `.ai/research/WR097_V21_PROTECTED_SCORING_CONSUMER_TEST.py`;
- `scripts/custody/test_wr097_v21_protected_scoring.py`.

Do not modify:
- `scripts/custody/wr097_v21_protected_scoring.py`;
- workflow YAML;
- Manager/shared/Auditor files;
- protocol/source/cohort/model/gate artifacts;
- production/ranking/composition surfaces.

## Requirements

1. Reproduce the omission synthetically before the fix.
2. Prove stage-gate artifact contains the expected `status_label`.
3. Prove the pre-fix bridge result omits it.
4. Add the smallest consumer fix so bridge and artifact expose the same status label.
5. Add a direct regression that would fail on the old behavior.
6. Add an actual protected-wrapper synthetic regression that reaches stage-gate and proves the wrapper accepts the bridge label.
7. Preserve exact validation/confirmation gate formulas and status-label semantics.
8. Preserve prediction-lock-before-target exposure.
9. Preserve validation-before-confirmation chronology.
10. Preserve wrapper fail-close behavior if `status_label` is missing/empty/tampered.
11. Do not access retained provider data or real target outcomes.
12. Do not dispatch WR-097.
13. Run focused consumer/custody regressions and Full War Room CI.
14. Publish task-specific Work Helper evidence/handoff and one immutable PR target.

## Hard prohibitions

No retained provider access. No real scoring. No new authority. No rerun of `35444278227`. No 2022–2025 raw target inspection. No 2026 outcomes. No gate/model/protocol/source/cohort changes. No production/ranking/composition/Phase 6 work.

## Next gate

Exact WR-106 freeze -> fresh WR-107 independent audit -> exact audited remediation integration -> canonical Full CI / no-scoring proof as required -> separate Manager decision on any future one-time execution authority.
