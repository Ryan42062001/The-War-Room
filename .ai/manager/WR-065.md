# WR-065 — Post-WR-063 Safe-Consumer Retained-Evidence Parser Bridge

TASK ID: WR-065
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: ASSIGNED
DATE: 2026-09-14
DEPENDENCY: INDEPENDENT
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-065-retained-safe-consumer-parser`
ASSIGNMENT MODE: `DIAGNOSIS + REMEDIATION`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-066
POST-MERGE CANARY REQUIRED: YES
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Objective

Resolve the exact execution gap frozen by WR-059 PR #184 without weakening any accepted custody, rights, privacy, or no-scoring boundary.

Create a narrowly scoped protected parser bridge that:

1. reads only the exact 15 source-byte identities already admitted by historical WR-042 custody;
2. retrieves them only through the accepted non-mutating retained-object trust model;
3. independently re-verifies authoritative SHA-256, byte size, and B2/R2 byte equality before any parsing;
4. completes provider access before the consumer phase;
5. proves provider secrets/credentials are absent from the consumer phase;
6. lets the consumer read only the already-verified runner-temporary files plus a verified local manifest;
7. emits only deterministic privacy-safe derived evidence needed by WR-059; and
8. cleans raw bytes and temporary reports on success and failure.

This task is infrastructure/evidence plumbing only. It does not perform WR-059 research synthesis and does not authorize WR-060 or any model work.

## Governing frozen evidence

Use these as immutable authority:

- WR-039 accepted evidence contract head: `00a9e787e716d6697e6cd0d9252982a672abbbe0`;
- WR-D008 accepted machine lock SHA-256: `3fac50f89afa1bb69d356c67f458f882f167ea595661d8eb4772b1a66cdb677a`;
- historical WR-042 evidence head: `614445a20c2c15fbc3d8c107644a5244ddb52076`;
- executed WR-042 manifest commit: `cc9005ae4bd9065cf80f1c184f31974904165c54`;
- WR-042 manifest SHA-256: `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- WR-042 custody run/job: `34871882486` / `104069521779`;
- accepted WR-063 exact audited target: `9db29b082cb61b5ef902b56bb5c745fc8ee739b2`;
- accepted WR-063 protected proof: run/job `34906157295` / `104183220181`;
- WR-064 verdict: `PASS`, no findings;
- WR-059 frozen fail-closed checkpoint: PR #184 / head `3c02f5a9a3ea858235772e7f2d065604632e7ee7`.

The exact retained allowlist must equal the 15 WR-042-admitted identities. Do not hand-wave or substitute provider state. If the historical manifest cannot be bound exactly, fail closed.

## Required implementation shape

Prefer a new dedicated WR-065 workflow and new scripts rather than altering accepted WR-063 runtime files.

Authorized implementation surface is limited to the registry write scope. In particular:

- do not modify `.github/workflows/wr063-retained-version-read.yml`;
- do not modify `scripts/custody/read_retained_versions.py`;
- do not modify accepted mutation-capable custody workflows/helpers;
- do not modify `.ai/research/**`.

The protected workflow must separate provider and consumer phases.

### Provider phase

The provider phase may receive only the accepted dedicated read credentials already proven by WR-063/WR-064.

It must:

- verify the B2 bucket/prefix/capability boundary before object access;
- use exact full custody keys only;
- use version-aware B2 discovery and immutable file-ID retrieval;
- use exact-key R2 reads;
- require exact authoritative SHA-256 and byte size;
- require B2/R2 equality for every source instance;
- process exactly the 15 WR-042-admitted source identities and no others;
- perform zero provider mutation operations;
- never acquire or refresh upstream source bytes.

`draft_picks.csv` is not among the 15 admitted identities and must remain absent.

### Secret-isolation boundary

Before parsing:

- provider secret variables must be absent;
- shared mutation-capable custody credentials must be absent;
- consumer execution must receive no provider endpoint, bucket, API token, key ID, application key, access key, secret key, or equivalent credential;
- the consumer may receive only runner-local verified file paths and a privacy-safe verified-input manifest.

Use a separate workflow step/process with a deliberately minimal environment. Add tests proving the consumer cannot obtain provider credentials through inherited environment.

### Safe consumer

The safe consumer must re-hash and re-size every local input before parsing and fail closed on mismatch.

It may derive only evidence needed to close the WR-059 byte-derived gaps, including:

- physical full-file row count;
- exact ordered raw columns;
- WR-039-compliant ordered `(column,type,nullable)` schema and canonical schema hash;
- exact approved-view counts needed for cross-checks;
- exact ordered stable `(player_id_namespace, player_id, position)` inventories from completed historical regular-season QB/RB/WR/TE rows for target seasons 2014–2025, but never a 2026 target/outcome artifact;
- deterministic per-source and aggregate canonical digests;
- explicit source-instance lineage back to the exact WR-042 SHA identity.

Do not invent a new type/nullability contract. Implement the accepted WR-039 canonical semantics exactly. If WR-039 does not specify executable inference semantics sufficient to derive a field deterministically, stop and report that as a blocker rather than silently defining new research semantics.

The current retained `players.csv` may be parsed only for byte-derived source evidence explicitly permitted by WR-039. Do not use current metadata to rewrite historical cohort membership or introduce hindsight.

## Evidence publication

Raw bytes must remain runner-temporary and must never be committed, printed, or uploaded as an Actions artifact.

A privacy-safe derived evidence package may be preserved under `.ai/work_helper/**` only if it contains no raw source bytes, secrets, tokens, provider credentials, or disallowed 2026 outcome data.

The package must be deterministic, canonical-hashed, and sufficiently complete for later WR-059 R&D to consume without re-accessing raw provider bytes. At minimum it must preserve the exact typed-schema/row-count evidence for all 15 source instances and the ordered historical key inventories needed to repair the 2014–2017 gap.

## Required testing / live proof

Add focused local/unit regression coverage plus exact-head CI.

Before publication, run one protected live proof using the dedicated read-only credentials. The proof must demonstrate:

- exactly 15 retained identities verified;
- B2/R2 digest and size equality for every identity;
- provider mutation count `0`;
- consumer provider-credential presence `false`;
- consumer re-hash/re-size PASS for every input;
- parser output canonicalization PASS;
- raw-byte cleanup PASS;
- raw-byte Actions artifact count `0`;
- no upstream source access;
- no `draft_picks.csv`;
- no 2026 regular-season outcome-table use;
- no model/scoring/ranking/production/Phase-6 work.

## Publication contract

Publish one immutable WR-065 implementation/live-proof PR/head.

Update `.ai/work_helper/HANDOFF.md` and publish a concise task-specific evidence report under `.ai/work_helper/**`.

Return:

- exact PR/head;
- protected proof run/job;
- exact input count;
- provider mutation count;
- consumer credential-presence result;
- derived evidence artifact path/hash;
- raw artifact count;
- cleanup result;
- any failed-closed blocker.

Do not activate WR-066 yourself. Manager freezes the exact target first.

## Next gate

WR-066 independently audits the exact frozen WR-065 implementation/live-proof target. PASS-family alone permits Manager exact-head integration and the mandatory canonical-main canary. WR-059 remains blocked until that acceptance sequence completes.
