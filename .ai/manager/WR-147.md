# WR-147 — Rollback/Restore Rehearsal Target Qualification and Execution Contract

TASK ID: WR-147
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
ASSIGNMENT MODE: DIAGNOSIS ONLY — ROLLBACK/RESTORE PREFLIGHT
STATUS: CLOSED — WR-D073 accepts REHEARSAL_NOT_SAFE; no production rollback authorized
DATE: 2026-09-23
WORKFLOW: V3.5 CANONICAL
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: wr-147-rollback-restore-rehearsal-qualification
ASSIGNED BRANCH: wr-147-rollback-restore-rehearsal-qualification
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: 72c888e426a44155f928c71dfcfe58bdfcddd515
DEPENDENCY: INDEPENDENT
AUDIT REQUIRED: NO — preflight-only evidence; any actual rollback/restore is a separate Manager-authorized operational task
POST-MERGE CANARY REQUIRED: NO — publication is documentation-only

## Accepted upstream evidence

WR-146 is accepted as DEPLOYMENT_IDENTITY_ESTABLISHED.

Manager acceptance:
- WR-146 PR #411 exact head afce7bcd50d181908cc0e248e141c294ca9d9813
- exact two Work Helper files
- exact-head War Room CI #35898079375 SUCCESS
- Governance #107306983909 SUCCESS
- Manager acceptance comment 5800036567
- evidence integrated canonical as 72c888e426a44155f928c71dfcfe58bdfcddd515
- post-integration canonical-main War Room CI #35898669523 SUCCESS / Governance #107308989302

WR-146 independently established that GitHub Pages uses dynamic Pages branch-source build inputs from main / repository root, and the observed deployment/run bound the served candidate to an immutable main SHA plus a 29-file served-byte manifest. Current accepted product content originates at WR-143 integration 45cab0c189c284b4a3011b78ce953b99dd857194.

WR-146 recommended ROLLBACK_RESTORE_REHEARSAL_READY_FOR_SEPARATE_AUTHORIZATION.

Manager does NOT authorize rollback in WR-147. WR-147 must first qualify the rollback target and produce an exact operational contract.

## Why this preflight is necessary

A production rollback rehearsal must be materially distinct enough to prove deployment/restore behavior, but safe enough to expose briefly.

The obvious pre-WR-143 candidate family is materially distinct because js/war-room-command-bar.js contains the old myNextPick === null completion shortcut. That is the known WR137-F01 LOW defect and therefore such a target is NOT automatically "known-good."

A docs-only historical SHA is operationally safer but product-byte-identical and would not prove a material rollback.

WR-147 must independently determine whether:
1. a materially distinct historical canonical target exists that is release-safe enough for rehearsal;
2. if not, a transport-only target with a known bounded limitation can be safely fenced for a short maintenance rehearsal;
3. or no production rollback rehearsal should be authorized.

## Broad read-only authority

Read/inspect as needed:
- current canonical main, ACTIVE_TASKS, Workflow V3.5, Work Helper charter, WR-145/146 accepted evidence;
- latest Pages build/deployment evidence at WR-147 execution time, not only WR-146's earlier observation;
- git history, product-changing canonical commits, accepted Manager dispositions, Auditor verdicts and canonical-main FULL CI receipts;
- historical product source/blobs for candidate SHAs;
- service-worker/cache behavior, public Pages cache headers and deployment propagation evidence;
- current exact served-byte manifest/product tree;
- prior incident/findings history relevant to candidate safety;
- current repository source and tests READ ONLY.

No external provider/live ESPN action.

## EXACT authorized writes — TWO files only

1. .ai/work_helper/WR147_ROLLBACK_RESTORE_REHEARSAL_QUALIFICATION.md
2. .ai/work_helper/HANDOFF.md

NO OTHER WRITES.

## A. Refresh current restoration identity

Because every main docs/control-plane merge can trigger a new Pages deployment even when product bytes are unchanged, refresh the latest successful Pages deployment at task execution time.

Record separately:
- exact current canonical main SHA;
- latest successful Pages deployment/run SHA and IDs;
- accepted current product-origin SHA;
- current served runtime manifest digest / discriminating asset hashes;
- service-worker cache name and relevant HTTP cache headers.

Future execution must reverify these immediately before any mutation; WR-147's recorded SHA is not permanently authoritative if main later advances.

## B. Enumerate materially distinct prior candidates

Find the most relevant historical canonical product states before current WR-143 product integration.

Do not examine only c8c03bdb... by name. Identify the actual product-changing canonical commits / product trees that are plausible rollback candidates.

For each serious candidate record:
- exact immutable SHA;
- material served-byte differences versus current product;
- which runtime files differ;
- canonical-main FULL CI receipt and product job if one exists;
- independent audit/Manager acceptance state;
- known unresolved findings or later-remediated defects present in that candidate;
- whether the defect can affect a user during a short rehearsal;
- service-worker/cache differences;
- whether candidate is a genuine historical canonical state rather than an unmerged branch target.

Classify each candidate exactly:
- QUALIFIED_RELEASE_SAFE
- QUALIFIED_TRANSPORT_ONLY_WITH_KNOWN_LIMITATIONS
- REJECTED

QUALIFIED_RELEASE_SAFE requires no known accepted unresolved blocking or user-misleading defect that makes brief production exposure inappropriate.

QUALIFIED_TRANSPORT_ONLY_WITH_KNOWN_LIMITATIONS may contain a bounded known non-blocking defect only if the rehearsal can prevent meaningful user exposure and the defect does not undermine the deployment/restore measurements themselves.

REJECT candidates with historical failed CI/audit, unresolved blocking findings, unsafe trust/state behavior, ambiguous custody, or inability to distinguish served bytes.

## C. Decide whether a transport-only target is acceptable

If no QUALIFIED_RELEASE_SAFE materially distinct target exists, explicitly decide whether one known-limitation candidate can be used only for transport mechanics under all of these fences:
- short declared maintenance window;
- no live draft / no ESPN validation while rolled back;
- no recommendation/ranking/source conclusions;
- immediately measurable distinguishing asset;
- restoration prepared before rollback;
- current restore target identity frozen immediately before mutation;
- rollback success must never be interpreted as release quality.

The known WR137-F01 early DRAFT COMPLETE wording is not automatically acceptable. Analyze whether it is sufficiently fenced or disqualifying.

## D. Exact reversible mechanism — no force push

Design the safest repository-native reversible process compatible with Pages source main/root.

Compare plausible methods and choose exactly one proposed mechanism, such as guarded revert/revert-of-revert PRs, only if workflow and history make it safe.

The proposed mechanism must:
- never force-push or reset main;
- preserve audit history;
- use expected-head guards;
- isolate exact intended runtime/product paths where practical;
- require applicable pre-merge CI before each merge;
- capture exact canonical landed SHA after rollback and after restore;
- wait for genuine Pages build/deploy success on each landed SHA;
- verify served bytes after each deployment;
- restore to exact Manager-frozen current product identity even if documentation commits advance;
- avoid unrelated code/source/data changes.

Do not perform the mechanism.

## E. Deployment/cache/service-worker verification contract

Define exact evidence required at four checkpoints:
1. BEFORE rollback
2. ROLLED BACK
3. RESTORED
4. POST-RESTORE stability

At each appropriate checkpoint specify:
- canonical main SHA;
- Pages run/build/deploy IDs and conclusions;
- served response headers;
- exact discriminating runtime asset hash/blob;
- broader served manifest digest if needed;
- service-worker script hash/cache name;
- browser controller/cache handling;
- network-first/offline implications;
- cache-busting/query strategy that does not falsely prove normal-client update;
- smoke checks.

Pages currently advertises cache-control max-age around 600 seconds and service worker uses a named shell cache. Account for both CDN and browser/service-worker stale-client risks.

## F. Smoke checks

Define a small no-provider smoke suite for rollback and restore, such as:
- page loads;
- no fatal browser console error;
- settings/default contract visible;
- manual Taken/Mine basic operation;
- saved-state/persistence basic operation;
- command-bar discriminating behavior if the candidate intentionally differs there.

Do not use live ESPN or provider traffic.

For a transport-only known-defect target, do not require the known defect to pass as correct behavior. Instead use it only as a discriminator if safe.

## G. Abort/restore-failure contract

Define fail-closed stop conditions, including:
- expected main SHA moved unexpectedly;
- CI failed/cancelled/stalled;
- Pages deployment failed or deployment SHA mismatch;
- served bytes do not match intended target;
- CDN/service-worker ambiguity cannot be cleared;
- public app fatal error;
- restoration PR cannot merge cleanly;
- restoration deployment fails;
- restored bytes do not equal frozen current manifest.

If rollback succeeds but restoration does not, escalation must prioritize returning the current known-good product bytes and must not proceed to live ESPN/A6.

## H. Overall preflight result

Return EXACTLY ONE:
- REHEARSAL_AUTHORIZATION_READY_WITH_RELEASE_SAFE_TARGET
- REHEARSAL_AUTHORIZATION_READY_TRANSPORT_ONLY
- REHEARSAL_NOT_SAFE

If ready, identify exactly one qualified candidate SHA/tree and one proposed reversible mechanism.

Then return EXACTLY ONE next Manager recommendation:
- AUTHORIZE_BOUNDED_ROLLBACK_RESTORE_REHEARSAL
- DO_NOT_RUN_PRODUCTION_ROLLBACK_USE_NONPRODUCTION_EVIDENCE
- TARGET_QUALIFICATION_REMEDIATION_REQUIRED

Do not perform rollback/restore.

## Hard stops

NO:
- main/branch mutation beyond the two documentation files;
- rollback/restore/revert execution;
- deploy/redeploy;
- Pages/environment/settings mutation;
- release/tag creation;
- product/test/workflow/source/data change;
- live ESPN/account/provider action;
- A6 go/no-go or release declaration.

## Publication

Publish ONE Work Helper PR from wr-147-rollback-restore-rehearsal-qualification, exactly the two authorized files.

Verify exact two-file cumulative scope, immutable final head, exact-final-head applicable War Room Governance SUCCESS, product correctly SKIPPED.

Stop writes after freeze. Return final SHA, PR, Governance run/job IDs, candidate classification table, overall preflight result, exactly one candidate if ready, proposed mechanism, abort contract and exactly one next Manager recommendation. Do not merge your own PR.


## WR-D073 — Accepted result / closure (2026-09-23)

WR-147 PR #413 exact Worker head b132cba0bf60d631bffc3570167c6bd1e898b15d; exact-two-file evidence; final-head War Room CI #35900428974 SUCCESS, Governance #107314975248 SUCCESS, product #107315066501 SKIPPED. Manager acceptance comment 5800335740. Worker diagnosis REHEARSAL_NOT_SAFE; recommendation DO_NOT_RUN_PRODUCTION_ROLLBACK_USE_NONPRODUCTION_EVIDENCE. Evidence integrated canonical cc76aa9f1b865d54f58ac40bbed3b8fd4c37f186; genuine post-integration main War Room CI #35900984817 SUCCESS incl Governance #107316905710 SUCCESS, product skipped.

The three inspected materially distinct historical canonical product families are REJECTED as public rollback rehearsal targets; no QUALIFIED_RELEASE_SAFE or QUALIFIED_TRANSPORT_ONLY target. Manager does not authorize production rollback/restore or deployment mutation. The prospective guarded two-PR procedure in the report is WITHHELD, not a work order. Follow-up is a separate, not-yet-assigned nonproduction recovery-evidence plan with independent admissibility review before formal A6. No live ESPN, 2027 source import, ranking-policy or release authority.
