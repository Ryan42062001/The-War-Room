# WR-099 — V3.5 Protected Workflow Identity Integration

TASK ID: WR-099
ROLE: Manager / Architect
STATUS: CLOSED — MERGED / CANARY PASS
DATE: 2026-09-18
DEPENDENCY: HARD — exact audited WR-097 canonical integration plus canonical-main NO-SCORING canary
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `manager/wr-099-protected-workflow-identity-integration`
AUDIT REQUIRED: YES
POST-MERGE CANARY REQUIRED: YES
PRODUCTION AUTHORIZATION: NONE
SCORING AUTHORIZATION: NONE
TARGET-OUTCOME AUTHORIZATION: NONE
USER ACTION REQUIRED: NO — prerequisite canary accepted

## Objective

Make the smallest Manager-controlled Workflow V3.5 integration change required for canonical authority-consumption verification to recognize the accepted protected workflow identity for Returning-Player v2.1 while preserving the already accepted WR-083 historical path.

This task is control-plane authority verification only.

It must NOT:
- create or consume a real scoring authority;
- dispatch authorized scoring;
- expose or inspect 2022–2025 outcomes;
- alter the WR-097 consumer, custody bridge, protected workflow, protocol, source/cohort authority, or publication semantics;
- alter WR-083 protected execution semantics;
- begin season-total composition or Phase 6.

## Activation prerequisite — canonical-main WR-097 NO-SCORING canary

WR-097 has been independently audited and integrated exactly:
- frozen audited WR-097 target: `75c0fbcd518438a226a8c49e3e11951de3944638`;
- WR-098 verdict: `PASS`, no findings;
- Auditor PR #277 / immutable Auditor head `941ace56d120dba7c3abb78f3c6ae3871448f658`;
- exact audited bytes integrated via PR #278;
- canonical integration merge: `3956e88be165df29a83442cb624b198b7347e381`;
- post-integration Full War Room CI `35419965619`: SUCCESS.

Before implementation begins, run on canonical `main`:

`WR-097 Returning-Player v2.1 Protected Scoring Bridge`

with:

`mode = no-scoring`

Manager must verify:
- event = `workflow_dispatch`;
- head branch = `main`;
- run head contains the exact audited WR-097 implementation bytes;
- preflight SUCCESS;
- trust gate SUCCESS in `no-scoring` mode;
- protected no-scoring readiness SUCCESS;
- future authorized v2.1 scoring SKIPPED;
- exactly 14 retained identities verified;
- provider mutation operations 0;
- consumer provider credentials absent;
- consumer re-hash/re-size 14/14;
- cleanup PASS;
- Actions artifacts 0;
- `real_scoring=false`;
- `historical_targets_exposed=false`;
- `target_outcomes_2022_2025_exposed=false`.

Until this canary passes, WR-099 remains BLOCKED.

## Exact problem to solve

Canonical `scripts/workflow-manager-transition.mjs` currently hard-codes successful protected workflow-run identity to:

`WR-083 Protected Historical Scoring Bridge`

in both live workflow-run verification and committed authority-consumption verification.

The accepted new workflow is:

`WR-097 Returning-Player v2.1 Protected Scoring Bridge`

The existing restriction is intentionally fail-closed. Do not replace WR-083 globally and do not weaken the check to an arbitrary name allowlist.

## Bounded implementation scope

Production implementation changes are limited to:

1. `scripts/workflow-manager-transition.mjs`
2. `scripts/test-workflow-manager-transition.mjs`

Manager-owned task/control-plane documentation may be updated separately as orchestration evidence, but no other executable surface is authorized.

## Required authority-binding design

Expected protected workflow identity must be derived from canonical task/authority context, never from caller-supplied workflow-name input.

At minimum, the implementation must preserve exact normalized `future_execution_authority` binding and distinguish the accepted execution families by authoritative consumer identity:

- legacy WR-081 authority consumer:
  `.ai/research/WR081_PROTECTED_SCORING_CONSUMER.py`
  -> expected workflow:
  `WR-083 Protected Historical Scoring Bridge`

- v2.1 authority consumer:
  `.ai/research/WR097_V21_PROTECTED_SCORING_CONSUMER.py`
  -> expected workflow:
  `WR-097 Returning-Player v2.1 Protected Scoring Bridge`

Unknown consumer/workflow families fail closed.

The live run and committed receipt/result verification must remain bound to the same canonical task, authority digest, branch/head, consumer path/digest, workflow run, publication parent/head, terminal result and one-time replay history.

The verified workflow run must also be tied to canonical repository/main execution context. A successful run with the right display name but wrong repository, branch, run identity, conclusion, or control-plane authority context must fail closed.

## Required adversarial regressions

Add direct regression coverage for all of the following:

- wrong workflow name;
- WR-083 authority paired with WR-097 workflow;
- WR-097 authority paired with WR-083 workflow;
- wrong repository;
- wrong branch;
- wrong workflow/control-plane head;
- wrong workflow run ID;
- failed workflow run;
- cancelled workflow run;
- in-progress workflow run;
- wrong consumer path;
- wrong consumer digest;
- replayed/previously consumed authority;
- fabricated authority-consumption receipt;
- authority/result cross-binding mismatch;
- workflow identity substitution;
- canonical run unavailable;
- GitHub API failure / non-success response;
- legitimate WR-083 historical authority path remains valid;
- legitimate WR-097 v2.1 authority path becomes valid only after this integration.

Preserve existing tests for:
- exact publication parent = authorized head;
- exactly one publication commit;
- terminal/receipt field binding;
- publication payload digest;
- machine-owned consumption history;
- removal/re-add replay protection.

## Freeze and audit chain

After the pre-implementation canary succeeds:

1. Manager unblocks WR-099 and creates the assigned branch from current canonical main.
2. Implement only the bounded transition-verifier/test changes.
3. Run direct Manager-transition/state regressions plus Full War Room CI.
4. Freeze one immutable WR-099 target.
5. Create a fresh Independent Auditor task using the next unused WR task number.
6. Auditor independently verifies all cross-binding/adversarial cases.
7. Manager integrates only the exact audited target.
8. Run a second canonical-main protected NO-SCORING canary.
9. Only after that complete chain may Manager consider a separate one-time real v2.1 validation-scoring task/authority.

No real scoring is authorized by WR-099.


## Activation prerequisite satisfied — 2026-09-19

Manager verified canonical-main WR-097 NO-SCORING run `35420945339` at control-plane head `21abf6e9d7bade0d638d40339b3ae4b699a6eacc`.

Required gate results:
- preflight SUCCESS;
- trust gate SUCCESS;
- protected NO-SCORING readiness SUCCESS;
- future authorized v2.1 scoring SKIPPED;
- exactly 14 retained identities verified;
- provider mutation operations 0;
- consumer provider credentials absent;
- consumer re-hash/re-size 14/14;
- cleanup PASS;
- Actions artifacts 0;
- `real_scoring=false`;
- `historical_targets_exposed=false`;
- `target_outcomes_2022_2025_exposed=false`.

WR-099 is unblocked for the bounded two-file Manager implementation only. No scoring authority exists.


## Manager exact implementation freeze — 2026-09-19

The bounded WR-099 implementation is frozen for fresh independent audit.

Exact target:
- PR #281 (draft; must remain unmerged during audit);
- branch `manager/wr-099-protected-workflow-identity-integration`;
- immutable target SHA `fd51d7ab40456182457fd19915baac8a88ae4468`;
- base canonical main `2da4444c7e5adfff86da769b17bee9b1b53768ca`;
- changed files exactly:
  - `scripts/workflow-manager-transition.mjs`;
  - `scripts/test-workflow-manager-transition.mjs`.

Manager verification before freeze:
- authority-derived workflow identity preserves exact WR-081 consumer -> WR-083 workflow mapping;
- exact WR-097 v2.1 consumer -> WR-097 protected workflow mapping added;
- unknown consumer families fail closed;
- live workflow verification binds run ID, workflow name, `workflow_dispatch`, canonical `main`, canonical control-plane head, canonical repository, completed/success state, authority SHA-256, consumer path and consumer digest;
- committed consumption verification rebinds the verified live evidence to the receipt/terminal/publication contract;
- existing one-time replay/global-history controls remain intact;
- direct regressions include cross-family workflow substitution, wrong repository/branch/head/run, failed/cancelled/in-progress runs, unknown consumer, wrong consumer digest, authority/result mismatch, API unavailable/non-success, and valid WR-083 + WR-097 paths.

Validation:
- Full War Room CI `35421600341`: SUCCESS;
- classify `105840218897`: SUCCESS;
- governance `105840233795`: SUCCESS, including workflow syntax, Manager-transition regression, canonical state, audit-readiness and custody regressions;
- product/browser test `105840261827`: SUCCESS, including browser determinism, WR-026 browser test, `npm test`, resilience syntax and backup/offline reload.

No scoring authority was created or consumed. No WR-097 workflow/consumer/custody code, source/cohort/protocol evidence, production ranking code or 2022–2025 outcome data was changed or inspected.

Next gate: WR-100 fresh independent audit of exactly `fd51d7ab40456182457fd19915baac8a88ae4468` on PR #281. Only a PASS-family verdict permits exact audited integration; a second canonical-main WR-097 NO-SCORING canary remains mandatory after integration and before any separate real v2.1 scoring decision.


## WR-100 audit failure / bounded remediation — 2026-09-19

Fresh independent WR-100 audit publication:
- audit PR #283;
- immutable Auditor head `9c587d609f8473717582c20dd4dbcecf1ad10158`;
- exact-head audit CI `35422248812` SUCCESS;
- audit evidence integrated as canonical main `25c593ca1e75f1493860e3b0dfe8304029a6797c`;
- post-merge War Room CI `35422443550` SUCCESS;
- verdict: `FAIL — REMEDIATION REQUIRED`;
- CRITICAL: none;
- HIGH: none;
- MEDIUM M-01: missing/null workflow-run `status` is accepted and coerced to `completed` when `conclusion === 'success'`;
- LOW L-01: focused regression gaps for wrong workflow event, direct verified-run consumer path/digest mismatch, and unconsumed-authority replacement.

The prior WR-099 target `fd51d7ab40456182457fd19915baac8a88ae4468` remains immutable failed-audit history and MUST NOT be merged.

Authorized remediation is strictly bounded to the existing two WR-099 files:
- `scripts/workflow-manager-transition.mjs`;
- `scripts/test-workflow-manager-transition.mjs`.

Required remediation:
1. require exact `run.status === 'completed'`;
2. remove `run.status ?? 'completed'` coercion;
3. add direct regressions for missing status and null status;
4. add focused wrong-event regression;
5. add direct verified-run consumer-path mismatch regression;
6. add direct verified-run consumer-digest mismatch regression;
7. add unconsumed-authority replacement regression;
8. preserve every previously passing WR-083/WR-097 identity, receipt, terminal, publication and replay boundary.

After remediation: run direct Manager-transition regression and Full War Room CI, freeze a NEW immutable WR-099 head on PR #281, then reactivate a fresh WR-100 audit lane. Do not run the second canonical-main WR-097 NO-SCORING canary and do not create/consume real scoring authority before PASS-family re-audit.


## Remediation freeze / fresh WR-100 re-audit — 2026-09-19

Bounded remediation completed on existing PR #281 without changing scope.

New immutable remediation target:
- branch `manager/wr-099-protected-workflow-identity-integration`;
- exact head `33d8d6037b1922841a134b9aba01eb3ea11ad97b`;
- parent failed-audit target `fd51d7ab40456182457fd19915baac8a88ae4468`;
- changed files remain exactly:
  - `scripts/workflow-manager-transition.mjs`;
  - `scripts/test-workflow-manager-transition.mjs`.

M-01 remediation:
- live run now requires exact `run.status === 'completed'`;
- null/undefined status no longer passes;
- no null-to-completed coercion remains;
- returned verified evidence preserves the actual required `run.status` value.

L-01 focused regression additions:
- wrong workflow event;
- missing workflow-run status;
- null workflow-run status;
- direct verified-run consumer-path mismatch;
- direct verified-run consumer-digest mismatch;
- replacement of an existing unconsumed authority.

Previously accepted WR-083/WR-097 mapping, canonical repository/main/control-plane binding, receipt/terminal/publication and replay protections remain unchanged outside the bounded remediation.

Validation at exact remediation head:
- push Full War Room CI `35422586327`: SUCCESS;
- PR Full War Room CI `35422588449`: SUCCESS;
- classify `105842857064`: SUCCESS;
- governance `105842869277`: SUCCESS, including Manager-transition regression;
- product/browser test `105842885865`: SUCCESS, including browser determinism, WR-026, `npm test`, resilience syntax and backup/offline reload.

Manager freezes `33d8d6037b1922841a134b9aba01eb3ea11ad97b` for a FRESH WR-100 re-audit. Prior WR-100 FAIL applies only to `fd51d7ab40456182457fd19915baac8a88ae4468` and is not overwritten or transferred.

PR #281 remains unmerged. No second canonical-main WR-097 NO-SCORING canary and no real scoring authority exists before PASS-family re-audit and exact audited integration.


## Final accepted disposition — 2026-09-19

Fresh WR-100 re-audit returned `PASS` with no findings on exact remediated target `33d8d6037b1922841a134b9aba01eb3ea11ad97b`.

Accepted audit evidence:
- Auditor PR #286;
- immutable Auditor head `453ce58c4ead8f3d734a8eea5568dfb22d4dfca6`;
- exact-head audit CI `35423220027` SUCCESS;
- Auditor evidence merged as canonical commit `59907cac3fa6d6f7deec5ef3884ddeb84f64f0e3`.

Manager then integrated only the exact audited WR-099 target through PR #281 as canonical merge `6d49fa07b86ecdb5c92fcf127f83dd12d841c4c9`.

Post-integration gates:
- canonical-main Full War Room CI `35423356815`: SUCCESS;
- second canonical-main WR-097 NO-SCORING canary `35423633965`: SUCCESS;
- canary preflight `105845688331`: SUCCESS;
- trust-gate `105845782358`: SUCCESS with mode `no-scoring`;
- protected readiness `105845796842`: SUCCESS;
- future authorized scoring `105845797460`: SKIPPED;
- 14/14 retained identities verified;
- consumer re-hash/re-size 14/14;
- provider mutation operations 0;
- consumer provider credentials absent;
- raw Actions artifacts 0;
- `real_scoring=false`;
- `historical_targets_exposed=false`;
- `target_outcomes_2022_2025_exposed=false`;
- `regular_season_2026_outcomes_inspected=false`;
- cleanup PASS.

WR-099 is CLOSED. Its completion authorizes only a separate Manager decision on a fresh one-time v2.1 validation-scoring task; WR-099 itself never authorizes scoring.
