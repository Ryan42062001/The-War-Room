# WR-078 — Workflow V3.3 Audit-Readiness + Manager Transition Automation

TASK ID: WR-078
ROLE: Manager / Architect
STATUS: REWORK_REQUIRED
DATE: 2026-09-16
DEPENDENCY: INDEPENDENT — parallel workflow-efficiency lane
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `manager/wr-078-workflow-v33-efficiency`
STARTING MAIN SHA: `ca6362e1b7128f334a6ae584c67101244a4d8db8`
AUDIT REQUIRED: YES — next fresh audit WR-080
POST-MERGE CANARY REQUIRED: YES
PRODUCTION AUTHORIZATION: NONE

## Objective

Implement a bounded Workflow V3.3 candidate that catches mechanical audit-readiness defects before independent audit and reduces Manager control-plane mismatch loops, while preserving every substantive V3.2 safety gate.

## Historical failed audit target

WR-079 independently audited exact PR #217 head:

`0b25767ce56c44505e9364adc9c536d57c46a1e5`

WR-079 Auditor head:

`a71b058644d594796e816a35d839eee2e160ad0c`

Audit PR:

`#219`

Verdict:

`FAIL — REMEDIATION REQUIRED`

Findings:

- CRITICAL: none
- HIGH: none
- MEDIUM: `WR-079-AUD-01`, `WR-079-AUD-02`
- LOW: none

The historical failed target above remains immutable evidence and must not be treated as accepted or silently rewritten.

## Authorized bounded remediation

Resolve only:

1. `WR-079-AUD-01` — automatic audit-readiness must not claim an active task solely from branch-name equality. Public-fork PRs reusing an active branch name must safely skip/fail closed unless repository/PR identity matches the authorized lane.
2. `WR-079-AUD-02` — `version_bump` must distinguish an invalid comparison ref from a valid ref where the artifact path is absent. Invalid/malformed comparison authority must fail closed.

Required focused regressions:

- same branch name / different PR head repository is not attributed to the active task;
- expected same-repository active branch remains eligible;
- malformed/unknown `relative_to` ref blocks `version_bump`;
- valid ref + absent path retains the intended new-artifact behavior;
- valid ref + existing path retains existing version-bump semantics.

## Preserved positive audit evidence

Do not redesign unrelated accepted portions of the candidate. WR-079 found the following positive boundaries intact:

- V3.2 remains canonical;
- readiness evidence is not an audit verdict;
- exact PR-head packet binding works;
- synthetic merge checkout is restored before remaining integration/custody checks;
- frozen CI token posture is read-only/no-secret;
- Manager transition helper is dry-run by default and has no commit/push/merge/verdict authority;
- collision safety and existing workflow/custody regressions remain intact;
- workflow/script changes force full product/browser CI;
- no unrelated product/research/model/custody behavior changed.

## Authorized write scope

Only:

- `.ai/shared/**` and `.ai/manager/**` needed for Manager-owned control-plane/state work;
- `.github/workflows/ci.yml`;
- `scripts/workflow-audit-readiness.mjs`;
- `scripts/test-workflow-audit-readiness.mjs`;
- `scripts/workflow-manager-transition.mjs`;
- `scripts/test-workflow-manager-transition.mjs`.

Do not modify production code, research/auditor/work-helper evidence, custody scripts, ranking/model semantics, or the WR-072/077 chronology.

## Next gate

Continue existing PR #217 and branch `manager/wr-078-workflow-v33-efficiency`.

Publish one new immutable remediation head, preserve historical failed head `0b25767...`, require exact-head FULL War Room CI and green readiness evidence, then Manager freezes the new exact target and activates WR-080 fresh Independent Auditor / QA.

Do not merge WR-078 before PASS-family WR-080. Even after PASS-family integration, V3.3 is not canonical until the mandatory canonical-main full-CI canary passes and Manager records final disposition.
