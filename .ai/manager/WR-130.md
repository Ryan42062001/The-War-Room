# WR-130 — Round-Count 5–30 Contract Unification

TASK ID: WR-130
ROLE: Implementation Engineer / Builder
STATUS: CLOSED — WR-D047 AUDIT PASS / EXACT INTEGRATION / CANONICAL FULL CI SUCCESS
DATE: 2026-09-21
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — consumes accepted WR-129 inventory / WR-D045
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-130-round-count-contract-unification`
ASSIGNED BRANCH: `wr-130-round-count-contract-unification`
PRE-ACTIVATION MAIN CHECKPOINT: `3adb811322e13b354e1f4538d66c52c56abd56e6`
ACTIVATION: Manager must merge the WR-D045 control-plane PR, verify genuine canonical-main push Governance SUCCESS, then create this Builder branch from that exact new main and record the initial SHA. The checkpoint above is not the permitted branch creation SHA.
AUDIT REQUIRED: YES — fresh independent Auditor on a Manager-frozen immutable Builder target before any production merge.
POST-MERGE CANARY REQUIRED: YES — genuine canonical-main FULL War Room CI after any independently audited implementation merge and before closure.

## Accepted finding and objective

WR-129 established a concrete contract mismatch. The visible/documented Draft Position contract is **5–30 rounds**, while several active app, persistence, external-pick and ESPN Companion paths accept, clamp, restore or expose **1–30**.

Manager independently confirmed this in current source, including:
- `index.html#pcRounds` min 5 / max 30;
- `js/war-room-command-bar-fixes.js:canonicalSettings()` and command-bar rounds input using minimum 1;
- `js/war-room-espn-sync.js` sync and saved-payload normalization using minimum 1;
- `js/war-room-external-picks.js` restored/snapshot round normalization using minimum 1;
- Companion popup/content/background paths using minimum 1.

Correct only this mismatch so every currently supported rounds ingress/persistence bridge enforces the existing 5–30 product contract. Do not add new league formats or personalization features.

## Authorized write scope — exact

Production / Companion:
- `js/war-room-command-bar-fixes.js`
- `js/war-room-espn-sync.js`
- `js/war-room-external-picks.js`
- `extensions/espn-companion/popup.html`
- `extensions/espn-companion/war-room-content.js`
- `extensions/espn-companion/background.js`

Focused tests:
- `scripts/test-browser.mjs`
- `extensions/espn-companion/test/background.test.cjs`
- `extensions/espn-companion/test/manifest.test.cjs`

Builder evidence only:
- `.ai/builder/WR130_ROUND_COUNT_CONTRACT_EVIDENCE.md`
- `.ai/builder/HANDOFF.md`

No other file is authorized. If a required behavior cannot be safely proven inside this exact scope, stop and report the blocker rather than broadening the patch.

## Required behavior

### A. Canonical contract

The only supported round-count range remains **5 through 30 inclusive**.

- 5 must remain valid.
- 30 must remain valid.
- 1–4 must never remain active as a league round count.
- values above 30 must not remain active.
- preserve default/fallback round count 16 where existing code uses it.
- preserve existing integer/fallback semantics except for changing the lower valid bound from 1 to 5.

### B. App settings and persistence

- Command-bar canonical settings and rendered rounds input must use minimum 5.
- ESPN/app sync settings normalization must enforce 5–30.
- Saved draft payload normalization must enforce 5–30.
- Existing settings/session isolation, migration and team/slot behavior must remain unchanged.

### C. External ESPN pick state

- Restored external-pick state and snapshot-derived settings must normalize round count to 5–30.
- No pick ownership, numbering, authority, ledger, ranking or recommendation behavior may otherwise change.

### D. Companion

- Popup rounds input must advertise min 5 / max 30.
- War Room content bridge must reject settings outside 5–30 rather than forwarding an unsupported round count.
- Companion stored/background config normalization must enforce 5–30.
- Preserve teams 2–20, slot 1..teams, default 16, read-only ESPN behavior and all credential/security boundaries.

## Required tests / evidence

At minimum, prove:
1. app command-bar input exposes min 5/max 30;
2. app canonical settings normalize 1–4 to 5 while preserving 5 and 30;
3. persisted malformed/legacy payloads with rounds 1–4 normalize to 5 and >30 normalize to 30;
4. external-pick restored/snapshot settings normalize 1–4 to 5 and preserve valid 5/30;
5. Companion popup exposes min 5/max 30;
6. Companion content bridge rejects rounds 1–4 and accepts 5/30;
7. Companion background stored config normalizes rounds 1–4 to 5 and >30 to 30;
8. existing team, draft-slot, saved-session isolation, ESPN pick-state and recommendation/scoring invariants remain unchanged;
9. exact final cumulative diff contains only the 11 authorized paths.

Run applicable literal syntax checks, focused browser/settings tests, Companion tests, existing persistence/recovery tests as needed, and genuine exact-final-head FULL War Room CI. Record tests actually executed rather than inferred.

## Forbidden changes

Do NOT:
- change `index.html#pcRounds` (already correct);
- add Half-PPR, Standard, custom roster-slot UI, keepers, risk preferences or reusable league profiles;
- change ranking/scoring/recommendation policy, ECR/ADP authority, VORP formulas, snake ownership or draft decisions;
- fetch/import 2027 rankings or contact any provider;
- alter ESPN Companion permissions, authentication, credentials, capture architecture or provider endpoints;
- modify datasets, package files, workflow/CI/runner configuration, deployment or release behavior.

Preserve WR-D001 PPR ECR value vs ESPN timing, WR-D018 fallback-first / `LIVE_DIRECT_UNVERIFIED`, WR-D027 NO PROVIDER CONTACT, WR-D038 accepted recommendation-card repair, WR-D043 A3 closure, WR-D045 WR-129 acceptance and paused Track B gates.

## Publication / audit gate

Builder publishes one implementation PR against main, leaves it OPEN / UNMERGED, and stops on one immutable final head after exact-head FULL War Room CI. Builder does not self-audit or merge.

Manager must independently verify cumulative scope, behavior and actual CI, freeze the exact Builder target and activate a **fresh independent Auditor task**. Only PASS-family on the exact unchanged target permits a later separate Manager integration decision. Any later implementation merge requires genuine canonical-main FULL War Room CI before WR-130 closure.

## WR-D046 — Manager audit-readiness freeze (2026-09-21)

Manager independently refreshed PR #367, the assigned Builder branch, cumulative base→target comparison, exact-head CI and the actual changed settings source.

Frozen Builder target: `616541256c43a2d05a3831b254c53b200ca5950a`.

Verified evidence:
- PR #367 is OPEN / UNMERGED / mergeable at exact head `616541256c43a2d05a3831b254c53b200ca5950a`.
- Builder branch `wr-130-round-count-contract-unification` is identical to that SHA: 0 ahead / 0 behind.
- Creation base / current canonical main is `333601ee04457b3fb90895f0d6d99e8c2d31d6f0`.
- Base→target comparison is 12 commits ahead / 0 behind with exactly the 11 WR-130-authorized paths.
- Exact-head FULL War Room CI #35641063617 completed SUCCESS: classify `106470215167`, Governance `106470279743`, full product test `106470347214`; bootstrap-reuse `106470282022` SKIPPED.
- Manager source spot-check confirmed command-bar/app sync/external-pick restore+snapshot/Companion bridge+background+popup paths enforce 5–30 while retaining their existing surrounding team/slot/default/pick logic.

WR-130 is now AUDIT_READY. This is not an audit verdict and not implementation-merge authorization.

FREEZE: no further Builder writes, branch movement, force-push or merge. If PR #367 or the Builder branch moves from `616541256c43a2d05a3831b254c53b200ca5950a`, the audit target fails closed and any verdict must not transfer.

Fresh WR-131 Independent Auditor / QA is separately assigned after Manager control-plane activation gates.

## WR-D047 — Final canonical closure (2026-09-21)

Manager accepted WR-131 fresh independent **PASS** with no CRITICAL / HIGH / MEDIUM / LOW findings on exact frozen Builder target `616541256c43a2d05a3831b254c53b200ca5950a`.

Auditor PR #369 exact head `4c18ec0f7d998159f15322e0eb1efd74a1ca0497` contained exactly the two authorized Auditor evidence files and passed exact-head War Room CI #35644378006: classify `106481143761`, Governance `106481187511`; product test `106481277175` correctly SKIPPED. Manager merged Auditor evidence first as canonical `b3f604d529746694e0b908d8c22c40a7f7ff5e39`.

Manager then re-confirmed Builder PR #367 and branch remained exactly at audited SHA `616541256c43a2d05a3831b254c53b200ca5950a` (0 ahead / 0 behind) and merged only that expected head as canonical integration `dc0fdad005b1e391f488c1ab561787e815e3b49f`.

Mandatory genuine canonical-main FULL War Room CI #35644877329 on that integration completed SUCCESS:
- classify `106482774335` — SUCCESS
- Governance `106482835967` — SUCCESS
- full product test `106483138780` — SUCCESS
- bootstrap-reuse `106482838162` — SKIPPED

The full product job passed browser determinism stress, WR-026 phone decision view, `npm test`, resilience syntax, and backup/offline reload.

**WR-130 CLOSED.** The existing league round-count contract is now consistently 5–30 across the approved app/persistence/external-pick/Companion settings paths. This closure does not authorize alternate scoring, custom roster settings, keepers, risk preferences, reusable profiles, A4/2027 ranking refresh, provider contact, protected-source work, deployment, release, or Track B source/model work.

