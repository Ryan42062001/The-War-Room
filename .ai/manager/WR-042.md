# WR-042 — Returning-Player v2 Exact Source-Custody Freeze

TASK ID: WR-042
ROLE: Research & Development (R&D)
STATUS: ASSIGNED
DATE: 2026-09-13
DEPENDENCY: INDEPENDENT
EXECUTION MODE: WORK_MODE_HIGH_VALUE
TARGET BRANCH: `wr-042-v2-source-custody-retry-2`
TARGET PR: TO BE CREATED
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-043 after one admitted immutable no-scoring custody target exists
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Reactivation authority

Historical retry PR #153 remains immutable fail-closed evidence at `98e32ed106350906a3bad3352099549d1c7f140f` with 0 admitted sources. Do not advance or reuse it.

The two prior blockers are now dispositioned:

- WR-056 trusted custody runtime bridge is independently accepted and merged through PR #158; canonical-main canary `34769306210` is `SUCCESS`.
- WR-057 rights disposition is accepted and merged through PR #165 / merge `bc23851f64158d27c6faf7e93719bbd869b516e1`: `RAW_CUSTODY_NOT_ESTABLISHED — EXCLUDE_SOURCE` for the PFR-derived nflverse `draft_picks.csv` asset.

## Fresh retry objective

Create a new no-scoring source-custody checkpoint using the accepted WR-056 runtime bridge.

For the frozen WR-039 source classes:

- attempt exact custody only for source instances whose current rights/retention evidence permits the complete independently auditable custody chain;
- **do not acquire, download, parse, custody, or use `draft_picks.csv`**;
- record `NFLVERSE_DRAFT_CAPITAL_MINIMAL` / `draft_picks.csv` as excluded under the accepted WR-057 rights disposition rather than silently substituting another provider;
- preserve provider identity, digest/schema/cutoff/rights evidence and explicit ADMITTED / REJECTED / UNAVAILABLE / rights-exclusion disposition for every source instance required by the source-snapshot contract;
- publish one immutable no-scoring source-snapshot target if any source can be lawfully admitted.

The accepted WR-039 evidence architecture remains authoritative. WR-057 does not authorize silently removing draft-capital semantics from a later model. Before any model-protocol or scoring stage can proceed without this source, Manager must route the required contract/feature-schema versioning and independent governance gate.

## Required runtime path

Use the accepted WR-056 trusted source-custody workflow/runtime. Do not modify custody implementation, workflow semantics, credentials, retention/lock policy, or WR039 / WR-D008 semantics inside WR-042.

Do not rerun the lawful WR-056 jq fixture merely to reconfirm the bridge. Execute only the bounded source-custody work necessary for this fresh WR-042 retry.

## Boundaries

No 2026 regular-season outcome inspection, source use before admission, model fitting/scoring/tuning/comparison/evaluation, target joins, ranking changes, production changes, or Phase-6 work.

Do not use `draft_picks.csv` under any route. Do not silently substitute a draft-data provider.

## Deliverable / next gate

Publish one fresh immutable WR-042 research target and evidence under `.ai/research/**`.

If at least one rights-compatible source instance is admitted into the frozen no-scoring snapshot, return that exact PR/head to Manager for WR-043 independent custody audit. If no source can be admitted, fail closed and return the exact evidence without activating WR-043.
