# WR-081 — Returning-Player v2 Historical Model Scoring + Result Evidence

TASK ID: WR-081
ROLE: Research & Development (R&D)
STATUS: ASSIGNED — CONSUMER PREPARATION
DATE: 2026-09-17
DEPENDENCY: HARD — accepted WR-072 / WR-077 pre-score protocol gate
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-081-v2-historical-model-scoring-execution`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-082 after protected bridge acceptance
EXTERNAL AUTHORITY EVIDENCE REQUIRED: NO

## Manager blocker disposition — 2026-09-17

WR-081 fail-closed attempt PR #227 is preserved as immutable blocker evidence at exact head `3f7ee6cc9294d8ae40921a5d4b50f2d0182f98ca`. Exact-head War Room CI `35296360912` completed SUCCESS: classify `105449599740`, governance `105449622679`, product test skipped for evidence-only scope.

Manager independently verified that PR #227 changes only:
- `.ai/research/WR081_HISTORICAL_SCORING_BLOCKER.md`;
- `.ai/research/HANDOFF.md`.

No development, validation, or confirmation scoring occurred. Terminal state is `NOT_ESTABLISHED — EXECUTION BLOCKED BEFORE SCORING`.

The blocker is technical, not a failed model result: the exact retained annual stats rows are not available to the R&D lane, while the accepted WR-069 protected path is intentionally bound to a schema/inventory consumer and destroys runner-temporary raw bytes after that reviewed consumer finishes.

WR-081 is therefore BLOCKED until the separately implemented protected scoring bridge passes WR-084 independent audit and Manager post-integration canary. WR-082 remains blocked because no model-result target exists.

Do not broaden PR #227 or treat it as a score/result target. Future WR-081 execution will require an explicit Manager reactivation on a fresh execution branch after bridge acceptance.

## Objective

Execute the accepted Returning-Player v2 model protocol exactly as frozen by WR-072 and accepted after WR-077 PASS. Produce the complete historical model-result evidence package required for independent result audit before any season-total composition or downstream ranking work.

Canonical protocol authority:
- protocol `returning-player-v2-model-protocol/1.2.0-wr072`;
- result gates `returning-player-v2-result-gates/1.2.0-wr072`;
- exact accepted protocol head `a228d0002545a701aea8c7bead5de0bf36994764`;
- machine-lock SHA-256 `aed044e6b7df9684153181a7a97a47a86c50ce9049db063ecdd8a973b1a832b6`;
- accepted source snapshot `wr-returning-player-v2-source-snapshot/1.2.0-wr059` / `6af88adaea478351a2b9c4884ca248dfed9527cb97f05e3648bd82c5fe0b3cea`;
- accepted cohort `returning-player-v2-cohort/1.2.0-wr059` / `f62075ec3c13784dea4568fa69aae8a84d39ca70f074ca769132f1f143f2c3d4`.

## Authorized work

R&D may now perform only the historical fitting/scoring/evaluation work required by the frozen protocol, including:
- protocol-defined historical feature construction from the accepted retained source/cohort evidence;
- protocol-defined historical target joins for the declared development/validation/confirmation seasons only;
- frozen preprocessing and Ridge candidate fitting;
- frozen primary/secondary baseline calculations;
- staged development, validation, and confirmation scoring under the frozen gates;
- frozen cluster bootstrap and ordering metrics;
- full-row keyed prediction/evidence serialization required by the accepted v2 evidence contract;
- deterministic hashes/manifests/result summaries sufficient for independent reproduction and audit.

Execute the chronology exactly. Do not inspect later-stage results before the protocol says that stage is eligible. Fail closed on lineage, missingness, fallback, target, serialization, hash, or gate-input violations exactly as frozen.

## Hard prohibitions

Do not:
- use or inspect 2026 regular-season outcomes;
- access, download, parse, substitute, or use `draft_picks.csv`;
- reacquire, refresh, replace, or substitute historical source bytes;
- introduce current/replacement `players.csv` or another provider;
- mutate provider/storage state;
- add Players metadata, draft capital, or any predictor beyond the frozen 28 stats-only features;
- change target, chronology, splits, preprocessing, model, alpha, baselines, gate thresholds, bootstrap mechanics, or evidence semantics;
- tune or search hyperparameters;
- change production/ranking/recommendation code;
- perform season-total composition;
- begin Phase 6.

If any required retained input cannot be read lawfully and exactly under accepted custody, stop and return the smallest evidence-backed blocker. Do not silently reconstruct or substitute.

## Required publication

Write only `.ai/research/**`.

Publish one immutable WR-081 target with:
- exact branch/head and PR;
- exact protocol/source/cohort bindings;
- changed-file list;
- complete full-row keyed prediction/result evidence required by the accepted contract;
- stage-by-stage gate evidence and terminal protocol outcome;
- hashes/manifests sufficient for independent verification;
- exact environment/runtime evidence required by WR-072;
- tests/commands actually run and exact-head CI;
- explicit attestation that no forbidden 2026/source/provider/production/composition work occurred.

Do not merge the result target and do not activate WR-082. Return to Manager for exact-head verification, audit-readiness preflight, and freeze.

## V3.4 routing rationale

When reactivated, WR-081 uses `STANDARD_CHAT_HIGH` by default. Its accepted source/cohort/protocol and future audited bridge are authoritative inputs; R&D should consume them rather than re-solve them. Escalate only with a `WORK_MODE_ESCALATION_RECOMMENDED` packet if actual scoring execution becomes materially interaction-heavy.


## Post-WR-084 blocker update — 2026-09-18

WR-084 returned `FAIL — REMEDIATION REQUIRED` on the protected bridge with HIGH findings `WR-084-AUD-01` and `WR-084-AUD-02`.

WR-081 remains blocked before real scoring. The new gate is:
`WR-083 bounded remediation -> Manager exact freeze -> WR-089 fresh independent re-audit -> PASS-family -> exact audited bridge integration -> protected canonical-main canary -> explicit WR-081 reactivation on a fresh execution branch`.

Do not reuse PR #227 as a scoring target. No retained historical target/model execution is authorized while the bridge findings remain open.


## Post-WR-089 integration gate — 2026-09-18

WR-089 returned PASS with no findings and Manager integrated only exact audited WR-083 target `c9b13959f598b3633a78e2ff78d0862881982dd2` through PR #234 as canonical merge `ee0071717364441db1130336a318e4288a993a41`.

Post-integration Full War Room CI `35348990387` is SUCCESS.

WR-081 remains BLOCKED only on WR-090: the required credentialed `workflow_dispatch` NO-SCORING canary on canonical main. Do not begin real scoring until Manager verifies that canary and explicitly assigns a fresh WR-081 execution branch plus complete `future_execution_authority`.


## WR-090 SUCCESS / fresh reactivation — 2026-09-18

The protected bridge acceptance gate is complete.

Canonical bridge history:
- WR-089 verdict: `PASS` with no findings;
- exact audited WR-083 target: `c9b13959f598b3633a78e2ff78d0862881982dd2`;
- WR-083 integration merge: `ee0071717364441db1130336a318e4288a993a41`;
- canonical-main control-plane head at canary: `11f1014ba73a70563c29a8c6d4b11f8303298cdf`.

WR-090 canonical-main protected NO-SCORING canary:
- workflow run `35366265783` / run #39;
- event `workflow_dispatch`;
- branch `main`;
- head `11f1014ba73a70563c29a8c6d4b11f8303298cdf`;
- preflight `105669385717` — SUCCESS;
- trust-gate `105669587530` — SUCCESS;
- protected-no-scoring-proof `105669633112` — SUCCESS;
- future-authorized-wr081-scoring `105669634455` — SKIPPED;
- verified retained inputs: 14;
- provider mutation operations: 0;
- R2 execution mutations: 0;
- consumer provider credential presence: false;
- consumer re-hash/re-size pass count: 14;
- cleanup: PASS;
- Actions artifacts: 0;
- `real_scoring=false`;
- `historical_targets_exposed=false`.

WR-081 is reactivated on a **fresh execution branch**:

`wr-081-v2-historical-model-scoring-execution`

The old branch / PR #227 remain immutable blocker history and are not the scoring execution target.

### Two-stage activation required by the audited bridge

The audited bridge requires canonical Manager authority to bind an exact branch, exact branch-head SHA, exact consumer path, and exact Manager-reviewed consumer SHA-256 **before any retained row or historical target can become consumer-visible**.

No reviewed WR-081 scoring consumer currently exists in the repository. Therefore reactivation proceeds in two stages.

#### Stage A — authorized now: consumer preparation only

R&D may:
- implement the protected scoring consumer entirely under `.ai/research/**`;
- use the recommended path `.ai/research/WR081_PROTECTED_SCORING_CONSUMER.py`;
- add only `.ai/research/**` tests/evidence needed to prove the consumer contract with synthetic/local fixtures;
- implement the bridge modes required by the audited wrapper: `predict`, `target-ingest`, and `stage-gate`;
- consume the already accepted WR-059 source/cohort and WR-072 protocol semantics exactly;
- prepare deterministic evidence serialization and local fail-closed tests without accessing retained provider bytes.

R&D must **not**:
- run `authorized-wr081-scoring`;
- access retained B2/R2 source bytes;
- perform any real historical target join/model fit/result inspection through the protected bridge;
- modify `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, `.ai/work_helper/**`, `.github/**`, `scripts/**`, `src/**`, or `public/**`;
- self-authorize a branch/head/path/digest;
- change the accepted protocol, chronology, feature set, baselines, gates, or source/custody policy.

Stage A completion packet must return:
1. exact fresh branch head SHA;
2. exact consumer path;
3. exact consumer SHA-256;
4. changed files;
5. local/synthetic tests actually run;
6. evidence that no retained-data scoring occurred;
7. PR number if R&D opens one.

Do not continue to real scoring after Stage A. Return to Manager.

#### Stage B — Manager authority, then protected scoring

After Stage A, Manager independently reviews the exact branch head and consumer file, then records `future_execution_authority` in canonical `.ai/shared/ACTIVE_TASKS.json` with exactly:

- `branch`;
- `head_sha`;
- `consumer_path`;
- `consumer_sha256`.

Only after that authority is merged to canonical `main` may the protected workflow be dispatched in `authorized-wr081-scoring` mode with values exactly matching Manager authority.

The protected workflow must then verify the live remote branch head, exact checkout, reviewed consumer identity, provider/custody boundary, prediction-before-target chronology, publication guard, and pre-push race check before publishing evidence.

### V3.4 routing

Stage A starts in `STANDARD_CHAT_HIGH` with `FAST_REFRESH`. Escalate with `WORK_MODE_ESCALATION_RECOMMENDED` only if consumer implementation becomes materially interaction-heavy. Do not consume a Work credit merely because the eventual scoring run is important.
