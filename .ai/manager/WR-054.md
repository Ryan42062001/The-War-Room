# WR-054 — Workflow V3.2 Lane-Identity Enforcement

TASK ID: WR-054
ROLE: Manager / Architect
STATUS: ASSIGNED
DATE: 2026-09-13
DEPENDENCY: INDEPENDENT
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `manager/wr-054-workflow-v32-lane-identity`
TARGET PR: TO BE CREATED
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-055
POST-MERGE CANARY REQUIRED: YES

## Serialization disposition

The temporary WR-056 collision is resolved. WR-056 is independently accepted, merged, and green on canonical-main canary `34769306210`, so `.github/workflows/ci.yml` is released back to WR-054.

Existing WR-054 work is preserved:
- branch tip `13a755d217202b8533ecfe5e2e4fa013f50a3396`;
- dangling child `2f32468688ac983a4e2d27b09d0f65a739478622` contains the lane-identity implementation and must be reconciled rather than discarded.

Because WR-056 changed the same CI surface after the original WR-054 baseline, Manager must reconcile the preserved WR-054 work onto current `main` without regressing the trusted source-custody CI additions.

## Objective

Harden Workflow V3.1.1 into the bounded V3.2 lane-identity safety upgrade by closing two execution-lane identity gaps:

1. task-scoped local preflight/finish checks currently report the checked-out branch but do not fail when it differs from the branch assigned in `ACTIVE_TASKS.json`;
2. static state validation does not yet bind TARGET BRANCH, EXECUTION MODE, and dependency class back to registry truth.

## Required implementation

- enforce assigned-branch identity in preflight and finish checks;
- detached HEAD or wrong assigned branch must fail;
- statically bind TASK ID, STATUS, TARGET BRANCH, EXECUTION MODE, and dependency class;
- preserve descriptive dependency suffix compatibility;
- add focused regression coverage;
- update workflow documentation;
- retain schema v3 unless genuinely necessary;
- preserve all accepted WR-056 trusted-custody workflow/CI behavior.

## Boundaries

Do not modify `.ai/research/**`, `.ai/auditor/**`, `.ai/work_helper/**`, production/user-facing code, ranking/model logic/data, custody semantics/configuration, or WR039 / WR-D008 semantics.

## Validation

Reconcile the preserved implementation against current `main`, then run exact-head Full CI. WR-055 independent audit remains required before merge. Canonical-main post-merge canary is required before V3.2 closure.

## Deliverable

Publish one immutable exact-head V3.2 candidate on the dedicated WR-054 branch, then return to Manager for fresh WR-055 audit activation.
