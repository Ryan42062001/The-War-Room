# WR-126 — Legacy Overall Board Pressure Rendered Truthfulness Reproducer

TASK ID: WR-126
ROLE: Implementation Engineer / Builder — TEST-FIRST SYNTHETIC BROWSER DIAGNOSTIC ONLY
STATUS: CLOSED — BOUNDED SYNTHETIC RENDERED DIAGNOSTIC ACCEPTED; SEPARATE WR-127 AUTHORIZED
DATE: 2026-09-20
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — consumes CLOSED WR-125 inventory / WR-D040 Manager acceptance
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-126-overall-board-pressure-rendered-truthfulness-reproducer`
ASSIGNED BRANCH: `wr-126-overall-board-pressure-rendered-truthfulness-reproducer`
PRE-ACTIVATION MAIN CHECKPOINT: `a2bd5cf55946ff64612276f7f82785b905bcc797`
ACTIVATION: Verify Manager WR-D040 PR merge and genuine post-merge canonical-main push Governance SUCCESS; Manager creates assigned branch at exact new main and verifies its initial SHA BEFORE Builder work. Checkpoint above is NOT the assigned branch creation SHA.
AUDIT REQUIRED: NO — bounded test-only diagnostic with direct Manager source/test review; a later production display fix requires fresh independent Auditor.
POST-MERGE CANARY REQUIRED: YES — changing the existing browser test harness requires exact canonical-main FULL War Room CI if integrated.

## Scope: establish the smallest real current-UI evidence, not another Command Center
WR-125 found the current Command Center already has on-clock bar, best-by-position decision strip, an expanded scored alternative, My Draft, position tiers, Targets queue, dedicated phone view, ESPN header trust and non-sticky coordinated layout. Never rebuild these. The only CURRENT LEAD under WR-126: `js/war-room-ui.js:updateDraftDayDashboard` renders `% next-pick survival` from an uncalibrated heuristic; `js/war-room-scoring.js:calculateNextPickSurvival` returns internal neutral 50 if market is unavailable for a nonadjacent pick; `draft-polish.css` hides the legacy Board Pressure in Position but not explicitly Overall. WR-125 DID NOT reproduce a misleading browser frame. WR-122 repaired the separate recommendation card and must not be changed.

## Authorized bounded execution
1. FAST_REFRESH main/registry/Builder charter/WR-125 accepted report/WR-D001,D018,D027,D038,D039,D040/current `scripts/test-browser.mjs`, `js/war-room-ui.js`, `js/war-room-scoring.js`, `draft-polish.css`, existing browser/phone/sync trust tests and Builder handoff. Inspect existing test fixtures and helpers first; do NOT create a new simulator/runner.
2. Add a MINIMAL deterministic synthetic rendered-browser diagnostic to the existing `scripts/test-browser.mjs`, using existing helpers and source-free fixture. Use Overall view at 390×844 and 1280×900. Choose or construct an actually rendered available positional best player with **verified** absent ESPN board rank/ADP AND FantasyPros fallback ADP, with valid actual draft state, >0 intervening picks, and `getMarketTimingDetails(best,context).marketRank == null`. Establish position row/card/player identity and current actual rendered `#board-pressure-widget` visibility; if fixture cannot prove those preconditions report `INDETERMINATE`, not a false finding.
3. Capture ACTUAL user-visible text and meter/accessible label when `#draft-day-dashboard` renders that player, including presence or absence of the allegedly misleading `50% next-pick survival`. In same harness, verify known-market control and that Position view hides legacy duplicate while WR-122-repaired recommendation card and current chosen player/action remain unchanged. Do not substitute an unmounted static HTML fixture or mock the display function's output. Exercise manual Taken/Mine only if necessary to reach a valid draft state, without changing real player datasets. Never reach out to ESPN or access a real room.
4. Report `REPRODUCED`, `NOT_REPRODUCED`, or `INDETERMINATE` with precise precondition values, viewport(s), actual text and screenshot only if already permitted by existing test artifact settings. A source-only `50` or a stand-alone helper result is NOT browser reproduction. Do not write a regression assertion that enshrines a misleading percentage as desired product behavior. The diagnostic may record it as an observation but should assert fixture preconditions and observable user-facing result.
5. No product fix is authorized in WR-126 even if the symptom reproduces. Produce one tightly bounded future Manager **conditional display-only** task recommendation, limited to the actual confirmed UI path and existing browser fixture; if NOT_REPRODUCED recommend NO PATCH or specify the precise missing fixture only. No changes to scoring, source resolver, numeric engine results, recommendation winner/weights, ESPN Companion, datasets or draft state.

## Exact write and tests
WRITE ONLY `scripts/test-browser.mjs`, `.ai/builder/WR126_OVERALL_BOARD_PRESSURE_REPRO_EVIDENCE.md`, `.ai/builder/HANDOFF.md`. Do not change `js/war-room-ui.js`, CSS, index, `package.json`, production modules, other tests/scripts, extension, dataset, CI/workflow/runner, shared/Manager/other-role files. If existing harness is insufficient within this exact scope, report BLOCKED with evidence and no unauthorized helper edits. Run existing `npm run test:browser` / applicable phone/overflow browser checks as available and full exact-head CI; identify execution limits honestly. One OPEN/UNMERGED Builder PR with exactly authorized changed paths, immutable final head and exact-head FULL War Room CI. Manager must review diagnostic credibility; if test is integrated, full canonical-main post-merge CI before closing. If working environment cannot produce real rendered evidence, return INDETERMINATE rather than claim a runtime finding.

Boundaries: preserve WR-D001 FantasyPros ECR VALUE vs ESPN market TIMING, WR-D018 fallback-first `LIVE_DIRECT_UNVERIFIED`, WR-D027 NO PROVIDER CONTACT, WR-D038 recommendation-card repair, paused custom-model no-source rights and WR-D039 no-duplication UX. No new employee, source/data, UI production code, rankings/scoring/algorithm, authenticated ESPN, provider contact, deployment or draft-ready release authorization. A future UI repair requires a separate Manager decision/Builder assignment, fresh independent QA and exact-main full canary.

## Final Manager disposition — WR-D041

Manager independently verified Builder PR #358 merged from exact Builder head `2bb97458bf08151d6cac3ae8259f62a3a794caf4` as canonical merge `e69bd96d549786e5b3d8c045d086dd5884ce3266`. Exact-head FULL CI `35555586954` completed SUCCESS (classify `106198499634`, Governance `106198515901`, full product test `106198543531`). Genuine canonical-main push FULL CI `35556294071` completed SUCCESS on the merge (classify `106200464625`, Governance `106200501153`, full product test `106200535682`).

ACCEPT `REPRODUCED` only for the bounded actual-mounted-render synthetic display-input case at 390x844 and 1280x900: with genuine unmodified row-backed missing-market data, the legacy Overall Board Pressure renderer displayed the internal neutral value 50 as `50% next-pick survival` with a 50%-width probability-like meter. The evidence preserves recommendation identity, player selection, action, scoring order and source-row attributes. It does not establish natural full-board prevalence, measured probability/calibration, live ESPN behavior, physical-device behavior or draft-ready release.

WR-126 is CLOSED and removed from the active-only registry. Production correction is not retroactively added to WR-126; it is separately scoped as WR-127 and requires fresh independent audit before implementation merge.
