# WR-069 — Accepted-Contract Safe-Consumer Retained-Evidence Parser Implementation

TASK ID: WR-069
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: ASSIGNED
DATE: 2026-09-15
DEPENDENCY: INDEPENDENT
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-069-retained-safe-consumer-parser-v2`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-070
POST-MERGE CANARY REQUIRED: YES
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Objective

Implement the fresh successor to fail-closed WR-065 now that WR-067/WR-068 have supplied and independently accepted executable deterministic CSV schema-inference semantics.

Build one separately reviewable provider-to-safe-consumer bridge that consumes exactly the 15 already-custodied WR-042 source identities, verifies them before parsing, removes provider authority from the consumer boundary, and derives only the byte-level evidence required for later WR-059 completion.

This is evidence infrastructure only. It is not R&D model work, not scoring, not production ranking work, and not Phase 6.

## Accepted semantic authority

The parser MUST implement exactly:

`wr-returning-player-v2-csv-schema-inference-addendum/1.0.0`

Accepted WR-067 PR #188 exact head:

`1e6b2105bce98408d3fb41f4aa07fcaa7ef6ca04`

Accepted machine lock:

`.ai/research/generated/RETURNING_PLAYER_V2_CSV_SCHEMA_INFERENCE_CONTRACT.json`

Machine-lock SHA-256:

`48d4ace7375a59ab28ad79b2777bd7de4a9c4871cea49e83447371131f60dddb`

Accepted conformance corpus:

`.ai/research/generated/RETURNING_PLAYER_V2_CSV_SCHEMA_INFERENCE_CONFORMANCE.json`

Conformance-corpus SHA-256:

`1f70d5e31ed5a62e00e5b02e06f6271e9c30b36951389d9607c50fdf5f71ffe1`

WR-068 independent audit PR #190 / exact audit head:

`a5ccce36012a5cf06d93bccf99d1834eaa268142`

WR-068 verdict: `PASS`, no findings, 49/49 conformance cases independently reproduced.

Exact accepted contract integration:

`abff69901a040bec378c6562845adde9780f1da5`

Canonical-main Governance after integration:

`34919117910` — SUCCESS

The accepted contract is normative. Library/default CSV parsing, dtype inference, NA-token inference, trimming, date inference, Unicode normalization, float coercion, sampled inference, or alternate serializer behavior is forbidden unless wrapped so the exact accepted semantics and all 49 conformance vectors reproduce byte-for-byte.

## Retained-source authority

Consume exactly the 15 already-custodied source identities bound by historical accepted WR-042 evidence:

- WR-042 evidence head `614445a20c2c15fbc3d8c107644a5244ddb52076`;
- executed manifest commit `cc9005ae4bd9065cf80f1c184f31974904165c54`;
- custody manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- trusted custody run/job `34871882486` / `104069521779`;
- 14 player-summary CSV assets for seasons 2012–2025 plus the one accepted players-metadata asset.

Use the executed WR-042 manifest/result as the exact identity authority for source names, SHA-256 values, byte sizes, retained keys, and any immutable provider identity already recorded.

`draft_picks.csv` is not one of the 15 and remains excluded under WR-057. Reject it or any substitute before provider access.

Do not silently refresh, reacquire, replace, recustody, or substitute any upstream source.

## Provider phase — required security boundary

The provider phase must finish all provider access before consumer execution.

B2:

- use only the accepted dedicated WR-063 read-only B2 credential secret pair;
- verify provider-issued bucket/prefix/capability restrictions before any list/download operation;
- require the accepted bucket/prefix boundary and `listFiles` + `readFiles` with no mutation-capable authority;
- discover versions only within the full exact content-addressed custody key/name boundary;
- require exact file-name equality;
- accept only the exact retained upload/version that reproduces the authoritative digest and byte size;
- download by immutable file/version identity where required.

R2:

- perform only exact-key read operations authorized by the accepted retained-read architecture;
- no broad listing sweep;
- verify exact authoritative digest and byte size.

For every one of the 15 identities:

1. B2 retrieval must match authoritative SHA-256 and byte size;
2. R2 retrieval must match authoritative SHA-256 and byte size;
3. B2 and R2 bytes must be equal before the file can enter the consumer manifest.

Provider mutation operations must be zero.

Do NOT perform or implement PUT/upload/copy/delete/overwrite/multipart mutation, metadata/tag mutation, retention mutation, Legal-Hold mutation, Bucket-Lock mutation, or governance bypass.

Do not modify, import, or invoke mutation-capable custody helpers.

Do not modify accepted WR-063 workflow/script/test files.

## Secret isolation / safe-consumer boundary

The consumer must run only after provider retrieval/verification completes.

Before invoking the consumer:

- produce a local verified manifest containing only privacy-safe input identity, expected digest/size, source role, and runner-temporary local path information required for consumption;
- remove/unset provider secret values and provider-authority environment variables from the consumer process;
- invoke the consumer with an explicit allowlisted environment or equivalent provable isolation boundary;
- make B2/R2 credentials, tokens, endpoints, bucket authority, key IDs, application keys, access keys, secret keys, session tokens, or equivalent provider authority unavailable to the consumer.

The consumer must independently re-hash and re-size each local file before parsing. Any mismatch fails closed and the mismatched local file must be removed immediately.

Tests must prove provider credentials are absent from the consumer execution environment and that a deliberate secret-presence injection fails closed.

## Parser requirements

The safe consumer must implement the accepted WR-067 contract exactly, including:

- strict BOM/UTF-8 behavior;
- exact four-state CSV grammar and quote semantics;
- LF/CRLF/lone-CR rules;
- exact header, blank-header, duplicate-header, width, EOF, whitespace, and malformed-input behavior;
- exact null distinction between unquoted empty and quoted empty;
- exact closed `boolean|int64|decimal|utf8` type vocabulary;
- exact signed-int64, overflow-to-decimal, exponent, signed-zero, leading-zero, identifier, non-finite, date/datetime non-inference, and UTF-8 fallback semantics;
- deterministic source-independent promotion;
- whole-file nullable semantics;
- complete-file scan and physical row-count semantics;
- exact fatal codes and precedence;
- exact version-bound canonical typed-schema serialization;
- exact schema SHA-256 procedure.

Before touching retained provider bytes in protected proof, focused tests must independently reproduce all 49 accepted synthetic conformance cases exactly: 33 PASS and 16 FATAL, zero mismatches.

Any ambiguity or conformance mismatch fails closed before retained-byte parsing.

## Authorized derived evidence

For each admitted retained source instance, derive only evidence required for WR-059, including as applicable:

- exact physical full-file row count;
- exact ordered raw columns;
- exact ordered `[column,type,nullable]` schema;
- exact canonical typed-schema SHA-256;
- approved-view row/count cross-checks needed by accepted WR-039 evidence semantics;
- exact source-instance lineage to the authoritative WR-042 identity;
- deterministic canonical digest(s) for the derived evidence;
- exact retained `players.csv` byte-derived parser evidence required by WR-059;
- exact ordered historical player-ID/position inventories required to close the missing 2014–2017 Returning-Player cohort evidence, without rewriting membership from current metadata.

The current retained `players.csv` may be used only for the byte-derived evidence specifically permitted by the accepted contract. It must not be used to rewrite historical cohort membership, infer unavailable historical state, or introduce hindsight.

Do not emit arbitrary raw rows or a general-purpose retained dataset.

Privacy-safe deterministic derived evidence may be committed only under `.ai/work_helper/**` and must be sufficient for later R&D to consume without receiving provider credentials or raw retained files.

## Raw-byte handling

Raw source bytes are runner-temporary only.

They must never be:

- committed;
- printed in logs;
- included in summaries;
- uploaded as GitHub Actions artifacts;
- copied into `.ai/**` evidence files;
- persisted beyond the protected job.

Cleanup is mandatory on success and failure.

## Required tests

At minimum test:

- exact 15-identity allowlist and rejection of any 16th/non-authorized identity;
- explicit `draft_picks.csv` rejection;
- accepted WR-067 machine-lock and corpus digest binding;
- all 49 conformance vectors reproduce exactly;
- fatal vectors emit no partial schema/hash/row count;
- wrong expected digest/size fails before parsing;
- non-exact B2 version name rejected;
- non-upload B2 version rejected;
- provider mutation operations absent;
- mutation-capable custody helpers are not imported/invoked;
- consumer environment contains no provider credentials/authority;
- deliberate consumer secret injection fails closed;
- consumer independently re-hashes/re-sizes all local inputs;
- raw artifact upload path absent;
- cleanup on success/failure;
- existing WR-056 custody and WR-063 retained-read regressions remain green.

## Protected live proof

Before requesting audit, produce one successful protected live proof covering all 15 authoritative source identities.

The proof must show, privacy-safely:

- exact expected input count = 15;
- dedicated B2 authorization boundary PASS;
- bounded exact-key/version-aware B2 retrieval PASS for all 15;
- authoritative B2 digest/size PASS for all 15;
- exact-key R2 retrieval PASS for all 15;
- authoritative R2 digest/size PASS for all 15;
- B2/R2 equality PASS for all 15;
- provider mutation count = 0;
- provider access complete before consumer start;
- consumer provider-credential presence = false;
- consumer re-hash/re-size PASS for all 15;
- accepted contract/corpus binding PASS;
- 49/49 conformance reproduction PASS;
- retained-source parser derivation PASS or exact fail-closed source/fatal evidence;
- cleanup PASS;
- raw-byte Actions artifact count = 0;
- no upstream source access;
- no `draft_picks.csv`;
- no 2026 regular-season outcome-table use;
- no target join;
- no model fitting/scoring/tuning/comparison/evaluation/prediction;
- no ranking/production change;
- no Phase-6 work.

If any one of the 15 retained files is fatal under the accepted contract or cannot be verified/retrieved exactly, fail closed and return that exact blocker. Do not weaken or special-case the accepted contract to force success.

## Authorized write scope

Worker may write only:

- `.ai/work_helper/**`;
- `.github/workflows/wr069-retained-safe-consumer-parser.yml`;
- `scripts/custody/wr069_retained_safe_consumer.py`;
- `scripts/custody/test_wr069_retained_safe_consumer.py`;
- `scripts/validate-release-candidate.mjs` only if strictly required to admit the new protected workflow without weakening existing guards;
- `.github/workflows/ci.yml` only if a focused regression hook is strictly required.

Do not modify:

- `.ai/research/**`;
- `.ai/shared/**`;
- `.ai/manager/**`;
- `.ai/auditor/**`;
- accepted `.github/workflows/wr063-retained-version-read.yml`;
- accepted `scripts/custody/read_retained_objects.py`;
- accepted `scripts/custody/test_read_retained_objects.py`;
- mutation-capable custody helpers/workflows;
- product/model/ranking source files.

If the exact implementation cannot fit within this write scope, fail closed and return to Manager rather than broadening it yourself.

## Publication contract

Publish one immutable WR-069 PR/head containing the reviewed implementation, focused tests, privacy-safe derived-evidence package, and concise `.ai/work_helper/HANDOFF.md`.

Return:

- exact PR/head;
- exact files changed;
- focused tests and CI results;
- protected live run/job IDs;
- verified input count;
- B2/R2 verification summary for all 15;
- provider mutation count;
- consumer provider-credential presence result;
- 49-case conformance result;
- derived-evidence artifact path and SHA-256;
- row/schema/inventory derivation summary without raw rows;
- cleanup result;
- raw Actions artifact count;
- any fail-closed blocker or unresolved uncertainty;
- exact recommended Manager action.

Do not activate WR-070 yourself. Do not merge the implementation PR.

## Next gate

Manager freezes the exact successful WR-069 target and activates WR-070. PASS-family WR-070 is required before exact audited integration. Because WR-069 changes protected workflow/script infrastructure, mandatory canonical-main post-merge canary is required before WR-059 may resume.
