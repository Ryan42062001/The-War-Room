# WR-074 — Self-Hosted Heavy-CI Runner Pilot + Hardening

TASK ID: WR-074
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: CLOSED — AUDITED SELF-HOSTED PILOT ACCEPTED
DATE: 2026-09-19
DEPENDENCY: HARD — temporarily serialized behind WR-084 for write-scope collision safety
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
ASSIGNMENT MODE: WORKFLOW / CI TROUBLESHOOTING — DIAGNOSIS + REMEDIATION
TARGET BRANCH: `wr-074-self-hosted-heavy-ci-runner-pilot`
CURRENT CHECKPOINT: `7b4641499c50541abf523267eb4c0255813e8b6d`
AUDIT REQUIRED: YES — WR-075
PRODUCTION AUTHORIZATION: NONE

## Temporary Manager serialization — 2026-09-17

WR-074 is paused at immutable checkpoint `7b4641499c50541abf523267eb4c0255813e8b6d` while WR-083/WR-084 resolves the critical-path protected historical-scoring bridge. This is not a defect in WR-074.

Reason: WR-074 and WR-083 are both Work Helper lanes and may require `.ai/work_helper/**` plus exact `scripts/validate-release-candidate.mjs`. Workflow V3.3 requires serialization or narrower non-overlapping write scopes. Manager chooses temporary serialization rather than weakening either lane.

Do not advance WR-074 while WR-083 is runnable or WR-084 is pending. After WR-084 disposition and bridge integration/canary, Manager may reactivate WR-074 from this preserved checkpoint and reconcile canonical-main advancement.

## Objective

Evaluate and harden the user-operated self-hosted GitHub Actions runner for heavyweight War Room CI without weakening repository security, Governance independence, custody boundaries, or the Returning-Player v2 WR-072/077 chronology.

This repository is public. Treat arbitrary fork pull-request code as untrusted and never route it onto the self-hosted machine.

## Current observed checkpoint

Live branch head observed by Manager: `7b4641499c50541abf523267eb4c0255813e8b6d` (`WR-074: reconcile main and authorize pilot workflow in release guard`). The branch contains the dedicated pilot workflow `.github/workflows/wr074-self-hosted-heavy-ci-pilot.yml`, helper `scripts/ci/wr074-pilot.mjs`, and the narrowly authorized release-guard recognition change.

Historical pilot evidence remains:

- pilot run `35019774970`;
- trusted-ref-gate job `104552278012` — SUCCESS;
- self-hosted-heavy-parity job `104552310519` — historically QUEUED for no available matching dedicated runner;
- hosted reference run `35019774866` — classify/Governance SUCCESS, heavy test historically failed at release validation before the exact validator authorization.

Work Helper must refresh live Actions evidence from the current branch rather than assuming those historical statuses remain current.

## Required security architecture

Preserve all of the following:

1. classify and Governance remain GitHub-hosted;
2. custody/protected-proof/credential-bearing workflows remain GitHub-hosted unless a future separately approved task says otherwise;
3. self-hosted receives no B2/R2/provider credentials or retained raw source bytes;
4. arbitrary fork PR code cannot execute on the self-hosted runner;
5. do not use `pull_request_target` to execute untrusted PR-head code;
6. least-privilege permissions and non-persistent checkout credentials where compatible;
7. no sensitive machine identity, credentials or unrelated local files in logs/artifacts;
8. dedicated routing remains `[self-hosted, war-room-heavy-ci]` and must not be weakened.

## Authorized read scope

Broad read authority remains for workflows, Actions evidence, package/test scripts, workflow helpers, runner-related evidence, Manager/shared state and official GitHub runner/security documentation. WR-072 may be read only to avoid interference.

## Authorized write scope — narrowed for collision safety

Work Helper may write only:

- `.ai/work_helper/**`;
- exact `.github/workflows/wr074-self-hosted-heavy-ci-pilot.yml` for this pilot;
- `scripts/ci/**` only for dedicated pilot/preflight/cleanup/benchmark helpers;
- exact `scripts/validate-release-candidate.mjs` only to recognize the Manager-approved WR-074 pilot workflow while preserving fail-closed release validation.

The former broad `.github/workflows/**` allowance is intentionally narrowed. WR-074 has no authority to edit `.github/workflows/ci.yml` or unrelated workflows; Workflow V3.3 candidate WR-078 independently owns canonical CI changes.

Do not modify `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, `.ai/research/**`, `src/**`, `public/**`, production ranking/model/data logic, or `scripts/custody/**`.

## Required completion evidence

Before publication establish:

- clean hosted reference CI including release validation;
- successful execution on the intended dedicated self-hosted runner;
- repeated self-hosted runs sufficient to expose stale-workspace/environment dependence;
- exact runner labels/security trigger/permissions;
- cleanup/preflight behavior;
- heavy-test parity and benchmark versus hosted reference;
- no custody/provider secrets or sensitive leakage;
- hosted fallback/reference remains available;
- exact changed-file scope and remaining operational risks.

If the dedicated runner is offline or missing `war-room-heavy-ci`, fail closed and return the exact smallest user action rather than weakening routing.

## Publication

Publish one immutable Work Helper target on `wr-074-self-hosted-heavy-ci-runner-pilot`, open a PR to `main`, and do not merge it or activate WR-075. Return exact head, changed files, hosted/self-hosted run/job evidence, repeat-run results, parity/benchmark, cleanup/security evidence and remaining user action.


## Serialization update after WR-084 — 2026-09-18

WR-074 remains blocked and its preserved checkpoint remains unchanged. Serialization now extends through WR-083 bounded remediation and fresh WR-089 re-audit because WR-083 may still touch `.ai/work_helper/**` and `scripts/validate-release-candidate.mjs`.

Resume WR-074 only after protected-bridge PASS-family disposition, exact audited integration, and required protected canonical-main canary.


## Post-WR-089 integration serialization — 2026-09-18

The audited protected bridge is integrated, but WR-074 remains temporarily serialized through WR-090 until the required canonical-main credentialed NO-SCORING canary succeeds and Manager completes the bridge disposition.

After WR-090 SUCCESS, Manager may reactivate WR-074 from preserved checkpoint `7b4641499c50541abf523267eb4c0255813e8b6d`.


## Serialization cleared after WR-090 — 2026-09-18

WR-090 canonical-main protected canary succeeded. The WR-083 protected-bridge serialization dependency is cleared.

WR-074 remains preserved at checkpoint `7b4641499c50541abf523267eb4c0255813e8b6d` and is now PLANNED rather than BLOCKED. Manager is not spawning this infrastructure lane in the same transition because Workflow V3.4 requires a worker-spawn value check and the immediate critical path is WR-081 consumer preparation.

WR-074 may be reactivated later without waiting on WR-081, subject to fresh collision/branch refresh.


## Manager reactivation — 2026-09-19

WR-101/WR-102 are closed and the prior Work Helper serialization is fully cleared.

Canonical main at reactivation:
`94cb4826fc477d5bf592a37585f8cbea7574daea`

Live WR-074 branch remains exactly:
`7b4641499c50541abf523267eb4c0255813e8b6d`

Manager independently compared that preserved branch with current main:
- branch ahead by 2 commits;
- branch behind by 348 commits;
- branch-unique file delta is exactly:
  - `.github/workflows/wr074-self-hosted-heavy-ci-pilot.yml`;
  - `scripts/ci/wr074-pilot.mjs`;
  - `scripts/validate-release-candidate.mjs`.

Before further implementation or live pilot execution, reconcile those exact task-owned changes onto current canonical main without importing stale control-plane/research state.

Preserve all current Workflow V3.5 rules and all accepted custody/protected-execution changes now present on main.

Execution mode is `STANDARD_CHAT_HIGH`. Work mode is not required.

Required next evidence remains:
- clean hosted reference CI including release validation;
- intended dedicated self-hosted runner executes successfully;
- repeat self-hosted runs sufficient to expose stale-workspace/environment dependence;
- exact runner labels, trusted trigger and permissions;
- cleanup/preflight behavior;
- parity/benchmark against hosted reference;
- no custody/provider secrets or sensitive leakage;
- hosted fallback/reference remains available;
- exact final changed-file scope and operational risks.

If the dedicated runner is unavailable, offline, or lacks `war-room-heavy-ci`, fail closed and return the smallest user action rather than changing `runs-on` or weakening security.

Do not merge. Do not activate WR-075. Return one immutable final WR-074 target to Manager for freeze and fresh independent audit.


## Manager exact final freeze — 2026-09-19

Manager independently accepts the completed Work Helper packaging as the immutable WR-074 audit target.

Frozen target:
- PR #307;
- branch `wr-074-self-hosted-heavy-ci-runner-pilot`;
- exact final SHA `75fcd3756956b2943f18aff03115f9783a16d0aa`;
- immutable implementation SHA `c2e511da5d3767cbc0688de7e95236135a6975b2`;
- canonical base `cb544da20c7b82ded5552d425d69b8a47c880f30`.

Verified final changed paths are exactly:
- `.ai/work_helper/HANDOFF.md`;
- `.ai/work_helper/WR074_LINUX_RUNNER_ACTIVATION.md`;
- `.ai/work_helper/WR074_SELF_HOSTED_HEAVY_CI_REPORT.md`;
- `.github/workflows/wr074-self-hosted-heavy-ci-pilot.yml`;
- `scripts/ci/wr074-pilot.mjs`;
- `scripts/validate-release-candidate.mjs`.

The implementation delta is 28 commits ahead / 0 behind canonical base. The final three commits after implementation touch only the three Work Helper evidence paths. Temporary Windows diagnostic/shim files are absent from the final target.

Repeated parity evidence independently verified:
- `35460866285`: self `105944382151` SUCCESS; hosted `105944382105` SUCCESS; trust `105944368659` SUCCESS;
- `35461197805`: self `105945274820` SUCCESS; hosted `105945274919` SUCCESS; trust `105945263537` SUCCESS.

Both self-hosted runs independently show clean preflight, provider/custody authority absent, complete heavy-suite parity, resilience PASS, and clean post-run cleanup.

Final-head validation:
- pilot `35461615030` SUCCESS;
- War Room CI `35461622646` SUCCESS;
- classify `105946400743` SUCCESS;
- governance `105946432859` SUCCESS;
- full test `105946457088` SUCCESS.

Security boundary remains:
- exact push-only WR-074 branch trigger;
- GitHub-hosted trust gate;
- exact repo/ref/actor checks;
- exact `[self-hosted, war-room-heavy-ci]` routing;
- `contents: read`;
- non-persistent checkout credentials;
- no PR/`pull_request_target` path;
- no provider/custody credentials or retained-source retrieval on self-hosted;
- canonical hosted CI and credential-bearing custody/protected workflows remain GitHub-hosted.

Benchmark evidence shows no speed advantage; functional/security viability is the only claimed result.

Do not follow later movement of PR #307 or its branch for audit purposes. WR-075 must audit exactly `75fcd3756956b2943f18aff03115f9783a16d0aa`.

WR-074 is not merged and no self-hosted configuration is canonical infrastructure authority from this freeze alone.


## Final audited integration disposition — 2026-09-19

WR-075 returned `PASS` with no CRITICAL/HIGH/MEDIUM/LOW findings only for exact WR-074 target:

`75fcd3756956b2943f18aff03115f9783a16d0aa`

Accepted audit evidence:
- Auditor PR #310;
- Auditor head `0968f3e84aa852d6fa528e3ca9a9ca3383cc6362`;
- Auditor CI `35463331283` SUCCESS;
- audit evidence canonical merge `2d860ab27fd0fe02e3614311a6f202d7a91439df`;
- post-audit-evidence canonical CI `35475324576` SUCCESS.

Only the exact audited WR-074 target was integrated:
- PR #307 exact head `75fcd3756956b2943f18aff03115f9783a16d0aa`;
- canonical integration merge `3656d355351113bb4692759e4410e607b60967ea`.

Required canonical-main full validation:
- War Room CI `35475382820` SUCCESS;
- classify `105983625300` SUCCESS;
- governance `105983645613` SUCCESS;
- full test `105983665291` SUCCESS.

WR-074 is CLOSED.

Accepted scope:
- dedicated `[self-hosted, war-room-heavy-ci]` Linux/WSL2 heavy-CI pilot;
- exact branch-push trusted gate;
- hosted Governance/custody/protected separation;
- least-privilege contents-read checkout with non-persistent credentials;
- repeated clean preflight/cleanup and heavy-test parity;
- hosted reference/fallback remains available.

Explicit non-authority:
- no broader triggers;
- no generic self-hosted routing;
- no arbitrary PR execution;
- no credential-bearing or custody/protected workloads on self-hosted;
- no performance-superiority claim;
- no automatic expansion to unrelated CI workloads.
