# WR-062 — Independent Audit of GET-Only Retained-Object Read Path

TASK ID: WR-062
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-14
DEPENDENCY: HARD — WR-061 must publish one immutable implementation/live-proof target
EXECUTION MODE: WORK_MODE_PREFERRED
TARGET BRANCH: `wr-062-retained-object-read-path-audit`
TARGET PR/HEAD: TO BE SET AT ACTIVATION
PRODUCTION AUTHORIZATION: NONE

## Objective

Independently determine whether WR-061 establishes a genuinely bounded provider-read-only path sufficient for WR-059 evidence reconstruction without weakening existing custody/credential/governance protections.

## Activation condition

Remain BLOCKED until Manager freezes the exact WR-061 PR/head plus its protected live-proof run/job evidence. Auditor must not audit a moving implementation target.

## Required independent audit scope

Verify at minimum:

1. exact four-object allowlist matches WR-059 season/asset/digest/size/custody-key identities and cannot be widened by runtime input;
2. unknown/nonmatching keys, digests, sizes, or identities fail before provider access;
3. provider secret-bearing code is GET/download-only and contains no PUT/upload/copy/multipart/delete/metadata/tag/retention/Legal-Hold/Bucket-Lock mutation path;
4. existing mutation-capable custody helpers/workflows are neither modified nor imported/invoked by the new read path;
5. provider credentials use the existing secret set, are not exposed, are not global/job-scoped unnecessarily, and are unavailable to the later WR-059 consumer/parser step;
6. trusted retrieval code is pinned to the reviewed implementation rather than an arbitrary R&D target commit;
7. all four B2 and R2 live reads independently reproduce the pinned SHA-256 and byte size and match one another;
8. raw bytes are runner-temporary only, cleaned on success/failure, absent from commits/logs/Actions artifacts, and cannot be uploaded by the workflow path;
9. no source-byte refresh from GitHub/nflverse/other upstream source occurs;
10. the consumer boundary lets WR-059 process verified bytes after secrets are removed without giving R&D code provider-mutation authority;
11. focused fail-closed regression tests cover allowlist rejection, mutation absence, secret isolation, raw-artifact prohibition, cleanup, and exact digest/size verification;
12. release-candidate workflow allowlist, V3.2 governance, WR-056 trusted-custody regression, and unrelated production/research boundaries remain intact;
13. no 2026 outcome/model/scoring/ranking/production/Phase-6 work is introduced.

## Fail-closed audit rules

Return a blocking finding if the implementation can select arbitrary provider objects, execute mutation-capable provider operations, expose secrets to consumer code, publish raw bytes, bypass cleanup, use unreviewed branch code while secrets are present, or lacks independently reproducible live proof for all four exact objects.

Code-level assertions alone are insufficient for the live-provider requirements; audit the protected live run evidence independently.

## Write scope

Write only `.ai/auditor/**`.

## Publication contract

Publish a fresh WR-062 audit report, updated Auditor handoff, immutable audit branch/head, and Auditor-only PR.

Return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

PASS-family authorizes only Manager integration of the exact audited WR-061 head followed by the mandatory canonical-main post-merge canary. WR-059 may resume only after that integration/canary is accepted.
