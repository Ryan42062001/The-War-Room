# WR-071 — Fresh Independent Re-Audit of Remediated v2 Source Snapshot + Cohort Evidence

TASK ID: WR-071
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-15
DEPENDENCY: HARD — WR-059 must publish one new immutable remediation head resolving WR-060-AUD-01
EXECUTION MODE: WORK_MODE_PREFERRED
TARGET BRANCH: `wr-071-v2-source-snapshot-cohort-reaudit-2`
TARGET PR/HEAD: TO BE SET AT ACTIVATION
PRODUCTION AUTHORIZATION: NONE

## Objective

Freshly and independently re-audit the next Manager-frozen WR-059 target after bounded remediation of HIGH finding `WR-060-AUD-01`.

This successor audit must preserve WR-060's accepted positive evidence while independently verifying the corrected replacement-metadata provenance/fail-closed disposition and all regenerated canonical bindings.

## Activation condition

Remain BLOCKED until Manager independently verifies and freezes:

- one new immutable WR-059 PR #196 head;
- exact changed-file scope;
- new versioned source-snapshot ID and SHA-256;
- new versioned cohort ID/version and SHA-256;
- exact metadata-source admission/fail-closed totals;
- exact-head CI.

Do not audit the historical failed target `e871c861f8ba3c339af5b7a022892522b45b844f`.

## Required audit scope

At minimum independently verify:

1. WR-060-AUD-01 is actually remediated without weakening WR-039;
2. if Path A was used, exact provider-issued release ID and full provider-update timestamp are independently reproducible and bound to asset `563580371`;
3. if Path B was used, replacement `players.csv` is consistently failed closed/unavailable and all snapshot admission/failure/availability fields agree;
4. no timestamp precision was inferred and no replacement/current asset or provider was substituted;
5. no retained raw bytes were reacquired solely for remediation;
6. all unchanged historical custody identities remain exact;
7. regenerated source-snapshot canonical bytes/hash reproduce;
8. regenerated cohort artifact binds exactly to the new source-snapshot ID/hash;
9. the 5,176-key historical cohort, ordering, duplicates=0, and exact lineage remain unchanged unless WR-059 provides independently auditable justification otherwise;
10. `draft_picks.csv` remains excluded;
11. current metadata remains irrelevant to historical cohort membership;
12. no provider mutation, upstream refresh, 2026 outcome-table use, targets, model/scoring/tuning/comparison/evaluation/predictions, ranking/production changes, or Phase-6 work occurred.

## Fail-closed rule

Return a blocking finding if exact release provenance remains missing for an admitted metadata source, if fail-closed disposition is inconsistent, if versioned canonical bindings are not reproducible, or if the remediation weakens accepted custody/cohort boundaries.

## Write scope

Write only `.ai/auditor/**`.

Do not modify WR-059, `.ai/research/**`, Manager/shared state, Work Helper evidence, provider credentials, production/model/ranking files, or historical checkpoints.

## Publication contract

Publish:

- fresh task-specific WR-071 audit report under `.ai/auditor/**`;
- updated `.ai/auditor/HANDOFF.md`;
- immutable audit branch/head;
- Auditor-only PR.

Return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

A PASS-family verdict returns to Manager only. Auditor does not merge WR-059 or activate model/scoring/Phase-6 work.
