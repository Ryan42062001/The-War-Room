# WR-136 — Terminal Next-Turn Production State Repair

TASK ID: WR-136
ROLE: Implementation Engineer / Builder — DISTINCT WR-136 production repair lane; NO WR-135 Builder writes
STATUS: ASSIGNED
DATE: 2026-09-21
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — narrow separate production remediation, HARD upstream prerequisite for blocked WR-135
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-136-terminal-next-turn-production-remediation`
ASSIGNED BRANCH: `wr-136-terminal-next-turn-production-remediation`
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: `413be06069e395a87a2e0a0849b2f893d0b81451`
ACTIVATION: Manager must merge WR-D053 control-plane PR after exact-head Governance SUCCESS, verify genuine post-merge canonical-main push Governance SUCCESS, create this new Builder branch from EXACT THEN-CURRENT canonical main, verify 0 ahead/0 behind; the pre-activation checkpoint is NOT permission to bootstrap a stale branch.
AUDIT REQUIRED: YES — separate FRESH Independent Auditor / QA task WR-137 on immutable frozen WR-136 target, assigned ONLY after actual successful implementation and exact-head FULL CI.
POST-MERGE CANARY REQUIRED: YES — genuine canonical-main FULL War Room CI SUCCESS is mandatory before WR-136 closure or WR-135 resumption.

## Independently confirmed production defect

Manager inspected current unmodified `js/war-room-draft-state.js:getDraftAssistantState()`, the strict read-only WR-135 PR #379 reproducer/evidence at frozen blocked branch head `62fe08807f5db0105f078f7ccb80fd3bdb7ad59a`, and actual FULL CI test-job log #106579993743 from failing candidate `69b3e6d9e568292dc1f005f9639323bea3c28b09`.

At local actual-app Full-PPR snake 2-team×5-round×slot-2, 10/10 accepted numbered picks, expected and actual SHA-256 ledger equal `adcc692a890218e8c77a17d688f1b868943469b62c7667f99737ecb297bb4ddb`, authoritative completion true and 5 own picks, but `getDraftAssistantState().myNextPick` wrongly equals consumed final own pick 10 instead of null; currentPick is capped to totalPicks then next-owned-pick search uses `>=`. `js/war-room-command-bar.js` consumes null `myNextPick` as the complete-mode signal. Earlier 5/10 intermediate reload and 9/10 nonterminal checks passed. Original Builder CI #35675133071 FAILED; current documentary-head CI #35675575501 also FAILED. This is a production terminal-state defect, not a test-only or provider failure. WR-135 strict oracle is not authorized to change.

## EXACT authorized writes — FOUR PATHS ONLY

1. `js/war-room-draft-state.js` — the SMALLEST needed change exclusively within `getDraftAssistantState()` to ensure a fully completed draft cannot reselect an already-consumed final own pick or show on-clock/turn countdown. Preserve all established nonterminal current-pick, snake-pick and return-shape semantics; do not reshape unrelated draft state or edit other functions.
2. `scripts/test-browser.mjs` — add focused actual-current-app browser regression on terminal own-final-pick and nonterminal progression; reuse current browser harness and real source, no duplicated fake production function. Include 2×5 slot2 9/10 (next 10, onClock when appropriate) and 10/10 terminal (null myNextPick, null picksUntilMyTurn, onClock false, completed command-bar mode) plus saved terminal/reload and reopening after undo if supported. Also cover completed final OTHER-owned pick where feasible (e.g. 2×5 slot1), normal on-clock/waiting state and configurations with ordinary last pick before the draft terminal. Do not weaken any existing tests.
3. `.ai/builder/WR136_TERMINAL_TURN_REPAIR_EVIDENCE.md` — actual source diff, failed-before/fixed-after reproducer, exact executed test and CI receipts, security/boundary restrictions.
4. `.ai/builder/WR136_TERMINAL_TURN_REPAIR_HANDOFF.md` — compact separate-task Builder handoff; never touch shared `.ai/builder/HANDOFF.md` because it belongs to frozen WR-135 custody.

NO OTHER WRITES. In particular do NOT modify `scripts/test-draft-invariants.mjs`, WR-135 PR #379/branch/head, the accepted WR-133 harness, other production/Companion files, package.json, existing dataset/fixture/ranking, recommendation/scoring policy, workflow/runner, permissions/credentials, provider/source or deployment/release surfaces. NO WR-135 Builder action is authorized under WR-136.

## Narrow correctness contract

The patch must return `myNextPick:null`, `picksUntilMyTurn:null` and `onClock:false` upon complete numbered draft, including when the user's own pick was the final pick; command bar must signal completion. The same previously completed pick cannot be presented as a future turn. Preserve existing `currentPick` capped boundary if used by current consumers (do not globally advance it past `totalPicks` without independent compatibility evidence), preserve `myPicks` sequence and all normal preterminal turn semantics. Prevent regression at one-pick-before-terminal, non-own final pick, slot extremes, and saved/reloaded terminal state. Do not use a broad blanket complete flag that suppresses a valid incomplete draft or introduces silent source/provider changes. If focused tests show the root cause requires touching an unauthorized surface, STOP with evidence and ask Manager to rescope, not broaden the patch.

Do not use WR-135's failing FULL CI as a remediation PASS. Do not run/modify the unmerged WR-135 strict harness from the WR-136 branch or carry it into WR-136: it is a separately held blocked target. Build an independent focused real-browser regression on the canonical app, with exact input/actual output oracle and no provider contact.

## Validation and audit gates

Run and record actual `node --check js/war-room-draft-state.js`, `node --check scripts/test-browser.mjs`, `npm run test:browser`, `npm test` (which must include existing draft-invariants baseline, Companion 167/167, WR-118, WR-133), and genuine exact-final-head FULL War Room CI. Record immutable Builder PR/head, original base, cumulative EXACT FOUR PATHS, CI run/jobs and real browser result. Any failure remains FAILURE; do not relax existing tests or silently retry until green. Freeze one final target, STOP all Builder writes, leave PR OPEN / UNMERGED.

Manager separately verifies custody, exact-head tests and scope before assigning FRESH WR-137 Independent Auditor / QA on that immutable target, with a separate two-file Auditor report/handoff PR and exact-Auditor-head Governance. Only Manager-accepted independent PASS-family on unchanged WR-136 target can permit later guarded integration. After any WR-136 merge, require genuine canonical-main push FULL CI SUCCESS before WR-136 closure. Only AFTER THAT Manager may separately authorize WR-135 resumption against repaired canonical main; the old blocked test branch must not be silently rewritten or merged.

## Explicit non-authorizations

No live ESPN/provider contact, structured Direct promotion (LIVE_DIRECT_UNVERIFIED), new player rankings/data, 2027/A4 source refresh, Track B admission, draft scoring/recommendation policy changes, deployment/rollback, release or draft-ready declaration. This repair closes only the observed terminal-turn production-state defect; it is not supported-envelope validation or A6 release certification.
