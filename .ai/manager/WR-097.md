# WR-097 — Returning-Player v2.1 Protected Consumer + Execution Bridge

TASK ID: WR-097
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: AUDIT_READY
DATE: 2026-09-18
DEPENDENCY: HARD — WR-096 PASS and Manager acceptance of exact WR-095 protocol
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
ASSIGNMENT MODE: CROSS-FUNCTIONAL PROTECTED-EXECUTION IMPLEMENTATION
TARGET BRANCH: `wr-097-v21-protected-execution-bridge`
AUDIT REQUIRED: YES — fresh WR-098 after Manager freeze
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES
POST-MERGE CANARY REQUIRED: YES
PRODUCTION AUTHORIZATION: NONE
SCORING AUTHORIZATION: NONE
TARGET-OUTCOME AUTHORIZATION: NONE

## Accepted protocol authority

Manager accepts the exact WR-095 protocol after WR-096 independent PASS with no findings.

Authority:
- frozen WR-095 target `738296ad38282fc91738203e7e1ced888ba862ed`;
- audited protocol candidate `returning-player-v2.1-model-protocol-candidate/1.0.0-wr095`;
- machine SHA-256 `5c86dacac044538422ca24fbeb13eaee3161050f917acc40f0dbdc1ca6547a39`;
- source snapshot `wr-returning-player-v2-source-snapshot/1.2.0-wr059` / `6af88adaea478351a2b9c4884ca248dfed9527cb97f05e3648bd82c5fe0b3cea`;
- cohort `returning-player-v2-cohort/1.2.0-wr059` / `f62075ec3c13784dea4568fa69aae8a84d39ca70f074ca769132f1f143f2c3d4`;
- exactly 14 admitted annual Player Summary Stats source identities;
- zero Players metadata / draft-capital predictors;
- validation chronology 2022–2023;
- confirmation chronology 2024–2025 only after full validation PASS.

WR-096 audit evidence:
- audit PR #272;
- immutable Auditor head `3a779f6997c3bc57ec63f4fa4fa8084a609d5441`;
- exact-head CI `35415672844` SUCCESS;
- verdict `PASS`, no findings.

The exact audited five WR-095 blobs were integrated byte-for-byte through PR #273 as canonical-main merge `daa5e686cbc4fe2ca379baf696bf0335d05ad19d`.

## Objective

Implement the smallest independently auditable protected execution path capable of later executing the accepted v2.1 protocol, while performing NO REAL v2.1 scoring in WR-097.

This task owns the cross-layer implementation needed to make the protocol executable:
1. an exact hash-bindable v2.1 protected scoring consumer;
2. a dedicated v2.1 protected workflow/bridge;
3. synthetic/conformance tests proving protocol semantics and chronology;
4. a protected NO-SCORING readiness path proving custody, credential isolation, cleanup, publication barriers and V3.5 authority handling.

Do not execute 2022–2025 validation or confirmation scoring.

## Preserve accepted v2.1 semantics exactly

Consumer implementation must encode, without reinterpretation:

- exact WR-072 ordered 28-feature schema;
- separate QB/RB/WR/TE models;
- same-position `StandardScaler`;
- elementwise standardized feature clamp `[-6,+6]` in both fit and predict paths;
- Ridge `alpha=100`, solver/config exactly as machine protocol;
- primary baseline `prev1_ppr_pg`;
- model target `target_ppr_pg - prev1_ppr_pg`;
- training residual median;
- MAD `median(abs(residual - median))`;
- robust sigma `1.4826 * MAD`;
- residual adjustment bound `median ± 3 * robust_sigma`;
- final prediction `prev1_ppr_pg + bounded_adjustment`;
- invalid/zero residual scale fails the learned fold closed to the explicit persistence fallback;
- any prospective fallback prevents model-support because the result gates require `fallbacks=0`;
- no player-specific or position-specific exception;
- no hyperparameter search;
- unchanged validation/confirmation thresholds;
- tail diagnostics are evidence only, not additional hidden promotion gates;
- environment/version/seeds/evidence serialization exactly as accepted machine protocol.

If the human and machine accepted protocol appear contradictory during implementation, stop rather than choose a new interpretation.

## Protected chronology

Future real execution must be technically incapable of violating:

Validation:
1. build/train 2022 using only eligible prior outcomes through 2021;
2. lock immutable 2022 model/preprocessing/prediction evidence;
3. only then expose/join 2022 target;
4. after 2022 is locked/exposed/evaluated, 2022 may enter the 2023 rolling fit;
5. build/train 2023;
6. lock immutable 2023 evidence;
7. only then expose/join 2023 target;
8. evaluate the COMPLETE 2022–2023 validation gate;
9. if validation FAILS, terminal `VALIDATION_FAILED` and 2024–2025 remain unexposed.

Only if validation PASS:
10. execute 2024 under the same lock-before-target rule;
11. then 2025;
12. evaluate confirmation and bootstrap;
13. confirmation failure is terminal and does not grant downstream authority.

WR-097 itself must not perform these real outcome-exposure steps.

## V3.5 authority model

Use canonical Workflow V3.5 semantics.

The future scoring workflow must:
- accept only minimal dispatch mode input;
- obtain exact branch/head/consumer path/consumer SHA-256 from canonical Manager `future_execution_authority`;
- recompute and verify canonical authority identity;
- reject branch/head/path/digest substitution;
- recheck live remote head before protected execution and again before publication;
- consume authority exactly once;
- publish the canonical authority-consumption receipt and terminal-result separation required by V3.5;
- preserve machine-owned replay history;
- reject reused/consumed authority;
- fail closed if live workflow-run or repository evidence cannot be independently verified.

Do not create a real future execution authority in WR-097.

## Custody / provider boundary

Reuse accepted WR-063/065/069/083 custody principles rather than inventing a weaker path.

Required:
- retrieve only exact accepted retained objects by immutable identity;
- verify B2/R2 SHA-256 and size agreement before consumer access;
- raw retained bytes confined to `RUNNER_TEMP`;
- provider credentials only in retrieval/provider steps;
- consumer process starts with provider credential variables absent;
- no raw retained bytes committed, uploaded as Actions artifacts, logged, or retained after success/failure;
- provider storage remains read-only;
- cleanup proven on success and deliberate failure;
- no source reacquisition/refresh/substitution;
- no `draft_picks.csv`;
- zero Players metadata admission.

A pre-audit/live readiness proof may retrieve/re-hash accepted retained objects only in a NO-SCORING mode that does NOT expose target values to the consumer, construct real v2.1 model features, fit Ridge, emit real predictions, compare real baselines, calculate real validation/confirmation gates, or inspect 2022–2025 results.

If that cannot be guaranteed, do not run the credentialed readiness proof before audit; return the smallest blocker.

## Publication contract

Future real scoring publication must be restricted to the accepted v2.1 evidence families:

- RETURNING_PLAYER_V21_KEY_MANIFEST
- RETURNING_PLAYER_V21_FEATURE_SURFACE
- RETURNING_PLAYER_V21_PREPROCESSING_STATES
- RETURNING_PLAYER_V21_MODEL_STATES
- RETURNING_PLAYER_V21_PREDICTIONS_PRE_OUTCOME
- RETURNING_PLAYER_V21_EVALUATIONS
- RETURNING_PLAYER_V21_STAGE_GATES
- RETURNING_PLAYER_V21_ENVIRONMENT_LOCK
- RETURNING_PLAYER_V21_EXECUTION_CHRONOLOGY
- RETURNING_PLAYER_V21_RESULT_MANIFEST
- V3.5 terminal-result and authority-consumption receipt artifacts required by canonical workflow

Reject:
- raw retained-source bytes;
- arbitrary paths;
- source/provider manifests containing secrets;
- unreviewed consumer outputs;
- any later-stage artifact when the prior stage failed;
- confirmation artifacts when validation was not fully PASS.

No future result may write directly to `main`.

## Authorized write scope

Write only:

- `.ai/work_helper/**`;
- exact `.ai/research/WR097_V21_PROTECTED_SCORING_CONSUMER.py`;
- exact `.ai/research/WR097_V21_PROTECTED_SCORING_CONSUMER_TEST.py`;
- exact `.github/workflows/wr097-v21-protected-scoring-bridge.yml`;
- exact `scripts/custody/wr097_v21_protected_scoring.py`;
- exact `scripts/custody/test_wr097_v21_protected_scoring.py`;
- exact `scripts/validate-release-candidate.mjs` only if needed to recognize the new Manager-approved protected workflow without weakening any existing release guard.

Do NOT modify:
- `.ai/shared/**`;
- `.ai/manager/**`;
- `.ai/auditor/**`;
- accepted WR-095 protocol/failure-analysis artifacts;
- accepted WR-083 workflow/script/test;
- `src/**`;
- `public/**`;
- production rankings/recommendations;
- source/cohort/custody authority artifacts.

## Required synthetic/conformance tests

At minimum prove:

- machine protocol SHA binding;
- exact ordered 28-feature schema;
- StandardScaler then z-clamp order;
- training/prediction clamp symmetry;
- Ridge alpha/config lock;
- residual-target construction;
- exact MAD / 1.4826 robust-sigma calculation;
- residual bound and final prediction arithmetic;
- zero/invalid MAD fail-closed fallback;
- fallback count/result-gate interaction;
- no player/WR special case;
- 2022 prediction lock before 2022 target exposure;
- 2022 can enter 2023 training only after its lock/exposure;
- 2023 target unavailable before 2023 lock;
- no 2024/2025 access before complete validation PASS;
- full validation aggregation before confirmation unlock;
- confirmation failure/pass stage handling;
- deterministic serialization/hashing;
- allowed publication family enforcement;
- raw-source publication rejection;
- consumer credential absence;
- cleanup on success and deliberate failure;
- branch/head race rejection;
- arbitrary fork / PR code cannot trigger credentialed execution;
- no `pull_request_target` execution of untrusted PR-head code;
- V3.5 canonical authority and one-time receipt/replay semantics.

Synthetic fixtures must not contain copied 2022–2025 real outcomes.

## Required completion evidence

Return one immutable WR-097 implementation target with:

- branch/head/PR;
- exact changed files;
- accepted protocol/source/cohort hashes;
- consumer SHA-256;
- bridge/script/test hashes;
- synthetic/conformance results;
- exact-head Full War Room CI;
- protected workflow preflight result;
- if safely possible, exact live NO-SCORING readiness run/jobs proving custody/credential isolation/cleanup/no artifacts;
- proof no real scoring or future outcome exposure occurred;
- explicit publication allowlist;
- exact remaining step needed before future scoring;
- compact Work Helper handoff.

## Forbidden

Do NOT:
- fit/score the accepted model on real retained data;
- expose/join/inspect 2022–2025 target outcomes;
- inspect 2026 regular-season outcomes;
- create `future_execution_authority`;
- consume an authority;
- publish real predictions/evaluations/gates;
- reacquire/refresh/substitute sources;
- mutate provider storage;
- change protocol constants/gates;
- alter production rankings/recommendations;
- perform season-total composition;
- begin Phase 6;
- merge your PR;
- self-audit.

## Next gate

`WR-097 implementation -> Manager exact freeze -> fresh WR-098 independent bridge/consumer audit -> exact audited integration -> canonical-main protected NO-SCORING canary -> separate Manager decision on future validation scoring task`.

No scoring authority exists before all those gates complete.


## Manager exact implementation freeze — 2026-09-18

Work Helper completed WR-097 and published exact target:

- PR #275
- branch `wr-097-v21-protected-execution-bridge`
- exact SHA `75c0fbcd518438a226a8c49e3e11951de3944638`

Manager independently verified:

- PR #275 is OPEN / unmerged / mergeable at that exact head;
- changed files are exactly the nine Manager-authorized WR-097 paths;
- exact-head Full War Room CI `35418107240` SUCCESS:
  - classify `105830548278` SUCCESS
  - governance `105830573914` SUCCESS
  - bootstrap reuse `105830574701` SKIPPED
  - full test `105830601522` SUCCESS
- exact-head WR-097 protected workflow `35418107206` SUCCESS:
  - preflight `105830548445` SUCCESS
  - trust/scoring/readiness jobs skipped on ordinary final-head event as designed;
- predecessor regressions WR-046 `35418107211`, WR-063 `35418107209`, WR-069 `35418107234`, and WR-083 `35418107218` all SUCCESS;
- successful credentialed NO-SCORING proof was run on implementation/proof SHA `123149f330338b02381fdabeb09f575b7a94c26c`:
  - protected run `35417205490` SUCCESS
  - preflight `105828043700` SUCCESS
  - trust gate `105828139958` SUCCESS
  - protected no-scoring readiness `105828156958` SUCCESS
  - future authorized scoring `105828157786` SKIPPED
  - same-head War Room CI `35417205509` SUCCESS
  - Actions artifacts: 0;
- compare proof SHA -> frozen final SHA changes only:
  - `.ai/work_helper/HANDOFF.md`
  - `.ai/work_helper/WR097_V21_PROTECTED_EXECUTION_BRIDGE.md`
  - `.ai/work_helper/WR097_V21_PROTECTED_READINESS_SUMMARY.json`
  and does not alter reviewed consumer, bridge, workflow, test, or release-validator bytes.

The protected readiness summary attests:
- exactly 14 accepted identities;
- B2 digest/size 14/14;
- R2 digest/size 14/14;
- B2/R2 equality 14/14;
- provider mutations 0;
- consumer provider credentials absent;
- retained rows parsed by consumer false;
- historical targets exposed false;
- 2022–2025 target outcomes exposed false;
- real model fit/predictions/baseline comparisons/gates false;
- cleanup PASS;
- raw Actions artifact count 0.

Known integration blocker to audit explicitly:
- canonical `scripts/workflow-manager-transition.mjs` currently accepts successful protected workflow identity only when the workflow name is exactly `WR-083 Protected Historical Scoring Bridge`;
- WR-097 correctly does not modify this Manager-controlled file;
- therefore real v2.1 authority consumption remains fail closed until a later separately audited Manager integration change recognizes the accepted WR-097 workflow.

This freeze authorizes only WR-098 audit. No scoring authority exists.
