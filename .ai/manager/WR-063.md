# WR-063 — Retained-Object Identity Reconciliation + Version-Aware Read Recovery

TASK ID: WR-063
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: ASSIGNED
DATE: 2026-09-14
DEPENDENCY: INDEPENDENT
EXECUTION MODE: WORK_MODE_HIGH_VALUE
TARGET BRANCH: `wr-063-retained-object-version-read-recovery`
ASSIGNMENT MODE: `DIAGNOSIS + REMEDIATION`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-064
POST-MERGE CANARY REQUIRED: YES
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Reactivation checkpoint

WR-063 PR #178 remains the active moving Work Helper target. Current checkpoint head before reactivation: `4a54161beeca5faed8bf267c977ac6dd3e4c13d7`.

Protected run `34901593729` / job `104168617065` authenticated successfully to Backblaze B2 and then failed closed before any version query because the existing shared custody credential lacked `listFiles`.

Manager rejected broadening that existing mutation-capable custody credential. The user has now confirmed that a separate dedicated B2 read-only application key was created and saved as the two WR-063 GitHub Actions repository secrets below. This user confirmation clears the provisioning blocker, but it is NOT provider-scope proof.

Work Helper must independently verify the provider-issued authorization boundary in the next protected run before any version listing or object download. If the actual boundary differs from this task, fail closed.

Dedicated WR-063 secrets:

- `WR_CUSTODY_B2_READ_KEY_ID`
- `WR_CUSTODY_B2_READ_APPLICATION_KEY`

The existing shared secrets `WR_CUSTODY_B2_KEY_ID` / `WR_CUSTODY_B2_APPLICATION_KEY` remain unchanged and MUST NOT be used by the WR-063 B2 read path after this reactivation.

## Required dedicated-key boundary

Before any B2 version query, provider-issued authorization evidence must prove:

- bucket: exactly `War-Room-Custody-Primary`;
- file-name prefix: exactly `custody/sha256/` or a strictly narrower Manager-compatible prefix that still covers all four authoritative keys;
- required capabilities include `listFiles` and `readFiles`;
- no provider-mutation capability is present;
- no write, delete, key-management, bucket-mutation, retention-mutation, Legal-Hold-mutation, governance-bypass, or equivalent mutation authority is present.

Read-only retention/legal-hold capabilities may be present if Backblaze includes them for the selected read-only mode and WR-063 needs them for privacy-safe verification.

If any required boundary check fails, stop before listing/downloading and return the exact provider-issued mismatch.

## Historical baseline — inspect, do not merge

WR-061 PR #176 is CLOSED UNMERGED at exact head `5aff59928c4090730959d3f45bbae1483d34fde6`.

Relevant WR-061 protected attempts:

- `34889550688` / `104128551088`
- `34889871310` / `104129607200`
- `34890179111` / `104130645764`

WR-063 prior protected attempt:

- implementation head `15a35b3626929090b374eff5fdf4da4d0dfd32ce`
- run/job `34901593729` / `104168617065`
- contract-preflight job `104168574295` — PASS
- B2 authorization — PASS
- capability gate — FAIL CLOSED because `listFiles` absent
- B2 version queries / downloads — 0 / 0
- R2 reads — 0
- provider mutations — 0
- cleanup — PASS
- raw Actions artifacts — 0

## Authoritative four-object identity set

The executed WR-042 manifest/result at commit/head `cc9005ae4bd9065cf80f1c184f31974904165c54` / `614445a20c2c15fbc3d8c107644a5244ddb52076` is authoritative.

| Season | Asset ID | SHA-256 | Bytes | Exact custody key |
|---|---:|---|---:|---|
| 2013 | 512983282 | `dbc7804c32c8dbf46120bfe4e724cdd926537509caa8a1bb96cd3b6e159b21f8` | 792070 | `custody/sha256/dbc7804c32c8dbf46120bfe4e724cdd926537509caa8a1bb96cd3b6e159b21f8/raw` |
| 2014 | 512985320 | `7046a0fd69b979d0649feb679a42f82f6e8e175d19587857d3e9371f09427cd6` | 816553 | `custody/sha256/7046a0fd69b979d0649feb679a42f82f6e8e175d19587857d3e9371f09427cd6/raw` |
| 2015 | 512984105 | `b977be5bc8cad6b02dfb755e78974b4486a505fa272f683add499b968878e3eb` | 815021 | `custody/sha256/b977be5bc8cad6b02dfb755e78974b4486a505fa272f683add499b968878e3eb/raw` |
| 2016 | 512985505 | `041473e5860ca6cfcba7a29e98b2ddfc3f01db0df2469d5b44e44135603e2424` | 819194 | `custody/sha256/041473e5860ca6cfcba7a29e98b2ddfc3f01db0df2469d5b44e44135603e2424/raw` |

No alternative digest/key may be substituted.

## Authorized B2 operations

Using only the dedicated WR-063 read-only secrets, Work Helper may perform only non-mutating provider operations needed to:

1. authorize the dedicated application key;
2. verify the provider-issued bucket/prefix/capability boundary above;
3. query file-version metadata with the FULL EXACT custody key as the prefix/name boundary for each of the four keys, using `b2_list_file_versions` or equivalent read-only functionality;
4. filter returned records to exact file-name equality before candidate selection;
5. inspect privacy-safe version metadata such as action (`upload`/`hide`), file/version ID, content length, upload time, and retained custom digest metadata if present;
6. obtain read-only version/file metadata when needed to prove identity/state; and
7. download an exact retained upload version by immutable file ID/version identity solely to recompute authoritative SHA-256 and byte size.

No broad bucket/prefix sweep is authorized. Hide/non-upload records must never be treated as source bytes. If no retained upload version exactly matching the authoritative key/digest/size can be proven, fail closed.

## Authorized R2 operations

R2 remains limited to exact-key `HeadObject`/`GetObject` for the four authoritative keys, plus existing read-only lock/config verification only if necessary.

No listing sweep or provider mutation is authorized.

## Security / architecture requirements

1. Provider-mutation operations must be absent by construction. Do not import or invoke existing mutation-capable custody helpers.
2. Runtime selection cannot widen the four authoritative identities.
3. Dedicated B2 read credentials are step-scoped only to trusted WR-063 retrieval/discovery code and absent before the later consumer step.
4. Existing shared mutation-capable B2 custody credentials must not be exposed to the WR-063 read path.
5. Secret-bearing code must be reviewed WR-063/default-branch code, never arbitrary R&D code.
6. Every downloaded candidate must be hashed/sized immediately; mismatches are deleted before consumer access.
7. B2 and R2 accepted bytes must both equal the authoritative SHA-256/size for each season and equal one another.
8. Raw bytes are runner-temporary only, cleaned on success/failure, never committed/logged, and never uploaded as Actions artifacts.
9. No upstream GitHub/nflverse/other source-byte reacquisition.
10. Privacy-safe reports may include non-secret provider version/file IDs required for independent verification but never credentials, auth headers, signed URLs, raw bytes, or secret values.

## Required protected live proof

Produce one protected live proof on the exact final WR-063 implementation head showing for all four authoritative objects:

- dedicated-key provider boundary verification PASS;
- exact-full-key-bounded B2 version discovery PASS;
- exact retained B2 upload-version read PASS with authoritative SHA-256/size;
- exact R2 read PASS with authoritative SHA-256/size;
- B2/R2 equality PASS;
- provider-issued evidence reconciling the 2013 by-name 404, or fail closed;
- zero provider mutation operations;
- shared mutation-capable B2 custody credentials absent from the WR-063 read path;
- consumer credentials absent;
- cleanup PASS on success and tested failure;
- zero raw-byte Actions artifacts.

## Required tests

Retain or extend the focused fail-closed coverage proving:

- authoritative four-row identity equality;
- historical non-authoritative WR-061 2014–2016 identities rejected;
- exact-full-key version-query boundary;
- non-exact version-name rejection;
- hide/non-upload records rejected as source bytes;
- immutable-version digest/size gate;
- R2 operation set limited to HEAD/GET;
- dedicated B2 capability boundary rejects mutation-capable credentials;
- shared B2 custody secrets are not consumed by WR-063;
- consumer secret isolation;
- mutation helper/operation absence;
- raw-byte artifact prohibition;
- cleanup on success and failure;
- release-candidate workflow guard, Workflow V3.2 Governance, and WR-056 trusted-custody regression remain green.

## Authorized write scope

Only:

- `.ai/work_helper/**`;
- `.github/workflows/wr063-retained-version-read.yml`;
- `scripts/custody/read_retained_versions.py`;
- `scripts/custody/test_read_retained_versions.py`;
- `scripts/validate-release-candidate.mjs` only as required for the approved WR-063 workflow;
- `.github/workflows/ci.yml` only for focused permanent WR-063 regression coverage if needed.

Do not modify existing mutation-capable custody workflows/scripts, `.ai/research/**`, `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, production code, rankings, models, or public assets.

## Publication / handoff

Continue PR #178 on the assigned branch. Do not merge it.

After successful proof, publish one immutable final WR-063 head and return:

- exact PR/head;
- files changed;
- test/CI run IDs;
- protected live run/job IDs;
- provider-issued dedicated-key bucket/prefix/capability result;
- exact B2 version-resolution result for each season;
- exact B2/R2 digest/size results for each season;
- provider evidence explaining the 2013 by-name 404;
- raw-artifact count;
- cleanup result;
- remaining uncertainty;
- `INDEPENDENT AUDIT REQUIRED` only if the complete four-object proof succeeds.

Do not activate WR-064 and do not merge WR-063.
