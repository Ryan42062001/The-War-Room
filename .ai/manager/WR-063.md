# WR-063 — Retained-Object Identity Reconciliation + Version-Aware Read Recovery

TASK ID: WR-063
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: ASSIGNED
DATE: 2026-09-14
DEPENDENCY: INDEPENDENT
EXECUTION MODE: WORK_MODE_HIGH_VALUE
TARGET BRANCH: `wr-063-retained-object-version-read-recovery`
ASSIGNMENT MODE: `DIAGNOSIS + REMEDIATION`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-064
POST-MERGE CANARY REQUIRED: YES
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Problem / blocker

WR-061 built a promising non-mutating read architecture but failed closed before audit.

Two causes must be resolved:

1. the WR-061 Manager task accidentally pinned non-authoritative SHA/key values for 2014–2016; and
2. the authoritative 2013 B2 content-addressed name returned HTTP 404 through a by-name native download even though historical WR-042 protected custody proved that exact byte identity was successfully retained.

The likely explanation may involve historical B2 version state (for example a retained upload version no longer being the currently visible by-name version), but this is a hypothesis only. WR-063 must determine the actual provider state with read-only evidence.

## Historical baseline — inspect, do not merge

WR-061 PR #176 is CLOSED UNMERGED at exact head:

`5aff59928c4090730959d3f45bbae1483d34fde6`

Its GET-only architecture, tests, logs, and failed protected runs may be inspected and selectively reimplemented. Do not reopen, merge, or silently advance that historical target.

Relevant protected attempts:

- `34889550688` / `104128551088`
- `34889871310` / `104129607200`
- `34890179111` / `104130645764`

## Authoritative four-object identity set

The executed WR-042 manifest/result at commit/head `cc9005ae4bd9065cf80f1c184f31974904165c54` / `614445a20c2c15fbc3d8c107644a5244ddb52076` is authoritative.

| Season | Asset ID | SHA-256 | Bytes | Exact custody key |
|---|---:|---|---:|---|
| 2013 | 512983282 | `dbc7804c32c8dbf46120bfe4e724cdd926537509caa8a1bb96cd3b6e159b21f8` | 792070 | `custody/sha256/dbc7804c32c8dbf46120bfe4e724cdd926537509caa8a1bb96cd3b6e159b21f8/raw` |
| 2014 | 512985320 | `7046a0fd69b979d0649feb679a42f82f6e8e175d19587857d3e9371f09427cd6` | 816553 | `custody/sha256/7046a0fd69b979d0649feb679a42f82f6e8e175d19587857d3e9371f09427cd6/raw` |
| 2015 | 512984105 | `b977be5bc8cad6b02dfb755e78974b4486a505fa272f683add499b968878e3eb` | 815021 | `custody/sha256/b977be5bc8cad6b02dfb755e78974b4486a505fa272f683add499b968878e3eb/raw` |
| 2016 | 512985505 | `041473e5860ca6cfcba7a29e98b2ddfc3f01db0df2469d5b44e44135603e2424` | 819194 | `custody/sha256/041473e5860ca6cfcba7a29e98b2ddfc3f01db0df2469d5b44e44135603e2424/raw` |

No alternative digest/key may be substituted merely because it is easier to retrieve.

## Authorized B2 diagnosis / read operations

Using the existing secret set and protected environment, Work Helper may use only provider operations that are non-mutating by provider semantics.

Authorized B2 operations are limited to the minimum needed to:

1. authorize the existing account/application key;
2. query file-version metadata with the **full exact custody key as the prefix/name boundary** for each of the four keys (for example `b2_list_file_versions` or an equivalent read-only provider API);
3. filter returned records to exact file-name equality before any download;
4. inspect privacy-safe version metadata such as action (`upload`/`hide`), file ID/version identity, content length, upload time, and retained custom digest metadata if present;
5. obtain read-only file information/retention/Legal-Hold metadata for a candidate exact version when needed to prove identity/state; and
6. download a candidate retained upload version by immutable file ID/version identity (for example `b2_download_file_by_id`) solely to recompute exact SHA-256 and byte size.

No B2 list request may use a broad bucket/prefix sweep beyond the exact full custody key under investigation.

If a by-name 404 is caused by historical version state, preserve privacy-safe evidence demonstrating that state. Do not assume a hide marker or any other cause without provider-issued evidence.

If no retained version exactly matching the authoritative key/digest/size can be proven, fail closed.

## Authorized R2 diagnosis / read operations

R2 access is limited to exact-key read operations only:

- exact `HeadObject` if needed for read-only metadata;
- exact `GetObject` for the four authoritative keys;
- existing lock-rule/config read only if needed to confirm that the accepted lock evidence remains applicable.

No list-bucket sweep, upload, overwrite, copy, delete, metadata/tag mutation, or lock mutation is authorized.

## Security / architecture requirements

1. Provider-mutation operations must be absent by construction. Do not import or invoke existing mutation-capable custody helpers.
2. Runtime selection cannot widen the four exact authoritative identities.
3. Existing provider secret set only; no new credentials or scope changes.
4. Secret-bearing code must be reviewed WR-063/default-branch code, never arbitrary R&D code.
5. Provider credentials must exist only in trusted retrieval/discovery steps and be absent before any later WR-059 consumer/parser executes.
6. Every downloaded candidate must be hashed/sized immediately; mismatches are deleted and never exposed to consumer code.
7. B2 and R2 accepted bytes must both equal the authoritative SHA-256/size for each season and equal one another.
8. Raw bytes are runner-temporary only, cleaned on success/failure, never committed/logged, and never uploaded as Actions artifacts.
9. No upstream source-byte download from GitHub/nflverse/other provider.
10. Privacy-safe reports may include non-secret provider version/file IDs when necessary for independent retained-version verification, but never credentials, auth headers, signed URLs, raw bytes, or secret values.
11. If actual provider capability/credential scope prevents exact-key version discovery or version-addressed read, fail closed and return the exact blocker. Do not widen credentials yourself.

## Required live proof

Before requesting audit, produce one protected live proof on the exact WR-063 implementation head showing for all four authoritative objects:

- exact-key B2 version discovery/selection is bounded and privacy-safe;
- exact retained B2 version read PASS with expected SHA-256/size;
- exact R2 read PASS with expected SHA-256/size;
- B2/R2 equality PASS;
- the 2013 by-name 404 cause is reconciled with provider-issued evidence, or the task fails closed;
- zero provider mutation operations;
- provider credentials absent from the consumer boundary;
- cleanup PASS on success and tested failure;
- zero raw-byte Actions artifacts.

The proof must use the authoritative values above, not WR-061's mistaken 2014–2016 values.

## Required tests

Add focused fail-closed regression coverage proving at minimum:

- the hard-coded four-object identity set exactly matches the authoritative WR-042 manifest values;
- non-authoritative WR-061 2014–2016 digests are rejected;
- version/list metadata calls are restricted to exact full custody-key boundaries;
- only exact-name matching versions can be candidates;
- hide/non-upload records are never treated as source bytes;
- version-addressed downloads verify digest/size before consumer use;
- no mutation-capable provider operation/helper is reachable;
- credentials are absent from consumer execution;
- raw bytes cannot become workflow artifacts;
- cleanup executes on success and failure;
- WR-056 trusted-custody regression and Workflow V3.2 Governance remain green.

If `.github/workflows/ci.yml` is changed, keep it narrowly limited to permanent WR-063 regression coverage.

## Authorized read scope

Broad read/inspection authority needed to solve the contradiction, including:

- canonical workflow/state/task specs;
- WR-042/WR-043/WR-046/WR-056/WR-061 evidence;
- PR #176 and all three failed protected runs/logs;
- historical executed WR-042 manifest/result;
- current custody scripts/workflows for comparison only;
- authoritative Backblaze B2 / Cloudflare R2 documentation as needed;
- privacy-safe provider-issued metadata from the protected read-only diagnosis.

## Authorized write scope

Only:

- `.ai/work_helper/**`;
- `.github/workflows/wr063-retained-version-read.yml`;
- `scripts/custody/read_retained_versions.py`;
- `scripts/custody/test_read_retained_versions.py`;
- `scripts/validate-release-candidate.mjs` only to recognize the Manager-approved permanent WR-063 workflow;
- `.github/workflows/ci.yml` only for focused permanent WR-063 regression coverage if needed.

Do not modify:

- existing WR-042/WR-046 mutation-capable workflows;
- existing mutating custody scripts;
- `.ai/research/**`;
- `.ai/shared/**`;
- `.ai/manager/**`;
- `.ai/auditor/**`;
- production code, rankings, models, or public assets.

## Publication / handoff

Publish one immutable WR-063 Work Helper PR/head containing:

- implementation;
- focused tests;
- privacy-safe retained-version diagnosis evidence;
- successful protected four-object live-proof evidence, or exact fail-closed blocker;
- updated `.ai/work_helper/HANDOFF.md`.

Return:

- exact PR/head;
- files changed;
- test/CI run IDs;
- protected live run/job IDs;
- exact B2 version-resolution result for each season;
- exact B2/R2 digest/size results for each season;
- explanation/evidence for the 2013 by-name 404;
- raw-artifact count;
- cleanup result;
- remaining uncertainty;
- `INDEPENDENT AUDIT REQUIRED` only if the complete live proof succeeds.

Do not activate WR-064 and do not merge the implementation.
