# WR-063 — Retained-Object Identity Reconciliation + Version-Aware Read Recovery

TASK ID: WR-063
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
STATUS: BLOCKED
DATE: 2026-09-14
DEPENDENCY: INDEPENDENT
EXECUTION MODE: WORK_MODE_HIGH_VALUE
TARGET BRANCH: `wr-063-retained-object-version-read-recovery`
ASSIGNMENT MODE: `DIAGNOSIS + REMEDIATION`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-064
POST-MERGE CANARY REQUIRED: YES
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Current blocker

WR-063 implemented and regression-tested the authoritative-identity, exact-key-bounded, version-aware B2/R2 read path. Protected run `34901593729` / job `104168617065` authenticated successfully to Backblaze B2 and then failed closed before any version query because the provider-issued capability set for the existing shared custody credential does not include `listFiles`.

Existing credential capabilities observed by WR-063:

- `listAllBucketNames`
- `readFiles`
- `writeFiles`
- `readFileRetentions`
- `writeFileRetentions`
- `readFileLegalHolds`
- `writeFileLegalHolds`

Because that credential is mutation-capable and is shared with the accepted custody path, Manager explicitly rejects broadening it further merely to unblock reconstruction.

WR-063 is therefore BLOCKED on a user-provisioned, separately governed, dedicated B2 read-only application key.

## Manager credential decision — dedicated read-only key

The existing `WR_CUSTODY_B2_KEY_ID` / `WR_CUSTODY_B2_APPLICATION_KEY` secrets MUST remain unchanged.

Create a separate Backblaze B2 application key for WR-063 with all of these restrictions:

- bucket: exactly `War-Room-Custody-Primary`;
- file-name prefix: exactly `custody/sha256/`;
- required capabilities: `listFiles` and `readFiles`;
- read-only retention/legal-hold capabilities are permitted if Backblaze includes them for the selected read-only mode and WR-063 needs them for privacy-safe verification;
- `listAllBucketNames` is not required;
- no write, delete, key-management, governance-bypass, bucket-mutation, file-mutation, retention-mutation, or legal-hold-mutation capability is authorized.

Save the new credential only as GitHub Actions repository secrets:

- `WR_CUSTODY_B2_READ_KEY_ID`
- `WR_CUSTODY_B2_READ_APPLICATION_KEY`

Do not paste either value into chat, commits, PR text, logs, or evidence.

After provisioning, Manager will reactivate WR-063. Work Helper may then update only its already-authorized WR-063 workflow/script surfaces to consume the dedicated read secrets instead of the shared mutation-capable B2 custody secrets.

Before any provider version query, WR-063 must verify the provider-issued authorization boundary proves:

- the allowed bucket is exactly `War-Room-Custody-Primary`;
- the allowed name prefix is exactly `custody/sha256/` or a Manager-approved strictly narrower boundary that still covers all four authoritative keys;
- `listFiles` and `readFiles` are present;
- no mutation-capable capability is present.

If any of those checks fail, fail closed before listing or downloading.

## Historical baseline — inspect, do not merge

WR-061 PR #176 is CLOSED UNMERGED at exact head `5aff59928c4090730959d3f45bbae1483d34fde6`.

WR-063 PR #178 is currently a moving, non-auditable work target and remains open. Current checkpoint head: `4a54161beeca5faed8bf267c977ac6dd3e4c13d7`. The protected proof has not succeeded; WR-064 must not activate yet.

## Authoritative four-object identity set

The executed WR-042 manifest/result at commit/head `cc9005ae4bd9065cf80f1c184f31974904165c54` / `614445a20c2c15fbc3d8c107644a5244ddb52076` remains authoritative.

| Season | Asset ID | SHA-256 | Bytes | Exact custody key |
|---|---:|---|---:|---|
| 2013 | 512983282 | `dbc7804c32c8dbf46120bfe4e724cdd926537509caa8a1bb96cd3b6e159b21f8` | 792070 | `custody/sha256/dbc7804c32c8dbf46120bfe4e724cdd926537509caa8a1bb96cd3b6e159b21f8/raw` |
| 2014 | 512985320 | `7046a0fd69b979d0649feb679a42f82f6e8e175d19587857d3e9371f09427cd6` | 816553 | `custody/sha256/7046a0fd69b979d0649feb679a42f82f6e8e175d19587857d3e9371f09427cd6/raw` |
| 2015 | 512984105 | `b977be5bc8cad6b02dfb755e78974b4486a505fa272f683add499b968878e3eb` | 815021 | `custody/sha256/b977be5bc8cad6b02dfb755e78974b4486a505fa272f683add499b968878e3eb/raw` |
| 2016 | 512985505 | `041473e5860ca6cfcba7a29e98b2ddfc3f01db0df2469d5b44e44135603e2424` | 819194 | `custody/sha256/041473e5860ca6cfcba7a29e98b2ddfc3f01db0df2469d5b44e44135603e2424/raw` |

No alternative digest/key may be substituted.

## Authorized provider operations after reactivation

B2 operations remain non-mutating and limited to authorization, exact-full-key-bounded `b2_list_file_versions` (or equivalent read-only version metadata), exact-name filtering, read-only file/version metadata, and immutable file-ID/version-addressed download for immediate digest/size verification.

R2 remains exact-key `HeadObject`/`GetObject` plus existing read-only lock/config verification only if necessary.

No upstream source-byte reacquisition or provider mutation is authorized.

## Security requirements

1. Runtime selection cannot widen the four authoritative identities.
2. The new B2 read credential must be step-scoped only to trusted WR-063 retrieval/discovery code and absent from the later consumer step.
3. The existing mutation-capable B2 custody credential must not be exposed to the WR-063 read path once the dedicated key is provisioned.
4. Every downloaded candidate is hashed/sized immediately; mismatches are deleted before consumer access.
5. B2 and R2 accepted bytes must both equal the authoritative SHA-256/size and one another.
6. Raw bytes remain runner-temporary only, are cleaned on success/failure, and are never committed/logged/uploaded as Actions artifacts.
7. No upstream GitHub/nflverse byte refresh.
8. Privacy-safe reports may contain non-secret file/version IDs required for independent verification but no credentials, auth headers, signed URLs, raw bytes, or secret values.

## Required live proof after reactivation

Produce one protected live proof on the exact WR-063 implementation head showing all four authoritative objects pass:

- provider-issued dedicated-key boundary verification;
- exact-key B2 version discovery;
- exact retained B2 version retrieval and expected SHA-256/size;
- exact R2 retrieval and expected SHA-256/size;
- B2/R2 equality;
- provider-issued reconciliation of the 2013 by-name 404;
- zero provider mutations;
- consumer credentials absent;
- cleanup PASS;
- zero raw Actions artifacts.

Only after that proof may Manager freeze the target and activate WR-064.

## Authorized write scope after reactivation

Only:

- `.ai/work_helper/**`;
- `.github/workflows/wr063-retained-version-read.yml`;
- `scripts/custody/read_retained_versions.py`;
- `scripts/custody/test_read_retained_versions.py`;
- `scripts/validate-release-candidate.mjs` only as already required for WR-063 workflow recognition;
- `.github/workflows/ci.yml` only for focused permanent WR-063 regression coverage if needed.

Do not modify existing mutation-capable custody workflows/scripts, `.ai/research/**`, `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, production code, rankings, models, or public assets.

## Publication / handoff

After successful reactivation/proof, publish one immutable WR-063 Work Helper PR/head and return the exact PR/head, CI/live-proof IDs, B2 version-resolution results, B2/R2 digests/sizes, 2013 reconciliation evidence, cleanup, raw-artifact count, remaining uncertainty, and `INDEPENDENT AUDIT REQUIRED`.

Do not activate WR-064 and do not merge WR-063.
