# WR-070 — Independent Audit of Accepted-Contract Safe-Consumer Parser

TASK ID: WR-070
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-15
DEPENDENCY: HARD — WR-069 immutable implementation/live-proof target not yet frozen
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-070-retained-safe-consumer-parser-audit`
PRODUCTION AUTHORIZATION: NONE

## Objective

Freshly and independently determine whether the exact future WR-069 safe-consumer retained-evidence parser implementation correctly preserves the accepted WR-063 retained-read security boundary, implements the accepted WR-067 deterministic CSV schema contract exactly, and produces only the authorized privacy-safe derived evidence from the exact 15 WR-042 retained identities.

This audit is required before any WR-069 integration and before WR-059 may resume.

## Activation condition

Remain BLOCKED until Manager freezes:

- exact WR-069 PR;
- exact WR-069 head;
- exact protected live-proof run/job;
- exact derived-evidence artifact path/hash;
- exact implementation branch;
- exact accepted-contract binding.

Manager must populate `audit_target_task`, `audit_target_pr`, `audit_target_branch`, and exact `audit_target_sha` in the active registry before substantive audit execution.

## Required independent audit scope

Verify at minimum:

1. WR-069 consumes exactly the 15 authoritative WR-042 retained identities and rejects non-allowlisted identities and `draft_picks.csv`;
2. no upstream source reacquisition, refresh, substitution, or recustody occurs;
3. B2 provider access uses the accepted dedicated read-only WR-063 credential boundary and proves the expected bucket/prefix/capabilities with no mutation authority;
4. B2 version discovery is bounded by each full exact retained key/name and only exact upload versions can supply bytes;
5. R2 access is exact-key read-only by construction;
6. all 15 B2 and R2 reads reproduce authoritative SHA-256 and byte size and are byte-equal before consumer admission;
7. provider mutation operations are exactly zero and mutation-capable custody helpers/workflows are neither modified nor invoked;
8. provider access completes before consumer execution;
9. provider credentials/authority are absent from consumer execution, and deliberate secret injection fails closed;
10. consumer independently re-hashes and re-sizes all 15 local files before parsing and rejects mismatches;
11. WR-069 binds the accepted `wr-returning-player-v2-csv-schema-inference-addendum/1.0.0` machine lock and corpus exactly;
12. the implementation independently reproduces all 49 accepted conformance cases with zero mismatches;
13. raw parser behavior matches the accepted grammar, null/type/promotion/nullable/row-count/fatal/canonical-hash semantics rather than library defaults;
14. no source-specific special cases weaken the accepted contract;
15. the derived evidence contains only the fields authorized by WR-069 and is deterministically bound to exact source identities;
16. historical player-ID/position inventories are derived from the correct historical retained stats evidence and are not rewritten using current players metadata;
17. exact retained players metadata parsing is limited to the byte-derived evidence allowed by the task;
18. raw retained bytes remain runner-temporary and never appear in commits, logs, summaries, or Actions artifacts;
19. cleanup runs on success and failure;
20. focused parser/security tests, WR-056 custody regressions, WR-063 retained-read regressions, and applicable CI are green;
21. changed files remain inside the exact Manager-authorized WR-069 write scope;
22. no `.ai/research/**`, Manager/shared, Auditor, product/model/ranking, or accepted WR-063 implementation surfaces are modified;
23. no 2026 regular-season outcomes, target/outcome joins, model fitting/scoring/tuning/comparison/evaluation, predictions, rankings, production changes, provider mutation, or Phase-6 work occur.

## Independent reproduction

Do not rely only on Work Helper claims.

Independently inspect/recompute enough evidence to verify:

- authoritative static identity set and digests/sizes;
- accepted contract/corpus hashes;
- all 49 conformance outputs;
- privacy-safe derived-evidence artifact digest;
- provider mutation count and secret isolation from protected logs/configuration;
- zero raw-byte Actions artifacts;
- exact-head CI/test status.

Do not download or expose retained raw bytes merely for audit convenience unless Manager explicitly broadens the audit task. Prefer the protected proof's privacy-safe evidence plus repository/runtime inspection.

## Write scope

Write only `.ai/auditor/**`.

Do not modify WR-069 implementation/evidence, Work Helper files, Manager/shared state, research artifacts, workflows/scripts, provider credentials, production/model/ranking files, or historical checkpoints.

## Publication contract

After Manager activation, publish:

- fresh WR-070 audit report;
- updated `.ai/auditor/HANDOFF.md`;
- immutable audit branch/head;
- Auditor-only PR.

Return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

PASS-family authorizes only Manager to integrate the exact audited WR-069 head and then require the mandatory canonical-main post-merge canary. WR-059 may resume only after that canary passes.

This audit does not itself resume WR-059, activate WR-060, authorize model/scoring/ranking/production work, or begin Phase 6.
