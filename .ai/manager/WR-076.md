# WR-076 — Fresh Re-Audit of Remediated Returning-Player v2 Model Protocol

TASK ID: WR-076
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-15
DEPENDENCY: HARD — WR-072 must publish one Manager-frozen remediated protocol target resolving WR-073-AUD-01
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-076-v2-model-protocol-feature-schema-reaudit`
TARGET PR/HEAD: TO BE SET AT ACTIVATION
PRODUCTION AUTHORIZATION: NONE

## Objective

Freshly and independently re-audit the exact Manager-frozen remediated WR-072 pre-score protocol before any Returning-Player v2 scoring/evaluation may begin.

## Activation condition

Remain BLOCKED until R&D publishes a newly versioned WR-072 protocol lock/head and Manager independently freezes the exact PR/head, protocol identity/version, machine-lock SHA-256, changed-file scope, exact upstream binding, and exact-head CI.

The remediated target must resolve `WR-073-AUD-01` without result inspection.

## Required re-audit focus

Independently verify at minimum:

- failed WR-072 `1.0.0-wr072` protocol/hash remains immutable historical failed-audit evidence;
- the remediated protocol has a new identity/version and new machine-lock hash when bytes changed;
- every relative gate statistic has exact machine-readable numerator/denominator/sign/weighting/zero-or-undefined/comparison semantics;
- position, season, weighted ordering and secondary-baseline gate aggregation are deterministic and unambiguous;
- the confirmation player-cluster bootstrap freezes exact cluster universe ordering, sampling unit/count/replacement, duplicate weighting, RNG API/bit-generator/seed construction, replicate statistic, percentile endpoints, quantile/interpolation method, equality/boundary handling, and finite/degenerate behavior;
- no historical WR-029 implementation is silently treated as v2 normative authority unless explicitly and immutably bound;
- all previously positive WR-073 evidence remains exact, including source/cohort authority, 28 stats-only predictors, zero failed-closed metadata/draft-capital predictors, target/chronology/preprocessing/candidate semantics, 5,176-key evidence, outcome isolation, fail-closed rules, and no premature result work;
- no fitting, scoring, tuning, prediction, result comparison, target/outcome join or inspection, 2026 regular-season outcome inspection, source reacquisition/substitution, provider mutation, production/ranking change, season-total composition, or Phase-6 work occurred.

Return a blocking finding for any remaining result-dependent gate interpretation, nondeterministic bootstrap ambiguity, identity/hash reuse for changed bytes, or premature outcome/model work.

## Write scope

Write only `.ai/auditor/**`. Do not modify WR-072, `.ai/research/**`, Manager/shared state, Work Helper, workflows, production/model/ranking files, or custody scripts.

## Publication contract

Publish a task-specific WR-076 audit report, update `.ai/auditor/HANDOFF.md`, create an Auditor-only PR, record immutable audit head and exact-head CI, and return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

Auditor does not merge WR-072 or authorize scoring. Return to Manager for disposition.
