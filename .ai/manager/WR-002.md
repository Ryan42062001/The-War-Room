# WR-002 — ESPN Synthetic Navigation Attribution Level 4

TASK ID: WR-002

OBJECTIVE:
Perform one short Level-4 disposable ESPN mock validation on the merged provenance V3 implementation and determine the strongest defensible sanitized caller classification for automatic Players → Pick History → Players transitions.

WHY IT IS NEEDED:
Automated and prior live evidence prove the transitions are programmatic and not ordinary user clicks, but the final live caller classification has not yet been observed after V3 representative-frame selection.

VERIFIED STARTING STATE:
- Provenance V3 merged in PR #106 and included in canonical main `a6506d5815e6ec9027f71da759fbe607a40b5020`
- prior V2 live run synchronized 6/6 with 0 unmatched and produced a non-fallback multi-frame hash while displaying caller `unknown`
- V3 automated tests select the first sanitized non-unknown frame without changing mechanism inference or privacy contract

REQUIRED BEHAVIOR:
- load current main extension locally
- reset trace
- remain on Players and do not manually open Pick History
- reproduce at least one automatic transition if available
- copy diagnostics immediately after the transition
- classify only what the sanitized diagnostics actually support

LIKELY FILES / COMPONENTS:
- no implementation files expected
- `extensions/espn-companion/espn-click-provenance.js` is the behavior under validation
- live validation evidence / Auditor handoff may be updated after the run

NON-GOALS:
- do not change ESPN navigation
- do not suppress the flicker
- do not modify sync/reconciliation/scoring/rankings/recommendations
- do not infer ESPN or extension responsibility without evidence

EDGE CASES:
- caller may legitimately remain unknown
- no flicker may occur during a short run; diagnostics should still prove V3 loaded
- user clicks must remain distinguishable as trusted-user events

TEST REQUIREMENTS:
- Level 4 real/mock draft validation
- confirm sync correctness during the short run (captured/applied/unmatched, ACK lag, missing picks)
- inspect sanitized caller class/script/function/hash

ACCEPTANCE CRITERIA:
- current build is confirmed loaded
- one automatic transition is captured, or a bounded no-transition run is documented
- attribution result is recorded without overclaiming
- any new defect discovered is separately classified and routed

EXPECTED HANDOFF:
Independent Auditor / QA handoff with exact current main SHA, environment, diagnostic excerpt, sync result, caller classification, confidence/limitations, PASS/FAIL for WR-002, and recommended next action.
