# WR-072 — Returning-Player v2 Model-Protocol + Feature-Schema Freeze

TASK ID: WR-072
ROLE: Research & Development (R&D)
STATUS: ASSIGNED
DATE: 2026-09-15
DEPENDENCY: HARD — accepted WR-059 / WR-071 source-snapshot and cohort gate
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-072-v2-model-protocol-feature-schema`
TARGET PR: TO BE CREATED
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-073 after Manager exact-target freeze
EXTERNAL AUTHORITY EVIDENCE REQUIRED: NO

## Objective

Freeze the complete Returning-Player v2 **pre-score** model protocol and ordered feature schema required by the accepted `wr-returning-player-v2-evidence-contract/1.0.0` before any model fit, score, tuning run, target join, or outcome inspection is allowed.

This is a specification/evidence task only. It must create a new v2 lineage and may not treat WR-033/WR-D005 fitted state, predictions, or unavailable historical inputs as identical authority.

## Accepted upstream authority

Bind exactly to the accepted source/custody/cohort checkpoint:

- canonical integration commit: `2777ec44ca5b5f2fef77c07d17e4fa75b6013262`;
- WR-071 audit PR `#202` / immutable audit head `96a712ba2cf6a016ddbfdc0ea14cabba282bee04` / verdict `PASS`, no findings;
- WR-071 audit-evidence merge `0eb20f940fcfe455da3129a54525a73e39c966c6`;
- source snapshot ID `wr-returning-player-v2-source-snapshot/1.2.0-wr059`;
- source snapshot SHA-256 `6af88adaea478351a2b9c4884ca248dfed9527cb97f05e3648bd82c5fe0b3cea`;
- cohort ID `returning-player-v2-cohort/1.2.0-wr059`;
- cohort SHA-256 `f62075ec3c13784dea4568fa69aae8a84d39ca70f074ca769132f1f143f2c3d4`;
- historical cohort = 5,176 unique keys, zero duplicates;
- accepted WR-042 custody manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- accepted WR-069 privacy-safe derived-evidence SHA-256 `448baab9b5b3109faee3e322432369f72dcef1569685a16b0605ce25bd855fbb`.

The governing evidence-contract machine lock remains the accepted WR-039/040 authority. Preserve its v1/v2 identity boundary and prospective chronology.

## Mandatory source/feature boundary

The accepted source snapshot has:

- 14 admitted `NFLVERSE_PLAYER_SUMMARY_STATS` source instances;
- 0 admitted Players metadata sources;
- historical `players.csv` asset `563580371` retained but `FAILED_CLOSED` for v2 metadata use;
- `NFLVERSE_DRAFT_CAPITAL_MINIMAL` / `draft_picks.csv` excluded under WR-057.

Therefore WR-072 MUST NOT place any feature into the v2 schema that depends on failed-closed or excluded source classes.

Unless a future separately versioned source-contract/custody/audit gate authorizes them, the protocol must exclude metadata/draft-derived features such as age, birth date, rookie season, draft round, draft pick, current team/status, and any equivalent reconstruction.

Do not silently substitute current `players.csv`, another asset, another provider, or a derived proxy that recreates an excluded semantic field.

## Required frozen protocol

Publish a human-readable protocol plus a deterministic machine-readable lock and SHA-256 sidecar under `.ai/research/**` that predeclare at minimum:

1. **Protocol identity** — new `returning_player_v2` model-protocol version, feature-schema version, serializer version, and artifact IDs.
2. **Exact source/cohort binding** — the accepted source-snapshot and cohort IDs/hashes above.
3. **Ordered feature schema** — exact field names, logical types, nullability, source lineage, cutoff meaning, formula/transform, and canonical serialization order for every predictor.
4. **Target definition** — exact next-season expected-performance target semantics, eligibility/availability rules, zero-game/target-unavailable treatment, and lossless target serialization, declared without reading target outcomes during this task.
5. **Chronology/cutoffs** — target-season Y may use only approved Y-1/prior information available under the frozen contract. No target-season Week-1+ predictor leakage.
6. **Preprocessing specification** — per-position versus pooled treatment, scaling, missing-value/fallback rules, non-finite handling, fitted-state export format, and deterministic implementation identity.
7. **Candidate model family** — exact predeclared candidates/hyperparameters. Historical WR-D005 may inform candidate rationale, but no v1 fitted state or result identity is inherited.
8. **Baselines and future acceptance gates** — predeclare comparison baselines and pass/fail adoption criteria before any v2 result exists. Do not tailor gates to observed future results.
9. **Chronological split/fold protocol** — exact development/validation/confirmation season logic and any seed/determinism requirements.
10. **Full-row evidence contract implementation** — every cohort key must have inclusion/exclusion status, exact pre-transform feature evidence, feature hash, preprocessing lineage, and later prediction lineage before outcomes are joined.
11. **Outcome isolation** — define the separately access-controlled target/evaluation table and the hard gate that prevents target joins until pre-score prediction/evidence surfaces are frozen.
12. **Failure semantics** — missing required source/feature evidence, schema mismatch, extra/missing/duplicate keys, non-finite disallowed values, digest mismatch, or unavailable required lineage must fail closed.
13. **Environment/reproducibility lock** — runtime/library/version/locale/timezone/seed/code-SHA/command requirements needed by the future scoring task.
14. **Future publication contract** — exact artifacts the later scoring task must produce before Manager may authorize outcome evaluation.

## Historical evidence allowed for protocol design

You may read accepted pre-v2 research such as WR-025, WR-027, WR-029, WR-033/WR-D005, WR-034/WR-D006, and WR-039/040 only to predeclare candidate structure, target semantics, chronology, and gates.

Clearly distinguish:

- reuse of an idea or mathematical form;
- new v2 versioned specification;
- prohibited claims of identical input/fitted/prediction lineage.

## Forbidden execution

WR-072 must not:

- fit a model;
- score a model;
- tune or compare candidates on v2 outcomes;
- inspect or materialize 2026 regular-season outcomes;
- join historical or 2026 target/outcome values to feature rows;
- generate predictions;
- evaluate MAE/RMSE/rank correlation or any model-result metric;
- reacquire/refresh/substitute upstream source bytes;
- access retained raw bytes merely to redesign the protocol;
- mutate provider state;
- use `draft_picks.csv` or any replacement draft-capital source;
- use failed-closed `players.csv` metadata semantics;
- modify production rankings/code;
- begin season-total composition or Phase 6.

## Write scope

Write only `.ai/research/**`.

Do not modify `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, `.ai/work_helper/**`, `src/**`, `public/**`, or frozen WR-021/WR-023 artifacts.

## Required return to Manager

Return:

- exact branch and PR;
- exact immutable candidate head;
- exact changed-file scope;
- protocol ID/version;
- feature-schema ID/version;
- machine-lock SHA-256 and sidecar verification;
- exact source-snapshot/cohort binding;
- complete ordered predictor list and source lineage;
- explicit confirmation that no feature depends on failed-closed metadata or excluded draft capital;
- target definition and zero-game/target-unavailable semantics;
- preprocessing/candidate/split/gate declarations;
- deterministic validation results;
- exact-head War Room CI;
- explicit confirmation that no scoring, target join, outcome inspection, prediction, or evaluation occurred.

R&D does not activate WR-073 and does not merge its own PR.

## Next gate

R&D publishes one immutable pre-score protocol target -> Manager independently verifies/freezes exact head and hashes -> WR-073 fresh independent audit -> PASS-family only may authorize a later, separately assigned scoring/evaluation task.
