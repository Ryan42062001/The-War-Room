# WR-046 — Source-Custody Capability Recovery

TASK ID: WR-046
ROLE: Work Helper / Super Troubleshooter / Cross-Functional Operator
ASSIGNMENT MODE: CROSS-ROLE RECOVERY
STATUS: REWORK_REQUIRED — BOUNDED CREDENTIAL-SCOPE ATTESTATION
DATE: 2026-09-12
DEPENDENCY: WR-047 `FAIL — REMEDIATION REQUIRED`
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-046-custody-capability-recovery`
TARGET PR: #135
LAST AUDITED HEAD: `64ba4aff697c1f45472045b52f374b01ee9e1695`
LIVE PROVIDER PROOF LINEAGE HEAD: `4cade5204631f5f2875d664f862dcb4fa0a85200`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — fresh WR-050 re-audit after remediation

## What already passed
WR-047 independently passed the WR-046 exact-byte acquisition, B2 primary custody, B2 COMPLIANCE retention + Legal Hold, R2 independent backup, R2 indefinite Bucket Lock, direct retrieval/digest equality, secret/privacy handling, no-master/root boundary, contract preservation, no-source/no-model/no-production boundary, browser-CI separation, and final-head lineage.

Do not redo or weaken those mechanics merely because WR-047 failed overall.

## Blocking finding
`WR-047-AUD-01 — HIGH — Actual provider-side least-privilege credential scopes are asserted but not independently attested.`

The live workflow proves which permissions were exercised, but does not freeze privacy-safe provider-issued current authorization metadata proving that the exact configured credentials have no broader authority.

## Bounded remediation objective
After WR-048 completes and the Work Helper role is free, continue PR #135 and add durable privacy-safe provider-issued scope evidence for the exact configured credentials.

At minimum prove, without exposing any secret value:

### Backblaze B2
For the exact application key represented by `WR_CUSTODY_B2_KEY_ID`, capture provider-issued current authorization metadata sufficient to prove:
- allowed bucket identity is only the dedicated War Room custody bucket;
- `namePrefix` is exactly the approved `custody/` prefix or narrower;
- current capabilities are exactly the approved custody capability set or a strictly narrower sufficient subset;
- no delete, governance-bypass, bucket-admin, account-admin, or master-key authority exists.

Preferred evidence may use a provider API response such as `b2_authorize_account` or equivalent privacy-safe metadata, but must not expose the application key secret.

### Cloudflare R2 object credential
For the exact configured object credential, freeze provider-issued current policy/resource metadata proving:
- resource scope is limited to the dedicated War Room R2 custody bucket;
- object permission is only the intended Object Read & Write surface or narrower sufficient permission;
- no bucket-lock/configuration write/admin or broader account authority is present.

### Cloudflare Bucket-Lock read token
For the exact configured configuration-read token, freeze provider-issued current token-policy metadata proving:
- resource scope is no broader than required for the dedicated R2 custody resource/account read;
- permission group is read-only for the required R2 storage configuration visibility;
- no write/admin ability exists to alter or remove Bucket Lock.

## Fail-closed rule
If any configured credential is broader than the Manager-approved contract:
1. do not certify it;
2. replace/re-scope the credential using the minimum sufficient policy;
3. update the protected GitHub secret as needed;
4. re-run the live B2/R2 proof because the credential/policy changed;
5. freeze fresh scope evidence for the replacement credential.

If all exact configured credentials already match the approved least-privilege envelope, do not rerun the live fixture proof solely for documentation churn. Preserve the previously audited live proof lineage and add the missing provider-issued scope evidence.

## Security / evidence rules
- never print or commit secret values;
- do not print reusable bearer tokens, secret access keys, application keys, or credential bodies;
- record only provider-issued non-secret identifiers, resource scope, permission groups/capabilities, prefix restrictions, policy names/IDs where non-sensitive, timestamps, and verification outcome;
- redact account identifiers only where exposure would materially increase risk; keep enough non-secret identity to let the Auditor bind the attestation to the exact configured credential;
- do not use screenshots alone if a machine-readable provider response can be captured safely;
- do not rely only on a human statement like “I created it correctly.”

## Scope boundaries
No actual Returning-Player source admission/parsing. No 2026 regular-season outcomes. No model fitting/scoring/tuning/comparison/ranking/evaluation. No production/user-facing change. No `.ai/research/**` modification. No WR-D008 / WR-039 semantic expansion.

## Scheduling
The single Work Helper role is currently active on WR-048. Do not interrupt that canonical task. Resume this bounded WR-046 remediation only after WR-048 returns control to Manager.

## Next gate
Produce one immutable remediated PR #135 head containing the provider-issued credential-scope evidence (and, only if credentials changed, refreshed live-provider proof). Then activate WR-050 for a fresh independent audit.

A PASS-family WR-050 verdict is required before Manager may issue a bounded R&D exact-source custody re-attempt.
