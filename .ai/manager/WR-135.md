# WR-135 — Supported-Envelope Full-Draft Synthetic Boundary Regression

TASK ID: WR-135
ROLE: Implementation Engineer / Builder
STATUS: ASSIGNED
DATE: 2026-09-21
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — follows closed WR-133/WR-134 synthetic cross-layer regression and WR-132 remaining A6 evidence inventory
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-135-supported-envelope-full-draft-invariants`
ASSIGNED BRANCH: `wr-135-supported-envelope-full-draft-invariants`
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: `b4bfd575f913fbdf00865fa88d3ca1ef17d744ff`
ACTIVATION: Manager must merge WR-D052 Manager control-plane PR after exact-head Governance SUCCESS, verify genuine post-merge canonical-main push Governance SUCCESS, then create this fresh assigned Builder branch from the EXACT THEN-CURRENT canonical main and verify 0 ahead / 0 behind. The pre-activation checkpoint above is NOT the allowed Builder branch creation SHA.
AUDIT REQUIRED: YES — this test-only A6 boundary evidence requires fresh Independent Auditor / QA review on one immutable final head.
POST-MERGE CANARY REQUIRED: YES — any later audited harness integration requires genuine canonical-main FULL War Room CI before WR-135 closure.

## Accepted narrow evidence gap

WR-132 found that no complete current full-draft regression exercises both supported team/round extremes (2–20 teams, 5–30 rounds). WR-133/134 closed a DIFFERENT gap with independently audited synthetic Companion→real bridge→app/ACK 10-team × 16-round full-draft coverage. That accepted 10×16 cross-layer evidence is preserved and MUST NOT be restated as proof of either extreme.

Current `scripts/test-draft-invariants.mjs` already drives actual War Room browser/app source through a 717-row committed local board and complete 10×16 and 14×16 drafts, with existing synthetic ESPN segment, pick/ownership progress, view and persistence invariants. Extend that same app-side torture harness to test full accepted envelope extremes without duplicating its core draft algorithm or touching the separate WR-133 cross-layer harness.

## EXACT authorized writes — THREE PATHS ONLY

1. `scripts/test-draft-invariants.mjs` — narrowly extend existing synthetic browser/app-side full-draft scenarios, checks and logging.
2. `.ai/builder/WR135_SUPPORTED_ENVELOPE_FULL_DRAFT_EVIDENCE.md` — task-specific reproducible evidence.
3. `.ai/builder/HANDOFF.md` — concise Builder handoff.

NO other file may change. Do not touch `package.json`: `test:draft-invariants` is already named and included in the existing `npm test` aggregate. Do not modify `scripts/test-wr-133-companion-war-room-e2e.mjs`, any Companion source, production app, ranking data, dataset, permission/credential, workflow/runner, or deployment/release surface. Read current unchanged source and tests freely.

## Required app-side boundary scenarios

Use only the actual current War Room app in local Chromium with committed local 717-player board and deterministic synthetic picks. No live ESPN or provider/network fetch. Preserve existing `10x16`, `14x16`, and current synthetic ESPN segment tests and their assertions/intent.

A. **Minimum supported full draft:** 2 teams × 5 rounds × slot 2 = EXACTLY 10 unique numbered picks, ending with 5 Mine and 5 Taken. Exercise actual configured 2/5 values and valid last team slot, not only a static settings-clamp or a fabricated terminal flag.

B. **Maximum supported full draft:** 20 teams × 30 rounds × slot 20 = EXACTLY 600 unique numbered picks, ending with 30 Mine and 570 Taken. Use 600 distinct rows from the existing committed 717-row universe, no repeated/fabricated player row. Exercise actual 20/30 settings and last team slot.

For BOTH A and B, independently verify by accepted numbered pick:
- 1..N contiguous, strictly unique numbered ledger and player identity; exact independent snake mapping to team slot and Mine/Taken from configured draft slot; check actual DOM/authoritative row metadata, not just the harness's intended input;
- monotonic completed picks and active draft progress, correct configured teams/rounds/slot and product total, no beyond-N row/next turn;
- nonterminal N-1 does not fabricate pick N or claim authoritative completion; terminal N is complete with truthful no-next-turn state, correct own roster count and available-player count;
- actual saved state preservation and reload equivalence at a meaningful intermediate checkpoint and at terminal; catch missing/duplicate/misowned picks or changed settings across reload;
- board/position-tier/recommendation invariants at representative checkpoints and zero browser errors;
- deterministic seed/config, fixture/expected ledger and actual ledger/ownership digest or an equivalently exact, reproducible summary at meaningful intermediate, penultimate, terminal and post-terminal-reload checkpoints;
- local-only execution, explicit external-request observation/fail-closed handling consistent with the current local test context, and truthful count of unexpected network use.

Use the existing `runFullDraftScenario`, `applyOperation`, `assertInvariants`, `captureAuthoritativeState`, persistence and view helpers where feasible. Extend checks rather than introduce a second simulator. Do not silently reduce player/pick count, skip actual per-pick checks, loosen assertions, increase global timeouts merely for green output or hand-wave the 600-pick boundary as too large. Reasonable periodic expensive checks are acceptable only if per-pick ledger/progress/ownership checks remain real and explicit.

If actual current production or app/Companion contract fails at a boundary, STOP within the authorized evidence paths, preserve the deterministic reproducer and exact observed failure, and request separate Manager remediation authority. No production/Companion change is authorized here. If genuine CI capacity makes the 600-pick browser test impractical, report quantified timing/evidence and request a separate bounded Manager acceptance decision; do not silently replace it with a partial draft.

## Validation and evidentiary honesty

Actually run / prove:
- `node --check scripts/test-draft-invariants.mjs`;
- `npm run test:draft-invariants` on the exact final source;
- existing relevant WR-133 cross-layer named regression remains unchanged and passing;
- `npm test`, including the prior 10×16, 14×16, Companion 167/167, WR-118, WR-133 paths;
- genuine exact-final-head FULL War Room CI with actual classify, Governance and product job SUCCESS.

Record exact commands, pass/fail, seeds, counts, actual elapsed time, machine/CI environment if material, full CI run/job IDs, and first failing assertion if any. Do not infer success from a command registration or merely from green status without confirming relevant test execution.

**Claim boundary:** WR-135 is app-side full-draft supported-envelope synthetic evidence; it does NOT establish 2×5 or 20×30 Companion→bridge→app E2E equivalence, live ESPN fallback, independently proven structured Direct, physical-device compatibility, 2027 source freshness, deployment/rollback, or draft-ready release. Do not silently narrow release claims or suggest the remaining formal A6 gate is closed.

## Publication and separation

Builder publishes ONE open/unmerged PR from the assigned task branch with exactly the three authorized cumulative paths, one immutable final head, detailed evidence and compact handoff. Stop Builder writes after freeze. Manager independently verifies the live exact head/diff, real test coverage and exact-head FULL CI before separately assigning a FRESH independent Auditor task. Only accepted PASS-family on unchanged target permits a subsequent separate Manager integration decision, with mandatory genuine canonical-main FULL CI before WR-135 closure. No provider contact, rankings fetch, deployment or release.
