# WR-133 — Synthetic Companion→War Room End-to-End Regression

TASK ID: WR-133
ROLE: Implementation Engineer / Builder
STATUS: CLOSED — WR-D051 INDEPENDENT PASS ACCEPTED / EXACT BUILDER INTEGRATED / CANONICAL FULL CI SUCCESS
DATE: 2026-09-21
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — consumes accepted WR-132 release-evidence inventory
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-133-companion-war-room-synthetic-e2e-regression`
ASSIGNED BRANCH: `wr-133-companion-war-room-synthetic-e2e-regression`
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: `6e5280ef77e761e163dfdba376899b53517ea079`
ACTIVATION: Manager must merge the WR-D049 control-plane PR, verify genuine canonical-main push Governance SUCCESS, then create this Builder branch from that exact new main and verify 0 ahead / 0 behind. The checkpoint above is not the permitted branch creation SHA.
AUDIT REQUIRED: YES — this test becomes A6 release evidence only after fresh independent audit of one immutable Builder target.
POST-MERGE CANARY REQUIRED: YES — if the audited test-only change is later merged, genuine canonical-main FULL War Room CI is mandatory before closure.

## Accepted gap

WR-132 established one bounded current evidence gap:

Existing Companion parser/background/content/bridge tests are strong and current WR-118 app-side replay/reconnect coverage is strong, but no accepted single executable fixture drives the **real current Companion background ledger → real War Room content bridge → real War Room app snapshot ingress** through a complete deterministic draft/replay/correction/reconnect/terminal lifecycle.

This is an evidence gap, not an assumed production defect.

Close only that gap.

## Exact authorized writes — FOUR PATHS ONLY

1. `scripts/test-wr-133-companion-war-room-e2e.mjs`
2. `package.json`
3. `.ai/builder/WR133_COMPANION_WAR_ROOM_E2E_EVIDENCE.md`
4. `.ai/builder/HANDOFF.md`

NO OTHER FILE MAY CHANGE.

### package.json restriction

Only:
- add one named command: `test:wr133-companion-war-room-e2e`;
- add that named command to the existing aggregate `npm test` chain in the smallest appropriate location.

Do not change dependencies, versions, package metadata, existing command semantics or toolchain.

## Read / execution authority

The test harness MAY read and execute the current repository source unchanged, including:
- `extensions/espn-companion/background.js`
- `extensions/espn-companion/war-room-content.js`
- current Companion test fixtures/assets under `extensions/espn-companion/test/**`
- current War Room app code including `js/war-room-espn-sync.js`
- current WR-118 test/oracle patterns in `scripts/test-wr-118-espn-replay-reconnect.mjs`
- current local board/data files already committed to the repository

Those are READ-ONLY inputs under WR-133.

Do not copy/reimplement the Companion ledger, bridge, snake draft engine, recommendation engine or app sync algorithm inside the test merely to make a green result. The harness should execute real current source and independently compare observed state.

## Test architecture requirement

Build ONE deterministic source-free synthetic test harness.

The test must:
- use only local repository assets and synthetic fixture messages;
- make zero ESPN/provider requests;
- fail if an unexpected external network request occurs;
- execute the actual current Companion `background.js` in a bounded test context with mocked browser/Chrome APIs only as needed for deterministic execution;
- execute the actual current `war-room-content.js` message bridge on a trusted local War Room page/context;
- load the real War Room application in Chromium;
- deliver snapshots through the real current runtime/message contract rather than calling only `WarRoomEspnSync.applySnapshot` directly;
- allow the real app page listener to reach the real `applyEspnDraftSnapshot` path;
- exercise the real ACK path back through the content bridge/background contract where applicable;
- reuse existing fixture/message shapes and WR-118 state/oracle concepts rather than invent a second simulator.

The test harness may implement mocks for browser APIs, deterministic clocks/storage and local transport glue. Those mocks must not replace the actual Companion/app algorithms being tested.

## Fixed release-mode scenario

Use one fixed supported release-mode league:
- Full PPR
- redraft
- snake
- 10 teams
- 16 rounds
- draft slot 7
- 160 total picks

Use committed local player rows / synthetic IDs only. Do not fetch or introduce a new player source.

## Required lifecycle checkpoints

At minimum drive and assert:

### A. Setup / handshake
- Companion config and War Room settings agree on 10 teams / 16 rounds / slot 7.
- bridge channel/version contract is the real current contract.
- no unsupported permission/auth/provider behavior is introduced.

### B. Initial numbered draft progression
- deliver a deterministic numbered pick slice through Companion background/ledger → content bridge → app.
- compare Companion numbered ledger vs app numbered ledger.
- compare team ownership / Mine-vs-Taken ownership.
- verify applied/captured/unmatched/ACK counters or equivalent current truth where applicable.

### C. Duplicate / reordered delivery
- replay duplicate and permuted input.
- prove no duplicated app picks, no wrong ownership and no count inflation.

### D. Stale shorter replay
- deliver an older/shorter state after a newer accepted state.
- prove authoritative accepted state does not silently regress or erase valid picks.

### E. Unresolved / conflict stage
- include at least one synthetic unresolved or conflicting pick state using current Companion contract semantics.
- prove the unresolved/conflict state is not silently converted into a valid wrong app pick.

### F. Authoritative correction
- deliver the valid corrected state.
- prove Companion/app ledgers converge to the corrected numbered ownership and stale/conflicting state is displaced according to current production semantics.

### G. Session isolation
- exercise two distinct synthetic draft/session identities A and B using existing session/draft-key behavior.
- prove B does not contaminate A and A remains intact when returned to.

### H. Reload / synthetic reconnect
- reload the War Room page/context while preserving supported local state.
- re-establish the real content bridge.
- replay/resend the current Companion snapshot through the actual bridge path.
- prove app state converges without duplication, loss or ownership drift.

### I. Terminal sequence
- progress the fixed draft to 159/160 and then 160/160 through the real cross-layer path.
- verify terminal completion and no invalid next-turn state.
- reload at terminal and prove terminal ledger/completion remains converged.

## Ledger / identity proof

At named checkpoints record deterministic, privacy-safe digests for:
- synthetic input fixture/order;
- Companion numbered ledger;
- app numbered ledger;
- ownership/Mine-vs-Taken mapping.

Do not print private URLs, cookies, tokens or credentials.

Stable checkpoints must show exact Companion→app ledger equivalence for the accepted numbered picks, subject only to explicitly documented unresolved states.

## Negative controls — REQUIRED

Execute at least THREE non-vacuous test-only negative controls proving the oracle can actually fail.

They must cover at least:
1. lost/missing accepted pick detection;
2. wrong/misowned team or Mine/Taken assignment detection;
3. bridge contract/channel or numbered-ledger drift detection.

Negative controls must intentionally operate on test-only copied/observed values or bounded harness injection. They must NOT alter production source.

Log that each expected failure was detected.

## Existing evidence preservation

WR-133 must not weaken or rewrite:
- WR-118 accepted app-side replay/reconnect evidence;
- current Companion 167/167 behavior;
- WR-D001 ECR VALUE vs ESPN TIMING;
- WR-D018 fallback-first / `LIVE_DIRECT_UNVERIFIED`;
- WR-D027 NO PROVIDER CONTACT;
- WR-D038 recommendation truthfulness;
- WR-D043 Command Center closure;
- WR-D047 round-count closure;
- A4 season/source gate;
- paused Track B gates.

This task does NOT close:
- exact-current live ESPN fallback evidence;
- structured Direct validation;
- 2-team/5-round and 20-team/30-round full-draft boundary coverage;
- physical-device certification;
- deployment/rollback evidence;
- 2027 ranking/source freshness.

Those remain separate A6/A4 decisions.

## No remediation authority

If the honest WR-133 test exposes a production/Companion defect, contract incompatibility, silent divergence or missing testability seam that cannot be proven without changing production code:

STOP.

Do not modify `js/**`, `extensions/**`, current fixture files, ranking/scoring/recommendation code, workflows or deployment surfaces.

Preserve the reproducer/evidence within the authorized test/evidence paths and report the smallest justified Manager remediation scope.

Do not relax an oracle merely to obtain green CI.

## Required validation

Actually execute / prove on the final Builder head:
- `node --check scripts/test-wr-133-companion-war-room-e2e.mjs`
- `npm run test:wr133-companion-war-room-e2e`
- existing Companion test aggregate / relevant background/content/bridge tests
- `npm run test:wr118-espn-replay-reconnect`
- `npm test`
- genuine exact-final-head FULL War Room CI

Record actual commands/outcomes and exact run/job IDs. Do not infer execution from registration.

Verify cumulative Builder diff from the assigned creation base contains EXACTLY the four authorized paths.

## Publication / audit gate

Builder publishes ONE PR against main and leaves it OPEN / UNMERGED.

After implementation/testing:
- freeze ONE immutable final Builder head;
- stop all Builder writes after final publication;
- exact-final-head FULL War Room CI must be SUCCESS;
- Builder does not self-audit or merge.

Manager then independently verifies:
- exact four-path cumulative scope;
- real-source execution vs duplicated test logic;
- checkpoint/ledger/ownership assertions;
- negative controls;
- actual full CI logs;
- no provider/network/source/policy/deployment behavior.

Manager will then activate a FRESH Independent Auditor / QA task on the exact frozen target.

Only PASS-family on the exact unchanged target may permit later test-evidence integration.

After any later merge, genuine canonical-main FULL War Room CI is mandatory before WR-133 closure.

## Forbidden

No:
- live ESPN/account interaction;
- provider contact;
- provider/source downloads;
- 2027 ranking fetch/import/evaluation;
- ranking/scoring/recommendation policy change;
- production app change;
- Companion production change;
- extension permission/auth/credential/endpoint change;
- workflow/runner change;
- deployment or rollback;
- release or draft-ready declaration;
- second simulator.

## WR-D050 — Manager exact-target freeze and fresh audit assignment (2026-09-21)

Manager independently verified canonical main `3a0c5a9ad8f4e3641ef3953b89d807a550941df9`, live open/unmerged Builder PR #374 and branch at exact final target `f463f73b7e2d92a34b358f61742c4af5abfefb76`, 7 commits ahead / 0 behind, and exactly the four authorized cumulative paths. The `package.json` patch adds only the named test and aggregate registration; no production/Companion/source/permissions/workflow/runner/deployment path changed.

Genuine exact-final-head FULL War Room CI #35670686238 completed SUCCESS: classify #106566220511 SUCCESS; Governance #106566252568 SUCCESS; full test #106566300474 SUCCESS; bootstrap-reuse #106566253713 correctly SKIPPED. Actual full-test logs include Companion 167/167, WR-118, WR-133 A–I including protected B, explicit A restoration and bridge replay, 20-pick reconnect, 159/160, authoritative 160/160 and terminal reload, three negative-control detections, zero unexpected external browser requests and zero Companion fetches. The former #35655679877 G passive-restore failure remains a historical FAILURE; the repaired test respects the unchanged intentional passive-session guard and performs the real explicit Draft-menu return.

**FREEZE:** `f463f73b7e2d92a34b358f61742c4af5abfefb76` is the one immutable WR-133 audit target. Do not modify, force-push, merge or otherwise move Builder PR #374 / branch before fresh WR-134 audit and separate Manager disposition. If target moves, fail closed and reassign/re-audit an explicitly newly frozen head; a verdict does not transfer. Passing CI is not an independent audit verdict. WR-133 is AUDIT_READY, not MERGE_READY or CLOSED. Only a future independent PASS-family on the unchanged target may permit a separate Manager integration decision; genuine canonical-main FULL CI is mandatory after any later integration before WR-133 closure. No provider contact, ranking/source work, deployment or release.

## WR-D051 — WR-133 audited synthetic cross-layer regression closure (2026-09-21)

WR-134 independent Auditor / QA published **PASS; CRITICAL 0 / HIGH 0 / MEDIUM 0 / LOW 0**, bound exclusively to frozen Builder PR #374 head `f463f73b7e2d92a34b358f61742c4af5abfefb76`. Published Auditor PR #376 immutable head `acb4717d0ebfbe4b2029574b56b7b05aeaa4ad30` changed exactly the two authorized Auditor evidence paths and passed exact-head CI #35673159065 (classify #106573936908 SUCCESS, Governance #106573980063 SUCCESS; product test correctly SKIPPED). Manager independently reviewed the report and handoff and accepted that PASS only on the unchanged Builder target.

Auditor evidence PR #376 was integrated first as canonical `f770fd29641fb583c53d4843985fda645472e591`; genuine post-evidence canonical-main push CI #35673658677 SUCCESS, including Governance. Manager independently reverified the Builder head unchanged, its cumulative four-path authorized scope, nonoverlap with intervening Manager/Auditor documentary advancement, and exact-head FULL CI #35670686238 SUCCESS. Builder PR #374 was merged only with expected-head guard at `f463f73b7e2d92a34b358f61742c4af5abfefb76` as canonical `53e0c652a1d9043e6abce8f297b6fbff8128d8e8`.

**Mandatory genuine post-integration canonical-main PUSH FULL War Room CI #35673699491 completed SUCCESS** on canonical SHA `53e0c652a1d9043e6abce8f297b6fbff8128d8e8`, first attempt: classify #106575617629 SUCCESS, Governance #106575637018 SUCCESS, full product test #106575680004 SUCCESS, bootstrap-reuse #106575637753 correctly SKIPPED. Actual full-test logs show Companion 167/167, WR-118 PASS, named WR-133 PASS with three detected negative controls, all A–I synthetic lifecycle checkpoints, terminal 160/160 and reconnect, zero unexpected browser external requests / Companion fetches. Historical failed Builder CI #35655679877 remains historical **FAILURE**.

**WR-133 CLOSED** as an independently audited, integrated, post-merge-full-CI-tested **bounded synthetic local** Companion→bridge→War Room regression. This does NOT close the A6 formal draft-ready release gate, verify current live ESPN fallback or structured Direct, certify full-draft supported-envelope extremes or physical devices, produce deployment/rollback receipts, authorize provider contact/source admission, or establish A4/2027 ranking freshness. WR-D001 ECR VALUE / ESPN TIMING, WR-D018 fallback-first / LIVE_DIRECT_UNVERIFIED, WR-D027 NO PROVIDER CONTACT, season-gated A4 and paused Track B remain preserved.
