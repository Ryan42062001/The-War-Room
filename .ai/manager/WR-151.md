# WR-151 — One Bounded GitHub-Hosted Inert Loopback-Egress Preflight Implementation

TASK ID: WR-151
ROLE: Implementation Engineer / Builder
ASSIGNMENT MODE: STRICT IMPLEMENTATION — DORMANT HOSTED-RUNNER INERT PREFLIGHT ONLY
STATUS: CLOSED — WR-D080 NO_QUALIFIED_RUNNER; attempted hosted preflight/local surrogate DEFERRED; PR #422 remains DRAFT/UNMERGED/UNLABELED
DATE: 2026-09-23
WORKFLOW: V3.5 CANONICAL
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: builder/wr-151-inert-hosted-runner-loopback-preflight
ASSIGNED BRANCH: builder/wr-151-inert-hosted-runner-loopback-preflight
PRE-ACTIVATION CANONICAL MAIN CHECKPOINT: 2d91adb59f919a9d94db46047a03f2c3417f57b6
DEPENDENCY: INDEPENDENT
AUDIT REQUIRED: YES — fresh independent security/workflow Auditor on exact frozen Builder head BEFORE dedicated first executable pilot activation; independent audit on actual run evidence after pilot, if executed
POST-MERGE CANARY REQUIRED: YES — changes CI workflow; prospective integration only after separate Manager approval and genuine same-landed-SHA FULL CI

## Controlling evidence and immutable limits

WR-150 PR #420 exact Worker head 9371a0d7e44a128e96bc6e8c58e0e8be78829e27; exactly two Work Helper documentation paths; exact-head War Room CI #35940587593 SUCCESS / Governance #107447417657 SUCCESS / product #107447587902 correctly SKIPPED. Manager acceptance comment 5805671131 accepts RUNNER_PREFLIGHT_IMPLEMENTATION_SCOPED with first-trigger safeguards; evidence integrated canonical 2d91adb59f919a9d94db46047a03f2c3417f57b6, genuine post-integration main War Room CI #35941983280 SUCCESS incl Governance #107451676508 SUCCESS, product skipped.

WR-149 BLOCKED_BROWSER_OR_RUNNER is NOT a product failure or recoverability evidence. Previous GitHub-hosted canonical-main FULL CI #35885632498 product job #107265099200 downloaded Playwright Chromium/headless shell v1243 and ran browser tests. This proves HISTORICAL hosted-browser availability, NOT a future namespace, egress or service-worker PASS. User's currently active Ubuntu-24.04 WSL self-hosted service/runner has only availability evidence and MUST NOT be targeted by WR-151. WR-150 report: .ai/work_helper/WR150_HOSTED_RUNNER_ISOLATION_QUALIFICATION.md. Manager PR #420 comment 5805671131 controls and sharpens this task.

## EXACT authorized cumulative Builder PR files — five only

1. .github/workflows/ci.yml — smallest additive dedicated inert pilot job/event gate; do not change existing classify, governance, bootstrap-reuse or product job semantics beyond absolutely necessary pull_request event-type expansion to include labeled while preserving previous opened/reopened/synchronize behavior.
2. scripts/wr150-loopback-netns.sh — minimal safety wrapper, namespace/privilege/process containment, fail-closed cleanup.
3. scripts/wr150-loopback-preflight.mjs — inert test page + inert activated controlling service worker and network isolation probes/receipts, never War Room app.
4. .ai/builder/WR151_INERT_HOSTED_RUNNER_PREFLIGHT_IMPLEMENTATION.md — design, scope, security model, exact CI receipts/limitations and future independent Auditor handoff.
5. .ai/builder/HANDOFF.md — concise Builder return.

NO other repository write: no product, existing tests/scripts, package or lockfile, datasets, Companion, README, production/pages config, Manager/shared/Auditor files, secrets or labels.

## First-trigger security contract — nonnegotiable

A new executable PR workflow job can run on FIRST PR opened/synchronize BEFORE a reviewer sees it. New dedicated job MUST therefore remain TECHNICALLY SKIPPED on every opened/reopened/synchronize, ordinary push and all unrelated PRs, including first publication and subsequent head changes. The only permitted trigger is `pull_request` event action `labeled` carrying ONE dedicated label `wr151-inert-preflight-reviewed` on exact same-repository head branch `builder/wr-151-inert-hosted-runner-loopback-preflight` targeting `main`; job predicate must evaluate actual event.action AND event.label.name AND exact head repo/fullname and branch and designated PR state, not merely whether an old label remains on PR. Label MUST be absent from first Builder PR; never create, apply or ask someone to apply it in this Builder lane. A Manager with independently accepted fresh exact-HEAD Auditor report alone may authorize a single labeling action on the unchanged frozen head. A label is an operational trigger, NOT GitHub branch protection or cryptographic proof of audit; after any head movement Manager must re-freeze/re-audit before any new event, and a second labeled attempt is not automatically authorized.

Existing workflow currently declares `on: [push, pull_request]` whose default PR activity set does not include labeled; preserve existing push and PR behavior while explicitly adding `pull_request.types: [opened, reopened, synchronize, labeled]` as necessary. Do not invent `workflow_dispatch` or `pull_request_target`. Preserve standard PR exact-head Governance/FULL path-aware execution; since .github changed, applicable genuine exact Builder PR-head FULL is expected, with product test job SUCCESS and dedicated pilot job SKIPPED before any security audit. This dormant first pass does NOT count as pilot execution. If adding the dormant job unexpectedly runs it, STOP, preserve log/evidence, no unsafe continuation.

Dedicated inert job MUST be hosted-only e.g. `runs-on: ubuntu-24.04` (not self-hosted, not group fallback), no deployment environment or secrets, minimal job `permissions: contents: read`, checkout exact `github.event.pull_request.head.sha` with `persist-credentials: false`, record both PR head and default synthetic merge SHA and assert checked-out head identity before ANY executable pilot code. Never use a PR-derived shell interpolation or external fork. Pilot must have no access to private account/host runner and no action that modifies public Pages or production repository.

## Controlled provisioning and isolation design

Only for later separately Manager-triggered inert job: provision `npm ci`, `npx playwright install --with-deps chromium` on isolated disposable GitHub-hosted VM BEFORE network lockdown, pin frozen package-lock/tree and record versions. Do NOT require fresh Chromium on Builder's current scratch environment. Reject package-lock or other unexpected executable input changes. Verify `sudo`/`ip` and namespace capability on actual hosted runner; if unavailable, report FAIL, not permission to use persistent WSL runner or untrusted proxy routing.

Create a NEW ephemeral network namespace (`sudo ip netns add` or reviewed equivalent) with ONLY its loopback interface UP; no veth/bridge/IPv4/IPv6 default routes/forwarding. Run complete Node→Playwright→Chromium renderer/browser/service-worker process tree there as an unprivileged user with capability drop; no inheritable proxy/broker sockets, host Unix-socket forwarding, namespaces or external agents that provide a route. Sanitize ALL upper/lower HTTP/HTTPS/ALL/NO_PROXY and broker env settings before browser launch. Capture only env variable NAMES, not values, user secrets or all-environment snapshots. Record namespace inodes/process identities/capabilities, interfaces/routes, browser + SW controlling status and child-process associations. If direct browser/SW PID attribution cannot be observed credibly, mark UNVERIFIED and fail the gate.

Serve an INERT local HTML + INERT service-worker on 127.0.0.1 stable loopback origin (in disposable job temp directory and namespace). Positive app-free local fetch and worker control are mandatory. Negative page and service-worker requests to documentation-only reserved 198.51.100.1 and 2001:db8::1 with bounded timeouts, no DNS/providers or private LAN. Distinguish actual network unreachable/enforced namespace no-route from mere CORS, TLS or browser mixed-content/Playwright route failure: capture namespace routes, process confinement and probe/network error provenance, and use no network-facing relay/broker. Include explicit controlled test of proxy env path still denied by namespace despite scrub; fail if proxies accidentally provide egress. No War Room assets, rankings, dataset, provider or user account loaded. Include fail-closed abort if worker doesn't control page, process escapes, an unexpected network succeeds or isolation/cleanup unproven.

Finish by killing descendant processes, verifying no residual browser/server/proxy, deleting fixture/profile and network namespace, retaining sanitized auditable exact-head job/run IDs, probes and cleanup receipts. No CI artifact containing environment values/cookies/credentials. Test this static implementation only with syntax/dormant gating tests safe under FIRST publication; never manually execute the namespace/network probe in Builder lane.

## Audit and activation sequence (separate roles and immutable target)

1. Builder implements ONLY exact 5 files on assigned branch; publishes one DRAFT Builder PR; BEFORE label, actual final-Builder-head applicable FULL CI and Governance SUCCESS; pilot job demonstrably SKIPPED on ordinary PR opened/synchronize. Builder returns frozen PR/head/5-file diff/CI and stops writes.
2. Manager separately activates fresh distinct Independent Auditor task/branch from then-current canonical main. Auditor reviews exact immutable Builder PR/head, event and job predicate as ACTUAL GitHub executable trigger, privilege, checkout, only inert assets, preflight probes, namespace/worker/proxy scope, failure cleanup, FULL CI scope and before-review SKIP; submits independent report on Auditor-only paths/PR with exact-head Governance. Any finding returns to Builder on same unmerged PR with a new freeze/re-audit; no label/test in this path.
3. ONLY after separate Manager acceptance of PASS-family on unchanged Builder target: Manager independently rechecks scope/head and that preflight label is ABSENT; explicitly approves ONE inert pilot activation using that label. Verify the exact target SHA of the actual `pull_request:labeled` run; inspect actual job logs and all safety receipts, and stop on any movement/missing/failed gate. If mechanism cannot trigger safely, classify `NO_QUALIFIED_RUNNER` and defer.
4. After a real inert pilot, require fresh distinct Independent Auditor/QA on immutable executed evidence and independent Manager disposition before considering CI change integration or any later WR-148 experiment. A failing pilot is a one-pass stop: do NOT relabel/retry or broaden CI without a new explicit Manager decision; preferred response is defer local surrogate with production recovery limitation preserved.
5. A successful inert isolation PASS on disposable GitHub-hosted runner ONLY qualifies a FUTURE separately authorized nonproduction WR-148 local fixture A→B→A task. It does NOT itself certify War Room browser/service-worker production traffic, actual public Pages build/artifact/deployment/CDN, production rollback/visitor convergence, formal A6 or release.
6. Since these three files are executable/workflow changes, a guarded Builder merge, if later approved, additionally requires exact-HEAD applicable FULL and genuine same-landed-SHA canonical-main PUSH FULL product SUCCESS. Do not substitute Governance-only, label pilot run or later unrelated SHA.

## Terminal failure/permission limits

If no credible safe dormant trigger can be implemented without executing unreviewed code, or sandbox cannot be sealed, STOP and report BLOCKED rather than authorizing a normal PR job run. No direct host/WSL firewall edit, hosted runner provision outside the one reviewed pilot, Github Pages mutation, production rollback, live ESPN/provider, 2027 source, Track B/A6/release. Do not self-audit, self-label, merge own PR, start pilot, use Work mode without user choice, or promise unattended future results.

## Worker publication

Return one frozen DRAFT PR with five cumulative paths and exact Builder head, actual FULL CI run/job results, verification that pilot job SKIPPED before audit, what remains untested, threat-model caveats and exact fresh independent Auditor handoff. If first-PR FULL/CI cannot be obtained, return BLOCKED with immutable receipts and do not label or merge.


## WR-D078 — Manager exact-source freeze and distinct security audit activation (2026-09-23)

Manager-frozen Builder PR #422 exact DRAFT/OPEN/UNMERGED head `fc6bbbf292462224dd108ebb8909daea149df44c` (10 commits ahead/0 behind canonical Builder baseline `55e3de8068c974870cd245811302741d322ba4ae`), five cumulative authorized files only; freeze comment #5806008717, activation label `wr151-inert-preflight-reviewed` ABSENT. Exact-final-head PR War Room CI #35943519728 SUCCESS, classify #107456373576 SUCCESS (FULL), Governance #107456399595 SUCCESS with decoded PASS/errors []/AUDIT_HEAD_SHA exact, product #107456432505 SUCCESS, pilot #107456374579 SKIPPED, bootstrap #107456400259 SKIPPED. Initial DRAFT PR opened run #35943280524 on historical earlier head aa8aa29fd24b939b5081c054a32919e84f820e86 SUCCESS including pilot #107455664008 SKIPPED, product #107455735145 SUCCESS; this earlier head is NOT the audit target.

Builder is FROZEN on PR #422 exact `fc6bbbf292462224dd108ebb8909daea149df44c` while WR-152 fresh independent Auditor challenges actual executable YAML/namespace/browser/SW/IPC/proxy/negative tests and first-trigger safety. No pilot executed. Do not add `wr151-inert-preflight-reviewed` label, run pilot, merge, claim network isolation, attempt A→B→A, touch WSL runner, public Pages/provider or authorize A6/release. A later Manager decision must separately accept fresh PASS-family on unchanged target and explicitly authorize one inert label-triggered pilot; failing/changed source requires remediation and new audit.


## WR-D079 — fresh independent WR-152 FAIL accepted, bounded SAME-TASK remediation (2026-09-23)

Manager independently ACCEPTED WR-152 independent FAIL — REMEDIATION REQUIRED on exact Auditor PR #424 head f0d86169c6086734262983d6b4ced729103600ff, Manager disposition comment #5806149599; exact-head PR War Room CI #35945267502 SUCCESS / Governance #107461734747 SUCCESS / product #107461901337 SKIPPED, distinct exact-head push CI #35945227324 SUCCESS / Governance #107461620621 SUCCESS. This is Auditor publication evidence, NOT egress-isolation PASS. Immutable FAILED frozen Builder PR #422 head fc6bbbf292462224dd108ebb8909daea149df44c, DRAFT/OPEN/UNMERGED, label wr151-inert-preflight-reviewed ABSENT; source-level blockers WR152-H01 HIGH (untrusted event-derived head/privileged label trigger), WR152-H02 HIGH (incomplete host IPC exclusion), WR152-M01 MEDIUM (cleanup PID enumeration errors masked), WR152-M02 MEDIUM (controlling SW CDP target not bound to actual OS PID/complete process lifetime). Historic Builder exact-head FULL War Room CI #35943519728 SUCCESS incl Governance #107456399595 and product #107456432505, dedicated pilot #107456374579 SKIPPED; no inert pilot or A→B→A ran.

**Same-task Builder remediation authorization (conditional on WR-D079 activation gates):** On existing `builder/wr-151-inert-hosted-runner-loopback-preflight` / existing DRAFT PR #422, remediate ONLY H01/H02/M01/M02 and their minimal deterministic negative controls, preserving cumulative EXACT FIVE allowed paths from this task. Do not rewrite existing product jobs or app assets, open new Builder PR, create new resource or treat historical FULL CI as pilot evidence. The canonical main may advance by this Manager control-plane documentation merge while the original Builder branch retains its original base; do NOT silently rebase/merge main into Builder branch. Read current main/active registry/target PR first and assess nonoverlapping Manager-only commit and original 5-path source diff; stop if target changes unexpectedly before authorized write. After remediation freeze a NEW final Builder SHA on SAME PR #422, exact-head genuine FULL CI including Governance and product SUCCESS and dedicated pilot SKIPPED on sync, then separate Manager NEW freeze and FRESH independent re-audit. WR-152 FAIL is historical, never retroactively changed. No label, execution or integration of PR #422.

**H01 security requirement:** A mutable PR event SHA checked against itself, a label alone, Manager prose, a mutable YAML embedded SHA, or a labeler reapplying a trigger is NOT a trusted exact-review/once-only privileged execution gate. Derive true authority from reviewed immutable source OUTSIDE untrusted Builder-head control; prevent actor/head/race/relabel bypass. If this cannot be implemented and independently reasoned about within five paths, STOP and return `NO_QUALIFIED_RUNNER` / defer surrogate rather than inventing trusted authority or adding paths without separate Manager scope approval.

**H02 security requirement:** A partial socket scan and same-host /proc filesystem are not complete IPC egress exclusion. Require independently enforceable host process/filesystem/FD/Unix broker isolation with fail-closed negative controls; if impossible on hosted runner within approved scope, STOP `NO_QUALIFIED_RUNNER`, do NOT relax to browser interception or user's persistent WSL host.

**M01/M02:** PID enumeration failures must be distinct from genuinely empty verified process sets, including teardown injection controls; deleted namespace name ≠ dead processes. Prove actual controlling CDP SW ↔ defensible OS process mapping and all browser/worker descendants across probes/teardown, or return UNVERIFIED/NO_QUALIFIED_RUNNER; do not accept name regex as linkage. Tests must stay inert/never load War Room or provider content.

No activation label, inert pilot, namespace execution, public Pages, production, user WSL runner, ESPN/provider, ranking/2027 data, Track B, A6 or release authorization. The Auditor PR #424 remains OPEN/DRAFT/UNMERGED as historical independent FAIL; no Auditor/Builder PR merge.


## WR-D080 — Terminal Manager disposition of current runner qualification (2026-09-23)

WR-D080 Manager decision (2026-09-23): NO_QUALIFIED_RUNNER — attempted hosted runner inert loopback-egress qualification stopped. Binding hold on Builder PR #422 original failed head fc6bbbf292462224dd108ebb8909daea149df44c remains DRAFT/OPEN/UNMERGED/UNLABELED, recorded PR comment #5806276024; WR-152 independent FAIL Auditor PR #424 head f0d86169c6086734262983d6b4ced729103600ff remains DRAFT/OPEN/UNMERGED. No inert pilot, browser/service-worker egress-isolation PASS, local A→B→A recovery execution or production recovery proof exists. All WR152-H01/H02/M01/M02 findings remain unresolved on failed implementation; ordinary FULL CI/Governance do not resolve them. This decision supersedes the earlier WR-D079 conditional WR-151 same-task remediation authorization. Local surrogate is DEFERRED. WR-149 recovery is UNPROVEN. Any renewed design/implementation requires a separate Manager authorization with trusted immutable reviewed-head and one-time execution authority outside mutable/unreviewed PR control and a defensible independently verifiable complete host-process/FD/IPC/Unix-broker network isolation boundary; no preflight label or run is authorized by this note. The public Pages/production rollback, ESPN/provider, 2027 sources, Track B, formal A6 and release gates remain held/unmodified; later A6 must explicitly assess the recovery-evidence limitation.

**No further WR-151 Builder writes are assigned.** Historical frozen source PR #422 at fc6bbbf292462224dd108ebb8909daea149df44c and its unresolved WR-152 FAIL are preserved without a new Builder head. No egress isolation, restoration or release claim transfers from ordinary CI. No approval to label, run or merge the pilot. Future revival requires a NEW separately scoped/authorized security design with independent review; this CLOSED task and WR-D079 conditional remediation language do not authorize resumption.
