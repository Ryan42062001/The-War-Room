# WR-060 — Independent Re-Audit of v2 Source Snapshot + Cohort Evidence

TASK ID: WR-060
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-14
DEPENDENCY: HARD — WR-059 must publish one immutable remediation target
EXECUTION MODE: WORK_MODE_PREFERRED
TARGET BRANCH: `wr-060-v2-source-snapshot-cohort-reaudit`
TARGET PR/HEAD: TO BE SET AT ACTIVATION
PRODUCTION AUTHORIZATION: NONE

## Objective

Freshly and independently determine whether WR-059 fully remediates WR-043 findings `WR-043-AUD-01` and `WR-043-AUD-02` while preserving the already-accepted exact 15-source custody identities and all governance boundaries.

## Activation condition

Remain BLOCKED until Manager freezes one immutable WR-059 PR/head. Manager must populate exact audit-target metadata before substantive audit execution.

## Required audit scope

At minimum independently verify:

1. every admitted source instance in the remediated source snapshot is bound to the exact retained/reference identity from the historical 15-source custody set or is explicitly failed closed;
2. versioned `source_snapshot_id` and canonical source-snapshot SHA-256 reproduce deterministically;
3. provider/release/acquisition identity, acquisition UTC, exact SHA/size, retained-object identity, rights/retention disposition, and acquisition-code identity are complete and evidence-backed;
4. ordered raw schema, schema hash, row count, exact approved columns, cutoff/availability semantics, mutability/as-of status, and cutoff-safety evidence reproduce from exact retained/reference bytes;
5. `draft_picks.csv` remains excluded with no silent substitute provider;
6. versioned `cohort_version` exists and the stable row key is `(target_season, player_id_namespace, player_id, position, cohort_version)`;
7. ordered cohort keys, source eligibility/availability, inclusion/exclusion reasons, and lineage are complete and deterministic;
8. duplicate-key count is zero and duplicate rejection is fail-closed;
9. cohort canonical digest reproduces and is explicitly bound to the audited source snapshot ID/hash;
10. no 2026 regular-season outcomes, target/outcome joins, model fitting/scoring/tuning/comparison/evaluation, ranking/production change, or Phase-6 work occurred;
11. historical B2/R2 custody evidence remains applicable to the exact identities and was not silently replaced or weakened.

## Fail-closed rules

Return a blocking finding if any required source-snapshot field, cutoff proof, lineage link, canonical hash, cohort key, source-eligibility reason, deterministic regeneration proof, or retained/reference identity is missing or non-reproducible.

Do not accept hash-only evidence in place of independently verifiable source/cohort evidence.

## Write scope

Write only `.ai/auditor/**`.

## Publication contract

Publish a fresh task-specific audit report, updated Auditor handoff, immutable audit branch/head, and Auditor-only PR.

Return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

A PASS-family verdict returns control to Manager for the next versioned contract/feature-schema governance decision only. It does not authorize model fitting/scoring/evaluation or production changes.
