# WR-125 — Draft-Day Command Center Existing-UX and Duplication Gap Inventory

TASK ID: WR-125
ROLE: Implementation Engineer / Builder — READ-ONLY DOCUMENTARY INVENTORY ONLY, not product implementation
STATUS: ASSIGNED
DATE: 2026-09-20
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — accepted Track A3 / WR-D039 planning
EXECUTION MODE: STANDARD_CHAT_HIGH
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-125-draft-command-center-current-state-inventory`
ASSIGNED BRANCH: `wr-125-draft-command-center-current-state-inventory`
PRE-ACTIVATION MAIN CHECKPOINT: `a1d9bb3236fd08e54ebec06d4ae29e6ea9f51885`
ACTIVATION: Manager must first merge WR-D039 PR, verify genuine canonical-main push Governance SUCCESS, create this branch at that exact new main and verify branch SHA. The preactivation checkpoint is NOT expected initial worker branch SHA.
AUDIT REQUIRED: NO — paper-only, with later product changes separately scoped/audited.

## User goal and already proven source baseline
User specifically wants a Command Center plan that DOES NOT duplicate existing UI. Manager already read current code and named tests at `a1d9bb3236fd08e54ebec06d4ae29e6ea9f51885`: existing Waiting/Near/On-the-Clock command bar, best-overall/by-position/next-pick strip, position tiers/Overall, source-aware recommendation+expanded scored alternative, My Draft roster needs/lineup, session-scoped Targets queue and What Changed alerts, 320–430px phone position tabs/top players/Show More, ESPN status/freshness header, Taken/Mine and session recovery. Do not recommend new dashboard, queue, roster system, mobile tabs, source/ranking/recommendation engine, or generic layout overhaul. WR-016 `layout-efficiency.css` already replaces historical sticky-stack advice with non-sticky coordinated shell plus urgent on-clock reveal; treat earlier `.ai/research/LAYOUT_EFFICIENCY_DISCOVERY.md` as historical hypothesis, not current implementation requirement. `draft-polish.css` intentionally hides duplicate legacy cards in Position view.

## Bounded evidence questions
1. Compare real on-clock candidate content: `js/war-room-command-bar.js:getDecisionAlternatives` currently derives up to TWO name-only ALT entries from best-by-position decision cards, while `js/war-room-rankings.js:renderCompactRecommendationCard` already provides ONE scored alternative/score gap. Verify actual reachable desktop/phone behavior and whether a matched 2–3 choice explanation using EXISTING ECR/tier/roster-fit/market provenance is genuinely absent or redundant. Never invent a player winner, score, weight, threshold, survival probability or separate shortlist authority.
2. Truthfulness: `js/war-room-ui.js:updateDraftDayDashboard` formats heuristic `calculateNextPickSurvival` as `% next-pick survival`; `draft-polish.css` hides legacy Board Pressure in default Position view. Inspect real Overall/alternate-mode reachability and missing-market neutral-50 behavior; distinguish exact source-path concern from actually reproduced browser output. WR-122 fixed only its in-scope recommendation card. If no runtime observation possible, say UNKNOWN and specify a minimal test.
3. Verify existing `#espn-sync-status`/`#data-freshness` visibility/reachability during On the Clock, disconnected/catching up/unresolved, and manual fallback without implying independently validated structured Direct. `js/war-room-command-bar.js` does not render a separate sync badge; assess whether a conditional attention cue would be useful rather than duplicate healthy header content.
4. Verify current My Draft needs, Targets/What Changed, top decision strip, Position/Overall and phone controls in COMBINED synthetic Waiting→Near→On-Clock/target taken/tier closing/sync attention/manual Taken-Mine contexts at 390×844 and 1280×900 (375×812 if feasible), including safe keyboard/focus, primary choice reachability and exact unchanged recommendation player/action/source. Do not infer browser fail from static layout or outdated R&D report.
5. Map current code and actual named TEST evidence to PRESENT_AND_TESTED, PRESENT_NOT_COMBINED_TESTED, SOURCE_RISK_NOT_REPRODUCED, UNKNOWN or MISSING. Read `scripts/test-command-bar.mjs`, `scripts/test-draft-awareness.mjs`, `scripts/test-phone-decision-view-final.mjs`, `scripts/test-layout-efficiency-behavior.mjs`, `scripts/test-browser.mjs`, `scripts/test-espn-sync-trust-ux.mjs`, WR-121 and WR-D001/018/027/038. Report any test actually executed with exact result; inability to run a browser is NOT PASS.
6. Return ONE smallest future code/test-only Manager scope with acceptance cases and exact proposed paths, or recommend NO PRODUCT PATCH if already sufficient. A later approved UI change requires new Builder task, independent Auditor and exact canonical-main FULL CI; this task never authorizes it.

## Exact writes and boundaries
Write ONLY `.ai/builder/WR125_DRAFT_COMMAND_CENTER_UX_INVENTORY.md` and `.ai/builder/HANDOFF.md` on the assigned branch. Open ONE separate OPEN/UNMERGED two-file documentation PR, verify immutable final SHA, changed paths and exact-head Governance CI; product tests may be skipped under docs-only classification. READ current application/CSS/fixtures and available synthetic outputs only. NO product/CSS/index/scripts/test/package/workflow/runner/dataset/Companion or Manager/shared/other-role file edits; no user ESPN login/real room/provider contact/source ingestion/custom model/ranking-policy/scoring/deployment/release or activation. Preserve WR-D001 FantasyPros expert PPR ECR VALUE vs ESPN TIMING, WR-D018 fallback-first and independent Direct unverified, WR-D027 no-provider-contact and completed WR-D038 truthfulness repair. Manager decides separately whether any production task follows.
