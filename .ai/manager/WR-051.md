# WR-051 — Workflow V3.1 Efficiency & State-Integrity Upgrade

TASK ID: WR-051
ROLE: Manager / Architect
STATUS: AUDIT_READY
DATE: 2026-09-12
DEPENDENCY: INDEPENDENT
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `manager/wr-051-workflow-v31-refresh`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-052
POST-MERGE CANARY REQUIRED: YES
REFRESH BASELINE: `2e13dcaa85c5daa15f570f23ca7df184c45ec634`
SUPERSEDES STALE CANDIDATE: PR #144 / `b56f9b98a530600e4b0c20ba1c34edcd99e4a232`

## Objective
Refresh and integrate The War Room Workflow V3.1 onto current canonical main without regressing later accepted custody, browser/persistence, audit, or repository-state evidence.

The original V3.1 implementation remains historical candidate evidence but was never independently audited or merged. Its branch drifted behind current main. This task therefore creates one new current-main-based immutable audit target.

## Required upgrades
1. Same-role task-scoped concurrency for independent work.
2. Machine-readable blocker typing and `user_action_required`.
3. Auditor self-publication: report + handoff + immutable head + audit PR before `COMPLETE`.
4. Path-aware CI: always-on governance CI; full product CI only for non-`.ai/**` changes or explicit force.
5. Machine state-integrity checking.
6. Active-only `ACTIVE_TASKS.json`.
7. External/provider authority evidence contract.
8. Post-merge canonical-main canary for audited shared infrastructure/test-harness changes.
9. Strong atomic Manager reconciliation requirement.

## Refresh-specific requirements
- Reuse the previously tested V3.1 workflow/role/CI/helper-script implementation byte-for-byte unless current main proves a compatibility reason not to.
- Regenerate Manager-owned active state from current canonical truth.
- Preserve accepted WR-048/049 browser/persistence integration.
- Preserve WR-053 PASS and accepted WR-046 custody capability integration.
- Preserve historical WR-047 / WR-050 FAIL evidence rather than rewriting it.
- Remove historical CLOSED tasks from the active fast-path registry.
- Mark WR-042 as a fresh bounded source-custody retry on a new branch, while keeping historical blocker PR #133 immutable.
- Keep WR-043 blocked until a real admitted custody target exists.

## Authorized writes
- `.ai/shared/**`
- `.ai/manager/**`
- `.ai/roles/MANAGER.md`
- `.ai/roles/AUDITOR.md`
- `.github/workflows/ci.yml`
- `scripts/workflow-*.mjs`

## Forbidden
- production/user-facing code;
- ranking/model logic/data;
- `.ai/research/**`;
- `.ai/auditor/**`;
- `.ai/work_helper/**`;
- frozen Returning-Player evidence semantics;
- custody contents/secrets.

## Acceptance
- Workflow status is V3.1 and preserves role/governance separation.
- Active registry contains only current work.
- Current custody/browser integration state is accurately represented.
- Blocker metadata distinguishes user action from upstream/technical/audit blocks.
- `workflow-state-check` catches malformed/contradictory active control-plane state.
- `.ai/**`-only PRs run governance checks without full browser/product CI unless `force-full-ci`.
- scripts/workflows/product changes run full CI.
- push-to-main infrastructure/test changes run the full post-merge canary.
- Auditor cannot report COMPLETE without publishing its own audit PR.
- external authority evidence is required when provider state affects acceptance.
- one atomic refreshed implementation head is independently audited by WR-052.

## Validation
Full CI is required on the refreshed WR-051 PR because it changes `scripts/**` and `.github/workflows/**`. WR-052 independently audits the exact refreshed head. After PASS-family + Manager merge, the canonical-main push/full-CI canary is required before final CLOSED reconciliation.
