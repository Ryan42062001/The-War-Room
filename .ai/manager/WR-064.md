# WR-064 — Independent Audit of Retained-Version Read Recovery

TASK ID: WR-064
ROLE: Independent Auditor / QA
STATUS: BLOCKED
DATE: 2026-09-14
DEPENDENCY: HARD — WR-063 must publish one immutable successful implementation/live-proof target
EXECUTION MODE: WORK_MODE_PREFERRED
TARGET BRANCH: `wr-064-retained-object-version-read-audit`
TARGET PR/HEAD: TO BE SET AT ACTIVATION
PRODUCTION AUTHORIZATION: NONE

## Objective

Freshly and independently determine whether WR-063 reconciles the WR-061 identity/state failure and establishes a genuinely non-mutating, version-aware retained-object read path that is safe to integrate before WR-059 resumes.

## Activation condition

Remain BLOCKED until Manager freezes one exact WR-063 PR/head plus successful protected live-proof run/job evidence.

Do not audit historical WR-061 PR #176 as a passing target.

## Required independent audit scope

Verify at minimum:

1. the four hard-coded source identities exactly match the executed WR-042 manifest/result for 2013–2016;
2. the mistaken WR-061 2014–2016 values are absent from the accepted allowlist and rejected by tests;
3. B2 retained-version discovery is read-only, limited to the full exact custody key under investigation, and cannot broaden to arbitrary bucket/prefix enumeration;
4. candidate B2 versions require exact file-name equality and valid upload/content state before version-addressed download;
5. the exact retained B2 version selected for every season reproduces authoritative SHA-256 and byte size;
6. the 2013 by-name HTTP 404 is reconciled by privacy-safe provider-issued version/state evidence rather than assumption;
7. exact R2 reads reproduce the same authoritative SHA-256/size and equal B2 bytes;
8. no PUT/upload/copy/delete/overwrite/metadata/tag/retention/Legal-Hold/Bucket-Lock mutation path is reachable;
9. existing mutation-capable custody helpers/workflows are neither modified nor invoked;
10. existing secret set only; secrets are not exposed and are absent from later consumer code;
11. raw bytes remain runner-temporary, are cleaned on success/failure, and are absent from commits/logs/Actions artifacts;
12. no upstream source-byte refresh or provider substitution occurred;
13. focused regressions and full required CI are green, including WR-056 trusted-custody Governance behavior;
14. no 2026 outcome/model/scoring/ranking/production/Phase-6 boundary changed.

Code assertions alone are insufficient for provider-state/live-byte claims; independently inspect protected live evidence.

## Write scope

Write only `.ai/auditor/**`.

## Publication contract

Publish a fresh WR-064 audit report, updated Auditor handoff, immutable audit branch/head, and Auditor-only PR.

Return exactly one verdict:

- `PASS`
- `PASS WITH NON-BLOCKING FINDINGS`
- `FAIL — REMEDIATION REQUIRED`

PASS-family authorizes only Manager integration of the exact audited WR-063 head followed by its mandatory canonical-main post-merge canary. WR-059 remains blocked until that canary is accepted.
