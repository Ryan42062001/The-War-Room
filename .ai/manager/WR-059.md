# WR-059 — Returning-Player v2 Source-Snapshot + Cohort Evidence Remediation

TASK ID: WR-059
ROLE: Research & Development (R&D)
STATUS: REWORK_REQUIRED — BOUNDED METADATA PROVENANCE
DATE: 2026-09-15
DEPENDENCY: HARD — WR-060 `FAIL — REMEDIATION REQUIRED`
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-059-v2-source-snapshot-cohort-remediation-2`
TARGET PR: `#196`
LAST AUDITED HEAD: `e871c861f8ba3c339af5b7a022892522b45b844f`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — fresh WR-071 re-audit after remediation
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## WR-060 audit disposition

WR-060 independently audited exact PR #196/head `e871c861f8ba3c339af5b7a022892522b45b844f`.

Final verdict:

`FAIL — REMEDIATION REQUIRED`

Audit PR/head:

- PR `#198`;
- head `5ae432ca8e7c32dad56701a9792cb55d59150611`;
- audit evidence merge `acf599e31ad8638f8e1ba399ea2e4328e5ff7bd0`.

Blocking finding:

`WR-060-AUD-01 — HIGH — admitted replacement players.csv provenance lacks independently reproducible exact release ID and full provider-update timestamp required by the frozen WR-039 source-instance contract.`

The failed audited head remains immutable historical evidence. Do not present its `1.1.0-wr059` source-snapshot/cohort artifacts as accepted.

## What already passed independently

Preserve, do not redo:

- all 15 expanded WR-059 source identities match historical WR-042 source class, asset/name, season, SHA-256, and byte size;
- historical WR-042 manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- accepted WR-069 evidence SHA-256 `448baab9b5b3109faee3e322432369f72dcef1569685a16b0605ce25bd855fbb`;
- frozen source-snapshot bytes independently hash to `f6ee530c7733b1aa9d984a3e874015ca9a3dd7cebda5ffc558df4cb159c591b9`;
- frozen cohort bytes independently hash to `d6927509968ac3a371591a69fd665861e9546a0868f1e7b1c5db6c31cc887354`;
- cohort expansion is 5,176 keys / 5,176 unique / zero duplicates;
- 2014–2017 = 410/412/423/423 = 1,668;
- 2018–2025 = 419/444/437/435/475/446/421/431 = 3,508;
- exact lineage to prior-season admitted stats source instance for every target-season segment;
- historical membership does not depend on current `players.csv`;
- `draft_picks.csv` remains excluded under WR-057;
- no provider/raw-byte operation or model/scoring/ranking/production/Phase-6 work occurred.

## Exact blocking evidence

Replacement metadata source:

- source class `NFLVERSE_PLAYERS_METADATA_MINIMAL`;
- asset ID `563580371`;
- exact SHA-256 `03a823a0e2344aff9a4ef67bdd62005d3e1d7ab62a98c790ce19a8a557d1c221`;
- exact byte size `7260242`.

The failed snapshot used a narrative placeholder for `release_id` instead of an exact provider identifier and represented `provider_updated_at` only as date `2026-09-14` with day precision.

WR-039 requires the exact release identity fields for every admitted source instance. WR-060 also made a fresh read-only request to the exact release-asset endpoint for asset `563580371`; it returned `404 Not Found`. Current provider state therefore cannot be silently treated as historical authority.

## Bounded remediation objective

Do not broaden WR-059. Resolve only `WR-060-AUD-01`.

Choose exactly one evidence-valid path:

### Path A — authoritative provenance recovery

Find privacy-safe authoritative provider/repository evidence that independently binds to exact asset `563580371` and supplies:

- exact provider-issued release ID;
- full provider-update timestamp.

The evidence must be independently reproducible and must not rely on inference from the known day, asset name, a replacement asset, or current mutable provider state that cannot be bound to the historical asset.

### Path B — fail closed metadata source

If exact provenance cannot be established, mark this metadata source failed closed/unavailable rather than admitted.

Then consistently regenerate:

- source-snapshot admission/failure totals;
- metadata availability/admission semantics;
- canonical source-snapshot artifact and sidecar;
- cohort artifact's exact source-snapshot ID/hash binding and sidecar.

Historical cohort membership itself must remain unchanged because current `players.csv` was never its authority.

## Versioning rule

The frozen failed identifiers:

- `wr-returning-player-v2-source-snapshot/1.1.0-wr059`;
- `returning-player-v2-cohort/1.1.0-wr059`

are immutable historical failed-audit evidence.

If canonical bytes change, use new versioned identities for both the source snapshot and bound cohort artifact and publish new deterministic hashes. Do not reuse the old version labels for changed content.

## Allowed investigation

Read-only provider/repository metadata research is allowed.

Do not:

- reacquire retained raw bytes;
- refresh or substitute source data;
- mutate provider state;
- use another asset/provider as if equivalent;
- infer a sub-day timestamp from `2026-09-14`;
- weaken or amend WR-039 to make the failed evidence pass.

If authoritative provenance remains unavailable, Path B is the required fail-closed disposition.

## Publication contract

Update only `.ai/research/**` on existing PR #196.

Return to Manager with:

- exact new PR #196 head;
- exact changed-file scope;
- chosen remediation path and evidence;
- new source-snapshot ID/hash;
- new cohort version/hash;
- source admission/failure totals;
- confirmation the 5,176-key cohort remains unchanged unless independently justified;
- exact validation commands/results;
- exact-head CI.

R&D does not activate WR-071.

## Next gate

R&D publishes one new immutable bounded-remediation head -> Manager independently verifies/freezes exact PR/head and artifact hashes -> WR-071 fresh independent audit.

## Boundaries

No 2026 regular-season outcome tables, targets, target/outcome joins, model fitting/scoring/tuning/comparison/evaluation, predictions, rankings, production changes, provider mutation, upstream source reacquisition, `draft_picks.csv` use/replacement, or Phase-6 work.
