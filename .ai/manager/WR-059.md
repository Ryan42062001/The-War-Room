# WR-059 — Returning-Player v2 Source-Snapshot + Cohort Evidence Remediation

TASK ID: WR-059
ROLE: Research & Development (R&D)
STATUS: BLOCKED
DATE: 2026-09-15
DEPENDENCY: HARD — WR-069/WR-070 safe-consumer parser gate and mandatory main canary must be accepted first
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-059-v2-source-snapshot-cohort-remediation-2`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-060
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Objective

Remediate only the two HIGH WR-043 findings without redoing or weakening accepted raw custody. Preserve the exact 15 already-custodied WR-042 byte identities and ultimately produce the complete no-scoring source-snapshot and deterministic cohort/source-eligibility evidence required by the accepted Returning-Player v2 contract.

## Frozen fail-closed checkpoints

WR-059 historical checkpoint:

- PR `#184`;
- exact head `3c02f5a9a3ea858235772e7f2d065604632e7ee7`;
- exact-head War Room CI `34911364983` — SUCCESS;
- disposition `CLOSED UNMERGED — FAIL-CLOSED EVIDENCE CHECKPOINT`.

This checkpoint preserves 15 exact custody identities, 3,508 ordered cohort keys, and 1,668 missing 2014–2017 TRAIN_ONLY keys. It is not a passing WR-059 target and must not activate WR-060.

WR-065 parser checkpoint:

- PR `#186`;
- exact head `d4e5ddeaf9f3b0d56846145f8dc3ffe9cf48df7f`;
- exact-head War Room CI `34913200700` — SUCCESS;
- disposition `FAIL CLOSED — ACCEPTED CONTRACT DID NOT YET DEFINE EXECUTABLE CSV TYPE/NULLABILITY INFERENCE`.

WR-065 stopped before provider access. No B2/R2 reads, provider mutations, upstream access, raw-byte materialization, consumer execution, or raw-byte artifacts occurred.

## Accepted contract clarification

The semantic blocker exposed by WR-065 is resolved by accepted WR-067/WR-068:

- WR-067 PR #188 exact audited head `1e6b2105bce98408d3fb41f4aa07fcaa7ef6ca04`;
- contract ID/version `wr-returning-player-v2-csv-schema-inference-addendum/1.0.0`;
- machine-lock SHA-256 `48d4ace7375a59ab28ad79b2777bd7de4a9c4871cea49e83447371131f60dddb`;
- conformance-corpus SHA-256 `1f70d5e31ed5a62e00e5b02e06f6271e9c30b36951389d9607c50fdf5f71ffe1`;
- WR-068 audit PR #190 exact audit head `a5ccce36012a5cf06d93bccf99d1834eaa268142`;
- WR-068 verdict `PASS`, no findings, 49/49 synthetic cases independently reproduced;
- audit evidence merge `07c89a4560d11e2a53538ca195ccd430af7cd905`;
- exact audited contract integration `abff69901a040bec378c6562845adde9780f1da5`;
- canonical-main Governance run `34919117910` — SUCCESS.

The accepted clarification resolves only deterministic raw CSV typed-schema inference. It does not itself authorize WR-059 to parse retained bytes.

## Current blocker

WR-059 remains blocked until a separately reviewed parser implementation exists. Manager has therefore assigned WR-069 and pre-created WR-070.

Required sequence before WR-059 can resume:

1. WR-069 implements the fresh safe-consumer retained-evidence parser bridge against the accepted WR-067 contract and publishes a successful protected 15-object live proof;
2. Manager freezes the exact WR-069 PR/head;
3. WR-070 independently audits that exact implementation and proof;
4. Manager integrates only the exact audited WR-069 target after PASS-family audit; and
5. the mandatory canonical-main canary passes.

Do not resume WR-059 directly from the accepted contract. The parser gate remains mandatory.

## Immutable identity boundary

Historical custody remains bound to:

- WR-042 evidence head `614445a20c2c15fbc3d8c107644a5244ddb52076`;
- executed manifest commit `cc9005ae4bd9065cf80f1c184f31974904165c54`;
- custody manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- custody run/job `34871882486` / `104069521779`;
- exactly 15 retained source-byte identities.

Do not reacquire, refresh, replace, recustody, or substitute a mutable upstream source. `draft_picks.csv` remains excluded under WR-057 with no replacement provider authorized.

## Required final WR-059 artifacts after later resume

Publish under `.ai/research/**`:

1. one complete versioned source snapshot binding every admitted source instance to exact identity, ordered typed schema/hash, physical row count, approved columns, cutoff/availability, lineage, rights/retention, retained-object identity, acquisition-code identity, admission/exclusion reason, `source_snapshot_id`, and canonical source-snapshot SHA-256; and
2. one deterministic no-scoring cohort/source-eligibility artifact with versioned `cohort_version`, stable key `(target_season, player_id_namespace, player_id, position, cohort_version)`, complete ordered key inventory, eligibility/availability reasons, exact source lineage, duplicates fatal, full coverage, canonical digest, and source-snapshot binding.

## Next gate

WR-069 -> WR-070 -> exact audited integration + mandatory main canary -> Manager reassigns WR-059 on the fresh branch -> complete immutable WR-059 target -> WR-060.

## Boundaries

No 2026 regular-season outcome tables, targets, target/outcome joins, model fitting/scoring/tuning/comparison/evaluation, predictions, rankings, production changes, provider mutation, or Phase-6 work.

## Write scope

When reactivated, write only `.ai/research/**`.
