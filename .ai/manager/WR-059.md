# WR-059 — Returning-Player v2 Source-Snapshot + Cohort Evidence Remediation

TASK ID: WR-059
ROLE: Research & Development (R&D)
STATUS: ASSIGNED
DATE: 2026-09-15
DEPENDENCY: HARD — publish one complete immutable remediation target before WR-060 may activate
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-059-v2-source-snapshot-cohort-remediation-2`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-060
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Objective

Remediate only the two HIGH WR-043 findings without redoing or weakening accepted raw custody:

- `WR-043-AUD-01`: complete WR-039 / WR-D008 source-snapshot contract evidence;
- `WR-043-AUD-02`: deterministic cohort/source-eligibility evidence and complete ordered cohort-key coverage.

Preserve the exact 15 already-custodied WR-042 byte identities and publish the complete no-scoring source-snapshot and deterministic cohort/source-eligibility evidence required by the accepted Returning-Player v2 contract.

## Accepted prerequisite gate — complete

The safe-consumer parser prerequisite is accepted and no longer blocks WR-059:

- WR-070 verdict `PASS`, no CRITICAL/HIGH/MEDIUM/LOW findings;
- audit PR `#194`;
- immutable audit head `434988473daf188f4b4efe3207df40b56977e9fd`;
- audit evidence merge `3436f3803e342fce00e784e67ba7dfdc9ddc9561`;
- exact audited WR-069 head `5d4fc5fce3567a9894ddf3c08243f0ce6c087543`;
- protected implementation `56f6581cd62fd474f4422bc5f7d353f48498a853`;
- protected run/jobs `34922718568` / `104234149528` / `104234179073` — SUCCESS;
- exact audited implementation integration `82ac95d8d85dfe0dff58e387aecdcc49f082ffec`;
- mandatory canonical-main War Room CI canary `34976191415` — SUCCESS.

Accepted privacy-safe derived evidence:

`.ai/work_helper/WR069_RETAINED_DERIVED_EVIDENCE.json`

SHA-256:

`448baab9b5b3109faee3e322432369f72dcef1569685a16b0605ce25bd855fbb`

Accepted facts include exact retained input count `15`, B2 and R2 authoritative digest/size `15/15`, B2/R2 equality `15/15`, provider mutation operations `0`, provider credentials absent from consumer execution, independent consumer re-hash/re-size `15/15`, accepted CSV conformance `49/49`, cleanup PASS, raw Actions artifact count `0`, and historical missing inventories 2014=`410`, 2015=`412`, 2016=`423`, 2017=`423`, total=`1668`.

The current retained `players.csv` was not used to rewrite historical cohort membership.

## Historical fail-closed checkpoints

Historical WR-059 checkpoint:

- PR `#184`;
- exact head `3c02f5a9a3ea858235772e7f2d065604632e7ee7`;
- disposition `CLOSED UNMERGED — FAIL-CLOSED EVIDENCE CHECKPOINT`.

It preserves 15 exact custody identities, 3,508 ordered cohort keys, and 1,668 missing 2014–2017 TRAIN_ONLY keys. It is not a passing target and must not be advanced.

Historical WR-065 checkpoint:

- PR `#186`;
- exact head `d4e5ddeaf9f3b0d56846145f8dc3ffe9cf48df7f`;
- disposition `CLOSED UNMERGED — FAIL-CLOSED EVIDENCE CHECKPOINT`.

It stopped before provider access because deterministic CSV typed/nullability semantics were not yet contractually executable. That blocker is resolved by accepted WR-067/WR-068 and WR-069/WR-070.

## Immutable identity and contract authority

Historical custody remains bound to:

- WR-042 evidence head `614445a20c2c15fbc3d8c107644a5244ddb52076`;
- executed manifest commit `cc9005ae4bd9065cf80f1c184f31974904165c54`;
- custody manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- custody run/job `34871882486` / `104069521779`;
- exactly 15 retained source-byte identities.

Accepted CSV semantics remain bound to:

- `wr-returning-player-v2-csv-schema-inference-addendum/1.0.0`;
- exact accepted WR-067 head `1e6b2105bce98408d3fb41f4aa07fcaa7ef6ca04`;
- machine-lock SHA-256 `48d4ace7375a59ab28ad79b2777bd7de4a9c4871cea49e83447371131f60dddb`;
- conformance-corpus SHA-256 `1f70d5e31ed5a62e00e5b02e06f6271e9c30b36951389d9607c50fdf5f71ffe1`.

Do not reacquire, refresh, replace, recustody, or substitute mutable upstream sources. `draft_picks.csv` remains excluded under WR-057 with no replacement provider authorized.

## Required source-snapshot artifact

Publish one complete versioned source snapshot covering every admitted source instance. It must include at minimum:

- content-addressed `source_instance_id`;
- provider/repository/acquisition identity;
- acquisition UTC;
- release/version/release ID/asset ID/filename/provider-update identity;
- exact SHA-256 and byte size;
- media/compression state;
- ordered raw columns;
- ordered typed/nullability schema;
- canonical schema hash;
- physical row count;
- approved columns;
- cutoff/availability semantics;
- mutability/as-of evidence;
- rights/license/attribution/retention disposition;
- retained-object identity/key;
- acquisition-code identity;
- admission/exclusion reason;
- versioned `source_snapshot_id`;
- deterministic canonical source-snapshot SHA-256.

## Required cohort/source-eligibility artifact

Publish one deterministic no-scoring cohort/source-eligibility artifact containing at minimum:

- versioned `cohort_version`;
- stable key `(target_season, player_id_namespace, player_id, position, cohort_version)`;
- deterministic ordered key inventory;
- source eligibility and availability;
- inclusion/exclusion reasons;
- exact lineage to `source_instance_id` values;
- duplicate detection with duplicates fatal;
- full declared key coverage;
- canonical digest;
- exact source-snapshot ID/hash binding.

The accepted WR-069 historical inventories close the previously missing 1,668 2014–2017 TRAIN_ONLY keys. Do not use current players metadata to rewrite historical membership.

## Execution inputs

Use repository authority, including:

- `.ai/shared/WORKFLOW.md`;
- `.ai/shared/ACTIVE_TASKS.json`;
- `.ai/shared/PROJECT_STATE.md`;
- `.ai/shared/ROADMAP.md`;
- `.ai/shared/DECISIONS.md`;
- `.ai/roles/RND.md`;
- this task spec;
- `.ai/manager/HANDOFF.md`;
- `.ai/research/HANDOFF.md`;
- `.ai/research/RETURNING_PLAYER_V2_EVIDENCE_CONTRACT.md`;
- `.ai/research/RETURNING_PLAYER_V2_SOURCE_RIGHTS_RETENTION.md`;
- `.ai/research/generated/RETURNING_PLAYER_V2_EVIDENCE_CONTRACT.json`;
- `.ai/work_helper/WR069_RETAINED_DERIVED_EVIDENCE.json`;
- only the prior WR-025 / WR-029 / WR-033 / WR-034 / WR-039 / WR-042 / WR-043 / WR-057 evidence needed to establish exact source/cohort semantics.

Do not request provider credentials or retained raw bytes unless Manager explicitly changes scope. The accepted privacy-safe evidence should be sufficient for this remediation.

## Publication contract

R&D must publish one complete immutable WR-059 target under `.ai/research/**`, including:

- human-readable source-snapshot and cohort evidence;
- deterministic machine-readable artifacts and their canonical hashes;
- updated R&D handoff;
- exact validation commands/results actually run;
- one R&D PR from the assigned branch.

When complete, return to Manager with the exact PR number/head plus canonical artifact hashes. R&D does not activate WR-060 itself.

## Next gate

R&D complete immutable WR-059 target -> Manager independent target/live-state verification and exact freeze -> WR-060 fresh independent audit.

## Boundaries

No 2026 regular-season outcome tables, targets, target/outcome joins, model fitting/scoring/tuning/comparison/evaluation, predictions, rankings, production changes, provider mutation, upstream reacquisition, `draft_picks.csv` use/replacement, or Phase-6 work.

## Write scope

Write only `.ai/research/**`.
