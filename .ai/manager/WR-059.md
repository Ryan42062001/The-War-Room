# WR-059 — Returning-Player v2 Source-Snapshot + Cohort Evidence Remediation

TASK ID: WR-059
ROLE: Research & Development (R&D)
STATUS: ASSIGNED
DATE: 2026-09-14
DEPENDENCY: INDEPENDENT
EXECUTION MODE: WORK_MODE_HIGH_VALUE
TARGET BRANCH: `wr-059-v2-source-snapshot-cohort-remediation`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-060
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Objective

Remediate only the two HIGH WR-043 findings without redoing or weakening the accepted raw-custody proof.

Preserve the exact 15 already-custodied byte identities from historical WR-042 PR #168 / head `614445a20c2c15fbc3d8c107644a5244ddb52076` and produce the complete no-scoring evidence package required by WR-039 / WR-D008.

## Governing audit findings

- `WR-043-AUD-01 — HIGH`: the frozen WR-042 checkpoint lacks the complete WR-039 source-snapshot record/manifest.
- `WR-043-AUD-02 — HIGH`: the frozen WR-042 checkpoint lacks a deterministic cohort/source-eligibility manifest and full ordered cohort-key coverage evidence.

The WR-043 FAIL does **not** invalidate the existing B2/R2 custody evidence for the 15 exact byte identities.

## Immutable identity boundary

Use the exact retained/reference objects already proven by WR-042 wherever possible. The historical custody input is bound to:

- manifest commit `cc9005ae4bd9065cf80f1c184f31974904165c54`;
- custody manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- custody run/job `34871882486` / `104069521779`.

Do not silently reacquire, refresh, replace, or substitute a mutable upstream URL/object. If exact retained/reference bytes cannot support a required field, fail closed for that source and report the precise blocker. Do not modify custody runtime/workflows or credential policy inside WR-059.

## Required source-snapshot artifact

Create a versioned canonical source-snapshot artifact under `.ai/research/generated/**` plus a concise research report/handoff. It must bind every admitted source instance to the complete WR-039-required evidence surface, including at minimum:

- content-addressed `source_instance_id`;
- source class and season applicability;
- provider/repository and exact acquisition method/canonical endpoint;
- acquisition UTC;
- release tag/version, release ID, asset ID, asset filename, provider-updated timestamp;
- exact SHA-256 and byte size;
- media type and compression state;
- ordered raw schema and canonical schema SHA-256;
- row count;
- exact approved columns and no unapproved-column expansion;
- target-season cutoff and availability semantics;
- mutability/as-of classification and cutoff-safety evidence;
- license/attribution/upstream-rights/retention disposition;
- project-controlled retained-object identity/URI or content-addressed key plus digest;
- acquisition-code SHA and exact acquisition command/path;
- status/reason for admission/exclusion;
- versioned `source_snapshot_id`;
- canonical source-snapshot SHA-256 over deterministic bytes.

The artifact must explicitly preserve accepted WR-057 exclusion of `draft_picks.csv`; no substitute draft provider is authorized.

## Required deterministic cohort/source-eligibility artifact

Create a no-scoring deterministic artifact under `.ai/research/generated/**` that is bound to the exact source snapshot above and contains:

- versioned `cohort_version`;
- canonical stable row key `(target_season, player_id_namespace, player_id, position, cohort_version)`;
- deterministic ordered key inventory;
- explicit source eligibility/availability and inclusion/exclusion status/reason for every declared key;
- source-lineage references back to the exact source-instance IDs;
- duplicate-key detection with duplicates fatal;
- full declared key coverage and deterministic canonical digest;
- explicit source-snapshot ID/hash binding.

Generate this artifact without 2026 regular-season outcomes, targets, fitted model state, predictions, scoring, tuning, model comparison/evaluation, rankings, or production behavior.

## Required validation

Before publication:

1. verify every source-snapshot record against exact retained/reference bytes and authoritative provider metadata where required;
2. deterministically recompute ordered schema, schema hash, row count, snapshot canonical hash, cohort ordered-key digest, and duplicate count;
3. prove the cohort artifact regenerates deterministically from the exact audited source snapshot and declared no-scoring rules;
4. confirm no source identity changed from the accepted WR-042 custody set unless a source is explicitly failed closed;
5. confirm `draft_picks.csv` remains excluded and no silent provider substitution occurred;
6. confirm no 2026 outcome/model/scoring/production/Phase-6 boundary crossing.

## Write scope

Write only `.ai/research/**`.

Do not modify `.ai/shared/**`, `.ai/manager/**`, `.ai/auditor/**`, `.ai/work_helper/**`, `src/**`, `public/**`, custody workflows/runtime, credentials, rankings, models, or production code.

## Publication contract

Publish one immutable WR-059 PR/head containing the complete source snapshot, deterministic cohort/source-eligibility artifact, supporting report, and updated R&D handoff.

If complete evidence cannot be truthfully produced from the exact retained/reference identities, fail closed and publish the exact blocker evidence rather than weakening the contract.

Return the exact PR/head, source-snapshot ID/hash, cohort version/hash, admitted/excluded source counts, duplicate count, any failed-closed source, and recommended Manager action.

PASS-like completion authorizes only WR-060 independent re-audit. It does not authorize model protocol or scoring.
