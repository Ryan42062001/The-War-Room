# WR-059 — Returning-Player v2 Source-Snapshot + Cohort Evidence Remediation

TASK ID: WR-059
ROLE: Research & Development (R&D)
STATUS: BLOCKED
DATE: 2026-09-14
DEPENDENCY: HARD — WR-065/WR-066 safe-consumer parser gate must be accepted first
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `wr-059-v2-source-snapshot-cohort-remediation-2`
PRODUCTION AUTHORIZATION: NONE
AUDIT REQUIRED: YES — WR-060
EXTERNAL AUTHORITY EVIDENCE REQUIRED: YES

## Objective

Remediate only the two HIGH WR-043 findings without redoing or weakening the accepted raw-custody proof. Preserve the exact 15 already-custodied byte identities from historical WR-042 and ultimately produce the complete no-scoring evidence package required by WR-039 / WR-D008.

## Frozen fail-closed checkpoint

R&D executed the bounded remediation after WR-063/WR-064 acceptance and correctly failed closed.

Historical checkpoint:

- PR: `#184`
- exact immutable head: `3c02f5a9a3ea858235772e7f2d065604632e7ee7`
- exact-head War Room CI: `34911364983` — SUCCESS
- disposition: `FAIL CLOSED — REMEDIATION INCOMPLETE / ESCALATION REQUIRED`
- source snapshot ID: `wr-returning-player-v2-source-snapshot/1.0.0-wr059-fail-closed`
- source snapshot SHA-256: `d062a30d92456e65f27aa82922011ab03b5dcb6fba9da736e9b98492e58ece3d`
- cohort version: `returning-player-v2-cohort/1.0.0-wr059-fail-closed`
- cohort SHA-256: `ba40123b8a09d3d04b6124745ef289d6d13ce41e4e9cedb9c0ea0bc2717f1cc6`

This checkpoint is evidence, not a passing remediation target. It remains closed/unmerged and must not be advanced or reused as the final WR-059 branch.

Observed fail-closed state:

- accepted WR-042 raw custody preserved: 15 exact identities;
- source-snapshot admitted: 0;
- source-snapshot rejected: 15;
- historical returning cohort expected: 5,176 keys for target seasons 2014–2025;
- exact ordered keys already retained: 3,508;
- missing TRAIN_ONLY ordered keys: 1,668 for 2014–2017;
- duplicate known keys: 0;
- `draft_picks.csv` remained excluded under WR-057.

## Remaining byte-derived evidence gap

PR #184 establishes that the complete final WR-039 package still requires evidence that cannot be invented from durable header/count artifacts:

- ordered raw `(column,type,nullable)` schema and canonical typed-schema hash for the exact retained source bytes;
- physical full-file row count for the exact retained source bytes;
- exact current retained `players.csv` byte-derived parser evidence;
- complete ordered player-ID/position inventories required to reconstruct the 2014–2017 cohort keys.

The accepted WR-063 path proves non-mutating retained-object retrieval and B2/R2 equality, but its reviewed workflow intentionally does not expose verified raw files to an R&D parser after provider-secret isolation.

## Current blocker

WR-059 is BLOCKED while WR-065 / WR-066 establish and independently audit a narrow post-WR-063 safe-consumer parser bridge.

WR-065 may create only the dedicated safe-consumer infrastructure authorized by its Manager task. WR-059 must not modify custody/runtime/workflow surfaces itself.

Resume requires all of the following:

1. WR-065 publishes one immutable implementation/live-proof target;
2. Manager freezes that exact target;
3. WR-066 independently returns a PASS-family verdict on the exact frozen target;
4. Manager integrates only the exact audited WR-065 head; and
5. the mandatory canonical-main post-merge canary passes.

Only then may Manager create/fast-forward the fresh WR-059 branch named above and reassign R&D.

## Immutable identity boundary

Historical custody remains bound to:

- historical WR-042 evidence head `614445a20c2c15fbc3d8c107644a5244ddb52076`;
- executed manifest commit `cc9005ae4bd9065cf80f1c184f31974904165c54`;
- custody manifest SHA-256 `d2196293ff34543b063e737efb175f579967bf37f5dc91b41c08dd45dfe44d26`;
- custody run/job `34871882486` / `104069521779`;
- exactly 15 admitted retained source-byte identities.

Do not silently reacquire, refresh, replace, recustody, or substitute a mutable upstream source.

## Required final WR-059 artifacts after resume

Publish under `.ai/research/**`:

1. a versioned WR-039-compliant source snapshot binding every admitted source instance to exact identity, schema/hash, physical row count, approved columns, cutoff/availability, lineage, rights/retention, retained-object identity, acquisition-code identity, admission/exclusion reason, `source_snapshot_id`, and canonical source-snapshot SHA-256; and
2. a deterministic no-scoring cohort/source-eligibility artifact with versioned `cohort_version`, stable row key `(target_season, player_id_namespace, player_id, position, cohort_version)`, complete ordered key inventory, eligibility/availability reasons, source-instance lineage, duplicates fatal, full coverage, canonical digest, and source-snapshot binding.

`draft_picks.csv` remains excluded under WR-057; no replacement provider is authorized.

## Next gate

After WR-065/WR-066 acceptance and Manager reactivation, R&D publishes one new immutable complete WR-059 target on the fresh branch. Manager then freezes that exact PR/head and activates WR-060 for the mandatory fresh independent re-audit.

Do not activate WR-060 on frozen fail-closed PR #184.

## Boundaries

No 2026 regular-season outcome tables, targets, target/outcome joins, model fitting/scoring/tuning/comparison/evaluation, predictions, rankings, production changes, provider mutation, or Phase-6 work.

## Write scope

When reactivated, write only `.ai/research/**`.
