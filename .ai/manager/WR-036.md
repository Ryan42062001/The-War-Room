# WR-036 — Independent Audit of WR-035 Season-Total Composition Research

TASK ID: WR-036
ROLE: Independent Auditor / QA
STATUS: ASSIGNED
DATE: 2026-09-10
DEPENDENCY: HARD on WR-035 completed research / PR #124
EXECUTION MODE: WORK_MODE_PREFERRED
TARGET PR: #124
TARGET HEAD: `9b4769899dd73f7c94679df6b6c67158e3ee39b6`
PRODUCTION AUTHORIZATION: NONE

## Objective
Independently audit WR-035 and determine whether the proposed Phase-5 disposition `INDEPENDENCE PRODUCT SUPPORTED` is methodologically, reproducibly, and operationally sound enough for Manager acceptance and merge.

Do not assume R&D is correct. Do not modify WR-035 research outputs, production code, canonical Manager/shared state, or the target PR. Auditor evidence may be written only under `.ai/auditor/**` on an audit branch.

## Required startup
Refresh actual `main`, then read:
- `.ai/shared/WORKFLOW.md`;
- `.ai/roles/AUDITOR.md`;
- `.ai/shared/ACTIVE_TASKS.json`;
- `.ai/manager/WR-035.md`;
- this task;
- actual PR #124 / exact diff / commits / CI;
- `.ai/research/HANDOFF.md` from exact target head;
- only WR-033/WR-034 evidence needed to verify upstream replay and boundaries.

Use Full Refresh because this is milestone-completion audit with a live CI discrepancy.

## Manager-review facts to challenge independently
R&D claims:
- protocol was frozen at commit `b9b353e712b297ffaa801141cb87954be622ebd3` before scoring/results;
- exact WR-033 and WR-034 replay succeeded without retuning;
- `INDEPENDENT_PRODUCT` is the selected central transform;
- confirmation MAE is 32.513 versus 41.408 for WR033 × PREV_RATE and 42.709 for schedule-adjusted prior total;
- the paired-residual dependence-aware challenger failed development and had confirmation MAE 34.815;
- repeated-player bootstrap challenger-minus-independence MAE delta is +2.301, 95% CI [+1.806,+2.680];
- paired empirical 80% distribution has 82.7% pooled coverage;
- high-value coverage is weak: 61.0% Q4, 50.0% D10, 64.0% WR Q4;
- deterministic rerun is byte-for-byte reproducible;
- no 2026 outcomes, production changes, or Phase-6 value work occurred.

Verify, do not restate blindly.

## Audit requirements
### A. Scope / chronology / integrity
- Confirm all PR #124 changed files are research-only and permitted.
- Confirm protocol/machine lock/candidate spec were frozen before scoring and that later commits did not silently change gates after results.
- Confirm WR-021/WR-023 frozen artifacts are untouched.
- Confirm no 2026 regular-season outcome or target-season Week-1+ predictor enters the experiment.
- Confirm WR-033 and WR-034 implementations/specifications were replayed, not retuned.

### B. Central composition result
- Verify formulas for `INDEPENDENT_PRODUCT`, baselines, and `PAIRED_RESIDUAL_MEAN`.
- Verify development/confirmation split and that confirmation could not rescue development failure.
- Verify pooled and by-position central metrics from generated artifacts/code where feasible.
- Verify the dependence-aware challenger genuinely failed the predeclared adoption gate and is not unfairly disadvantaged by an implementation error.
- Verify repeated-player-aware bootstrap construction, sign convention, seeds/replicates, and reported CI.

### C. Distribution / calibration
- Verify how residual draws are constructed and whether paired empirical draws preserve only defensible historical dependence.
- Verify pooled nominal coverage and interval width.
- Explicitly audit by-position/by-season/value-tier coverage.
- Treat Q4/D10/WR-Q4 undercoverage as a material downstream limitation. Determine whether it is non-blocking with warning/contract restrictions or whether the Phase-6 distribution interface must be narrowed.
- Do not allow empirical quantiles to be described as calibrated guarantees for high-value players when evidence does not support that claim.

### D. Phase-6 input contract
- Verify `season_total_transform_v1` keeps central projection, uncertainty, availability warnings, provenance, fallbacks, upstream hashes, cutoff/as-of, and transform version distinct.
- Confirm no ESPN ADP/ECR timing, drafted state, roster need, opponent demand, survival, position runs, replacement/FLEX/MSV, or recommendation-policy signal is smuggled into the intrinsic Phase-5 contract.
- Determine whether Phase 6 may consume the central estimate while treating high-value interval outputs as warning/diagnostic only unless later calibration improves.

### E. Reproducibility / fallback
- Verify upstream replay tolerances and the claimed WR-033/WR-034 identities.
- Verify deterministic rerun hashes/bytes where available.
- Verify expected-games fallback composition is deterministic and explicitly flagged.
- Review the historical player-metadata archival risk and decide whether it is blocking or non-blocking for this research milestone.

### F. CI discrepancy
PR integration run `34556251098` attempt 1 failed in existing `scripts/test-command-bar.mjs` because the slot input was hidden/detached during reconstruction. PR #124 changes only `.ai/research/**`. Base `main` `f99490a124e6f6f14a76bfd1b639fbdb61d4e1c4` passed full War Room CI in run `34554030426`.

Manager requested attempt 2 on the same immutable head. Auditor must inspect its final status/logs.
- If attempt 2 passes and no evidence connects the prior timeout to WR-035, record attempt 1 as non-coupled/flaky CI evidence, not a research finding.
- If attempt 2 fails again, investigate enough to distinguish pre-existing UI flake from a reproducible integration blocker; do not modify production from the Auditor role.
- If the issue becomes cross-layer/uncertain after bounded investigation, return a concise escalation for Manager to consider temporary Troubleshooting.

## Acceptance / verdict
Use the Auditor charter severity model: CRITICAL / HIGH / MEDIUM / LOW.

Final verdict must be exactly one of:
- PASS
- PASS WITH NON-BLOCKING FINDINGS
- FAIL — REMEDIATION REQUIRED

A PASS-family verdict must state precisely what Phase-5 outputs are safe for later Phase-6 use and what high-value uncertainty claims remain prohibited/qualified.

Do not merge PR #124.

## Required outputs
Create/update under `.ai/auditor/**`:
- a task-specific WR-036 audit report with evidence and findings;
- concise `.ai/auditor/HANDOFF.md`.

Handoff must include exact target head, CI attempt-2 disposition, scope/integrity verdict, methodology/reproducibility verdict, distribution/high-value verdict, Phase-6 interface verdict, findings by severity, exact final verdict, and recommended Manager action.

## Normal-chat fallback
If Work mode is unavailable, perform the same audit sequentially in normal chat: chronology/scope -> generated outputs/code -> upstream replay -> distribution/high-value -> CI -> verdict/handoff. Work mode is not a dependency.
