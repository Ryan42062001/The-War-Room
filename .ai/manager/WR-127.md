# WR-127 — Legacy Overall Board Pressure Truthful Market Display Repair

TASK ID: WR-127
ROLE: Implementation Engineer / Builder
STATUS: ASSIGNED — START ONLY AFTER MANAGER ACTIVATION PR MERGE, MAIN GOVERNANCE SUCCESS AND EXACT BRANCH BOOTSTRAP
DATE: 2026-09-20
WORKFLOW: V3.5 CANONICAL
DEPENDENCY: INDEPENDENT — consumes CLOSED WR-126 diagnostic / WR-D041
EXECUTION MODE: WORK_MODE
REFRESH MODE: FAST_REFRESH
TARGET BRANCH: `wr-127-overall-board-pressure-truthful-market-display`
ASSIGNED BRANCH: `wr-127-overall-board-pressure-truthful-market-display`
PRE-ACTIVATION MAIN CHECKPOINT: `e69bd96d549786e5b3d8c045d086dd5884ce3266`
ACTIVATION: Manager must merge the WR-D041 control-plane PR, verify genuine canonical-main push Governance SUCCESS, then create this branch from that exact new main and record the initial SHA. The checkpoint above is not the permitted branch creation SHA.
AUDIT REQUIRED: YES — fresh independent Auditor on an immutable Manager-frozen Builder target before any production merge.
POST-MERGE CANARY REQUIRED: YES — genuine canonical-main FULL War Room CI after any independently audited implementation merge and before closure.

## Accepted evidence and objective

WR-126 reproduced the actual mounted legacy Overall Board Pressure widget presenting an internal missing-market neutral value of 50 as `50% next-pick survival` and a 50%-width probability-like meter at both 390x844 and 1280x900. The fixture used a real unmodified source row inside a deliberately restricted synthetic positional display pool. It proves a reachable presentation branch, not incidence in natural drafts, live ESPN behavior or calibrated odds.

Correct only that legacy display so missing market timing is explicitly unknown and known timing is described truthfully as an internal heuristic. Preserve the numerical engine and all recommendation/ranking behavior.

## Authorized write scope — exact

- `js/war-room-ui.js`
- `scripts/test-browser.mjs`
- `.ai/builder/WR127_OVERALL_BOARD_PRESSURE_TRUTHFUL_DISPLAY_EVIDENCE.md`
- `.ai/builder/HANDOFF.md`

No other production, scoring, ranking, source, Companion, workflow, dataset, CSS, package or test-runner file is authorized. If these exact files cannot safely implement and verify the behavior, stop and report the concrete blocker; do not broaden scope.

## Required behavior

### A. Unknown market

When `getMarketTimingDetails(best, context).marketRank == null` or equivalent valid market timing is unavailable:

- display clear wording equivalent to `Market timing UNKNOWN — no survival estimate`;
- do not display the engine's internal neutral 50 as `50%` survival;
- do not render a 50%-filled probability-like meter;
- do not expose an accessible numeric survival label for that unknown state;
- do not derive a user-facing scarcity or urgency judgment solely from that neutral fallback;
- retain independently supported tier, cliff and position-count information.

### B. Known market

- Preserve the existing numerical engine result and authoritative resolved market inputs.
- If a numeric timing value remains visible, label it clearly as an internal heuristic/timing index, not a measured or calibrated probability that the player survives.
- Keep presentation understandable on desktop and phone.

### C. Invariants and forbidden behavior

Do not change `calculateNextPickSurvival`, its internal scoring behavior, player ECR/rankings/tiers, market-data authority or resolver, selected/recommended player, recommendation action, candidate order, scoring weights, draft state, ESPN Companion/sync, or the accepted WR-122 recommendation-card repair. Do not add a second dashboard, target queue, roster panel, comparison engine, sync badge or unrelated Command Center feature.

## Tests and evidence contract

Reuse and extend the existing WR-126 mounted browser diagnostic in `scripts/test-browser.mjs`. At both 390x844 and 1280x900 verify:

1. genuine missing-market state shows unknown/no-estimate wording, no numeric survival percentage, no 50%-filled meter and no accessible numeric survival label;
2. known-market control retains the same engine numeric value/source while copy identifies it as heuristic/timing, not calibrated probability;
3. Position view continues hiding the legacy widget;
4. accessibility remains coherent;
5. recommendation player/action, selected player, scored candidate order/final scores and source-row attributes remain unchanged;
6. WR-122 recommendation presentation remains unchanged.

Run literal syntax checks for both edited JS/MJS files, the focused browser suite, applicable phone/responsive/scoring/resilience tests and genuine exact-final-head FULL War Room CI. Historical WR-126 old-copy assertions must be replaced with desired repaired behavior; do not preserve misleading `50%` as a desired regression oracle.

## Publication, freeze and audit gate

Builder opens one implementation PR to main with exactly the four authorized paths, publishes one immutable final head, complete evidence and exact-head FULL CI, then stops. Builder must not self-audit or merge.

Manager independently verifies scope/diff/tests/CI, freezes the exact candidate and separately activates a fresh Independent Auditor on its own branch/task/report/PR. Only a PASS-family verdict on the exact unchanged target permits a later Manager merge decision. After any merge, genuine canonical-main FULL CI must pass before WR-127 can close. A failure or moved target returns to bounded remediation and fresh audit; it never becomes a silent implementation loop.

## Preserved project boundaries

Preserve WR-D001 FantasyPros ECR player-value authority versus ESPN market timing, WR-D018 fallback-first ESPN architecture and `LIVE_DIRECT_UNVERIFIED`, WR-D027 NO PROVIDER CONTACT, WR-D038 accepted recommendation-card repair, historical failed audits, and paused protected custom ranking/source-rights gates. No live ESPN account, provider contact, source admission, deployment or draft-ready release is authorized.
