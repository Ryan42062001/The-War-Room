# WR-037 — Phase-5 Season-Total Audit Remediation

TASK ID: WR-037
ROLE: Research & Development (R&D)
STATUS: ASSIGNED
DATE: 2026-09-11
DEPENDENCY: HARD on WR-036 FAIL — REMEDIATION REQUIRED
EXECUTION MODE: WORK_MODE_PREFERRED
TARGET RESEARCH PR: #124
TARGET FAILED HEAD: `9b4769899dd73f7c94679df6b6c67158e3ee39b6`
PRODUCTION AUTHORIZATION: NONE

## Objective
Perform bounded remediation of the WR-036 findings against WR-035 without reopening unrelated ranking/model/strategy questions. Repair the evidence chain and protocol fidelity required to determine whether the Phase-5 season-total result can be independently certified.

Continue the existing WR-035 research branch / PR #124 unless a repository constraint makes that impossible. Do not create a second competing research result PR.

## Why this is needed
WR-036 independently returned `FAIL — REMEDIATION REQUIRED`. The proposed `INDEPENDENCE PRODUCT SUPPORTED` result is numerically promising, but Phase 5 cannot be frozen because exact frozen-WR-033 identity is not proven for the full 3,508-row scored cohort and several audit-identified protocol/interface gaps remain.

Phase 6 remains blocked until WR-037 is completed and WR-038 independently passes.

## Verified starting state
- `main` before Manager reconciliation: `aa586cdc0b5b8bff8100fb7bed9bad867e20162f`.
- WR-035 PR #124 remains open/unmerged at failed audited head `9b4769899dd73f7c94679df6b6c67158e3ee39b6`.
- WR-036 audited that immutable head and returned `FAIL — REMEDIATION REQUIRED`.
- Current published audit PR #125 head at Manager disposition is `d153d3d1960c1def42358479b611ab7d3423a5c1`; it is later than audit checkpoint `16a48a2c5fde58d43504ebcaa7990ccd49c15086` and contains the superset of WR-036 findings.
- WR-034 keyed replay was independently verified across all 3,508 rows with maximum prediction delta about `2.19e-13`.
- The paired challenger genuinely failed the frozen development gate.
- High-value uncertainty undercoverage was correctly qualified and is not itself the primary blocker.

## Scope / dependency classification
HARD dependency on WR-036 findings. This task is remediation only.

Allowed work: `.ai/research/**` needed to remediate WR-035 and update its research evidence/handoff.

Forbidden:
- `.ai/shared/**`
- `.ai/manager/**`
- `.ai/auditor/**`
- production code / production rankings
- `.ai/research/generated/CONTEXT_SHADOW_2026_SNAPSHOT.csv`
- `.ai/research/WR023_2026_PROSPECTIVE_EVALUATION_PROTOCOL.md`
- `.ai/research/generated/WR023_PROTOCOL_MANIFEST.json`
- 2026 regular-season outcomes
- Phase-6 replacement/FLEX/MSV/value work
- retuning WR-033 or WR-034 from target results

## Required remediation

### 1. Resolve WR-036-AUD-01 — full-cohort exact WR-033 identity
Establish keyed exact WR-033 feature/prediction identity for **all 3,508 scored returner rows**, including the 1,627 zero-game rows.

Required evidence:
- stable key at minimum `(player_id, position, target_season)`;
- exact frozen WR-033 feature values or deterministic keyed feature hash sufficient to prove identity;
- exact WR-033 prediction for every scored key;
- row-for-row comparison against a trustworthy frozen or independently reconstructed reference;
- persisted machine-readable keyed replay/integrity artifact;
- fail-closed behavior on missing/duplicate keys or any feature/prediction mismatch.

Aggregate MAE/RMSE/Spearman equality is not sufficient.

Preferred paths:
1. reacquire/reconstruct the original locked player metadata from an immutable/provenance-safe source; or
2. independently prove replacement metadata yields identical frozen WR-033 features and predictions for every scored key.

If exact identity cannot be established, STOP and return `REMEDIATION BLOCKED — UPSTREAM IDENTITY NOT PROVABLE`; do not relabel the substituted-input experiment as exact frozen WR-033 replay and do not invent a retrospective equivalence rule.

### 2. Resolve WR-036-AUD-02 — residual fallback order
Implement the frozen fallback sequence exactly:
1. same-position prior OOS residual pairs when sufficient;
2. same-position in-sample training residuals with explicit `TRAINING_RESIDUAL_FALLBACK` when prior OOS is insufficient;
3. pooled prior OOS fallback only after the prior step is insufficient;
4. explicit distribution-unavailable state if still insufficient.

Add deterministic targeted validation that forces each path. If a different fallback policy is now believed necessary, do not silently change the old protocol; record a bounded remediation addendum before any rescoring and surface the change for Manager/Auditor review.

### 3. Resolve WR-036-AUD-03 — repeated-player contrasts
Implement the predeclared 5,000-replicate stable-player clustered comparison for all required primary contrasts:
- MAE;
- RMSE;
- interval score.

Preserve the frozen clustering rule, deterministic seed semantics, challenger-minus-independence sign convention, and report point contrasts plus confidence intervals.

### 4. Resolve WR-036-AUD-04 — central/distribution coherence
Before rescoring, freeze a bounded remediation addendum that explicitly defines the relationship between the selected central estimator and uncertainty draws/quantiles.

Either:
- produce a defensible deterministic distribution construction coherent with the selected central estimator; or
- narrow the Phase-5 interface so the empirical paired quantiles are explicitly diagnostic residual uncertainty and are not represented as a coherent predictive distribution around the raw central estimator.

Do not retroactively tune based on confirmation outcomes. Any changed construction must be fixed before rerunning affected scoring and must be re-evaluated chronologically.

### 5. Resolve WR-036-AUD-05 — negative/out-of-domain central values
Before rescoring, freeze an explicit fail-closed/fallback policy for impossible negative active-game or season-total expectations. The policy must be deterministic, documented, and applied consistently in central and uncertainty outputs.

If the policy changes any central projection, recompute all affected central/dependence/distribution/high-value metrics and selection evidence rather than preserving stale numbers.

## Remediation protocol discipline
Create and commit a concise remediation addendum / machine lock **before** running new affected scoring. It may correct audit-identified fidelity/integrity defects but may not opportunistically reopen previously rejected model families, change confirmation gates after seeing results, or inspect 2026 outcomes.

Preserve the original WR-035 protocol/result history; do not rewrite history to make the remediation appear pre-existing.

## Required rerun / validation
- deterministic full WR-035 rerun from the remediated contract;
- byte/hash reproducibility evidence for retained artifacts;
- exact keyed WR-033 full-cohort identity evidence;
- exact WR-034 keyed replay retained;
- explicit targeted fallback-path tests;
- clustered MAE/RMSE/interval-score evidence;
- central/distribution coherence report;
- negative/out-of-domain policy evidence;
- recomputed central/distribution/high-value metrics wherever inputs/policies changed;
- no 2026 outcomes inspected;
- no WR-021/WR-023/WR-033/WR-034 specification change;
- PR #124 remains research-only;
- exact final PR head and exact-head CI observed.

Known command-bar UI flake must not be hidden. If exact-head CI fails in the previously observed hidden/detached command-bar test, record it and use at most one bounded unchanged-head retry before escalating.

## Acceptance criteria
WR-037 is complete only when:
- WR-036-AUD-01 through WR-036-AUD-05 are each explicitly resolved, or the task returns a fail-closed blocked disposition where permitted above;
- full 3,508-row WR-033 keyed identity is independently auditable;
- protocol fallback fidelity is fixed;
- clustered MAE/RMSE/interval-score contrasts are present;
- central/uncertainty semantics are coherent and honestly scoped;
- impossible negative outputs have a predeclared deterministic policy;
- all affected historical results are rerun under the fixed contract;
- deterministic reproducibility is demonstrated;
- frozen/prospective and production boundaries remain intact;
- `.ai/research/HANDOFF.md` is updated;
- PR #124 is updated to one immutable remediation head for re-audit.

## Expected handoff
HANDOFF
Task ID: WR-037
Role: R&D
Status: COMPLETE — RE-AUDIT REQUIRED | REMEDIATION BLOCKED
Starting PR/head:
Final PR/head:
Execution mode used:
Remediation addendum/hash:
WR-036-AUD-01 full-cohort keyed WR-033 identity:
WR-036-AUD-02 fallback order:
WR-036-AUD-03 clustered MAE/RMSE/interval-score:
WR-036-AUD-04 central/distribution coherence:
WR-036-AUD-05 negative/out-of-domain policy:
WR-034 keyed replay:
Metrics changed from WR-035 original:
Deterministic rerun:
2026 outcomes inspected: NO
Frozen WR-021/WR-023 changed: NO
WR-033/WR-034 specification changed: NO
Production changed: NO
Exact-head CI:
Blocking issues:
Recommended next role: Independent Auditor / QA
Exact next action:
Checkpoint / SHA:

## Normal-chat fallback
If Work mode is unavailable, execute sequentially: full-cohort WR-033 identity -> remediation addendum lock -> fallback/contrast/coherence/out-of-domain fixes -> deterministic rerun -> exact-head CI -> research handoff. Work mode is an accelerator, not a dependency.
