# WR-085 — Workflow V3.4 ChatGPT-Usage Efficiency Upgrade

TASK ID: WR-085
ROLE: Manager / Architect
STATUS: AUDIT_READY
DATE: 2026-09-17
DEPENDENCY: INDEPENDENT — bounded workflow/control-plane efficiency lane
EXECUTION MODE: STANDARD_CHAT
TARGET BRANCH: `manager/wr-085-workflow-v34-efficiency`
REFRESH MODE: FULL_REFRESH
REFRESH REASON: Canonical workflow/control-plane architecture is being changed; this is an explicit Full Refresh exception.
AUDIT REQUIRED: YES — WR-086
POST-MERGE CANARY REQUIRED: YES
PRODUCTION AUTHORIZATION: NONE

## Objective

Implement the smallest safe Workflow V3.4 candidate that reduces ChatGPT usage, repeated-context overhead, unnecessary Work-mode consumption, premature audits, fresh-chat churn, and duplicated reasoning while preserving all V3.3 safety guarantees.

## Required policy

Adopt:
- `STANDARD_CHAT_HIGH` as the default execution mode;
- `WORK_MODE` only when hands-on autonomous execution materially reduces interaction/overhead;
- `FAST_REFRESH` as the default refresh mode;
- `FULL_REFRESH` only for documented exceptions;
- explicit Manager execution packets;
- strong accepted-decision consumption;
- compact continuation-oriented handoffs;
- self-validation before audit freeze;
- safe same-role chat reuse when independence is not required;
- worker-spawn cost checks;
- Work escalation and de-escalation contracts;
- batching of tightly related low-risk follow-up work where role/separation rules permit.

## Machine-safety requirements

Where practical, make execution/refresh mode drift fail closed. Preserve exact task/branch/PR/SHA controls, collision safety, audit independence, custody/provider restrictions, Manager merge authority, CI gates, post-merge canaries, fail-closed behavior and task ownership.

Do not weaken or bypass V3.3 controls to save tokens.

## Active-task review

Reclassify active tasks only where the new policy supports it. Record both changes and deliberate non-changes. In particular assess WR-074/075/081/082/083/084.

## Validation

Before audit freeze:
- inspect complete diff;
- run workflow static-state and task-contract regressions;
- run Manager-transition and lane-identity regressions;
- run any new execution/refresh-mode regression;
- run relevant syntax/tests;
- require exact-head War Room CI;
- confirm no product/research/custody/workflow-security behavior was weakened.

## Publication

Publish one immutable WR-085 candidate PR. Do not self-audit or merge it as canonical. Return exact head, changed files, tests, CI, active-task reclassifications, and documented residual risks to Manager freeze / WR-086.

## Manager exact freeze — 2026-09-17

Frozen audit target:
- PR: #230
- branch: `manager/wr-085-workflow-v34-efficiency`
- exact head: `0c7cc69e382b04ce8c1059851ca2fc3dcfdc5a6b`
- exact-head CI: `35299527394` — SUCCESS
- classify `105459050842` SUCCESS
- governance `105459089895` SUCCESS
- full test `105459120484` SUCCESS

The earlier head `2e624d9ac14841d5e11cf45f2ec28a213301b49b` is preserved as a self-validation failure that caught an allowlist filename typo before audit. Auditor must audit only the frozen head above.
